"use strict";
/*
 * ATTENTION: The "eval" devtool has been used (maybe by default in mode: "development").
 * This devtool is neither made for production nor for readable output files.
 * It uses "eval()" calls to create a separate source file in the browser devtools.
 * If you are trying to read the output file, select a different devtool (https://webpack.js.org/configuration/devtool/)
 * or disable the default devtool with "devtool: false".
 * If you are looking for production-ready output files, see mode: "production" (https://webpack.js.org/configuration/mode/).
 */
(self["webpackChunk_syncfusion_ej2_documents_vue_samples"] = self["webpackChunk_syncfusion_ej2_documents_vue_samples"] || []).push([["chunk-vendors-node_modules_syncfusion_ej2-pdf-data-extract_src_pdf-data-extract_core_image-extractio-0e8a7488"],{

/***/ "./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/image-extraction/colorspace.js":
/*!****************************************************************************************************************!*\
  !*** ./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/image-extraction/colorspace.js ***!
  \****************************************************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   _PdfAlternateCS: () => (/* binding */ _PdfAlternateCS),\n/* harmony export */   _PdfCalGrayCS: () => (/* binding */ _PdfCalGrayCS),\n/* harmony export */   _PdfColorPalette: () => (/* binding */ _PdfColorPalette),\n/* harmony export */   _PdfColorRgbConverter: () => (/* binding */ _PdfColorRgbConverter),\n/* harmony export */   _PdfDeviceCmykCS: () => (/* binding */ _PdfDeviceCmykCS),\n/* harmony export */   _PdfDeviceGrayCS: () => (/* binding */ _PdfDeviceGrayCS),\n/* harmony export */   _PdfDeviceRgbCS: () => (/* binding */ _PdfDeviceRgbCS),\n/* harmony export */   _PdfDeviceRgbaCS: () => (/* binding */ _PdfDeviceRgbaCS),\n/* harmony export */   _PdfIndexedCS: () => (/* binding */ _PdfIndexedCS),\n/* harmony export */   _PdfLabCS: () => (/* binding */ _PdfLabCS),\n/* harmony export */   _PdfPatternCS: () => (/* binding */ _PdfPatternCS)\n/* harmony export */ });\n/* harmony import */ var _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @syncfusion/ej2-pdf */ \"./node_modules/@syncfusion/ej2-pdf/index.js\");\nvar __extends = (undefined && undefined.__extends) || (function () {\n    var extendStatics = function (d, b) {\n        extendStatics = Object.setPrototypeOf ||\n            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||\n            function (d, b) { for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p]; };\n        return extendStatics(d, b);\n    };\n    return function (d, b) {\n        extendStatics(d, b);\n        function __() { this.constructor = d; }\n        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());\n    };\n})();\nvar __awaiter = (undefined && undefined.__awaiter) || function (thisArg, _arguments, P, generator) {\n    return new (P || (P = Promise))(function (resolve, reject) {\n        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }\n        function rejected(value) { try { step(generator[\"throw\"](value)); } catch (e) { reject(e); } }\n        function step(result) { result.done ? resolve(result.value) : new P(function (resolve) { resolve(result.value); }).then(fulfilled, rejected); }\n        step((generator = generator.apply(thisArg, _arguments || [])).next());\n    });\n};\nvar __generator = (undefined && undefined.__generator) || function (thisArg, body) {\n    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g;\n    return g = { next: verb(0), \"throw\": verb(1), \"return\": verb(2) }, typeof Symbol === \"function\" && (g[Symbol.iterator] = function() { return this; }), g;\n    function verb(n) { return function (v) { return step([n, v]); }; }\n    function step(op) {\n        if (f) throw new TypeError(\"Generator is already executing.\");\n        while (_) try {\n            if (f = 1, y && (t = op[0] & 2 ? y[\"return\"] : op[0] ? y[\"throw\"] || ((t = y[\"return\"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;\n            if (y = 0, t) op = [op[0] & 2, t.value];\n            switch (op[0]) {\n                case 0: case 1: t = op; break;\n                case 4: _.label++; return { value: op[1], done: false };\n                case 5: _.label++; y = op[1]; op = [0]; continue;\n                case 7: op = _.ops.pop(); _.trys.pop(); continue;\n                default:\n                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }\n                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }\n                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }\n                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }\n                    if (t[2]) _.ops.pop();\n                    _.trys.pop(); continue;\n            }\n            op = body.call(thisArg, _);\n        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }\n        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };\n    }\n};\n\n/**\n * Base palette for PDF color spaces, exposing helpers to convert to RGB buffers.\n *\n * @private\n */\nvar _PdfColorPalette = /** @class */ (function () {\n    /**\n     * Initializes a new instance of the `_PdfColorPalette` class.\n     *\n     * @private\n     * @param {string} name Color space name.\n     * @param {number} numComps Number of color components.\n     * @returns {void} nothing.\n     */\n    function _PdfColorPalette(name, numComps) {\n        this.name = name;\n        this.numComps = numComps;\n    }\n    /**\n     * Resamples (nearest-neighbor) an RGB image buffer, with optional alpha interleave control.\n     *\n     * @private\n     * @param {Uint8Array} src Source RGB bytes.\n     * @param {Uint8Array} dest Destination buffer to write into.\n     * @param {number} w1 Source width.\n     * @param {number} h1 Source height.\n     * @param {number} w2 Target width.\n     * @param {number} h2 Target height.\n     * @param {number} alpha01 0 for no alpha slot in dest, 1 to skip alpha slot per pixel.\n     * @returns {void} nothing.\n     */\n    _PdfColorPalette.prototype._resizeRgbImage = function (src, dest, w1, h1, w2, h2, alpha01) {\n        var components = 3;\n        alpha01 = alpha01 !== 1 ? 0 : alpha01;\n        var xRatio = w1 / w2;\n        var yRatio = h1 / h2;\n        var newIndex = 0;\n        var oldIndex;\n        var xScaled = new Uint16Array(w2);\n        var w1Scanline = w1 * components;\n        for (var i = 0; i < w2; i++) {\n            xScaled[i] = Math.floor(i * xRatio) * components;\n        }\n        for (var i = 0; i < h2; i++) {\n            var py = Math.floor(i * yRatio) * w1Scanline;\n            for (var j = 0; j < w2; j++) {\n                oldIndex = py + xScaled[j];\n                dest[newIndex++] = src[oldIndex++];\n                dest[newIndex++] = src[oldIndex++];\n                dest[newIndex++] = src[oldIndex++];\n                newIndex += alpha01;\n            }\n        }\n    };\n    /**\n     * Determines whether the provided decode array represents the default decode for this space.\n     *\n     * @private\n     * @param {any} decode The decode array or value.\n     * @param {number} numComps The number of components.\n     * @returns {boolean} `true` if decode is default; otherwise, `false`.\n     */\n    _PdfColorPalette.prototype._isDefaultDecode = function (decode, numComps) {\n        numComps = this.numComps;\n        if (!Array.isArray(decode)) {\n            return true;\n        }\n        if (numComps * 2 !== decode.length) {\n            return true;\n        }\n        for (var i = 0, ii = decode.length; i < ii; i += 2) {\n            if (decode[i] !== 0 || decode[i + 1] !== 1) {\n                return false;\n            }\n        }\n        return true;\n    };\n    /**\n     * Gets a single RGB triplet from source data at the given offset.\n     *\n     * @private\n     * @param {Uint8Array} src Source buffer.\n     * @param {number} srcOffset Byte offset in source.\n     * @returns {Uint8ClampedArray} A 3 byte clamped array [r,g,b].\n     */\n    _PdfColorPalette.prototype._getRgb = function (src, srcOffset) {\n        var rgb = new Uint8ClampedArray(3);\n        this._getRgbItem(src, srcOffset, rgb, 0);\n        return rgb;\n    };\n    /**\n     * Reads one color sample and writes a single RGB triplet into destination.\n     * Must be implemented by concrete color spaces.\n     *\n     * @private\n     * @param {any} src Source buffer.\n     * @param {number} srcOffset Start offset in source.\n     * @param {any} dest Destination buffer for RGB output.\n     * @param {number} destOffset Start offset in destination.\n     * @returns {void} nothing.\n     */\n    _PdfColorPalette.prototype._getRgbItem = function (src, srcOffset, dest, destOffset) {\n        (0,_syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__._unreachable)('Should not call _PdfColorPalette._getRgbItem');\n    };\n    /* eslint-disable */\n    /**\n     * Converts a sequence of samples into RGB and writes to destination buffer.\n     * Must be implemented by concrete color spaces.\n     *\n     * @private\n     * @param {any} src Source buffer.\n     * @param {number} srcOffset Start offset in source.\n     * @param {number} count Number of samples to process.\n     * @param {any} dest Destination buffer for RGB output.\n     * @param {number} destOffset Start offset in destination.\n     * @param {number} bits Bits per component in source samples.\n     * @param {number} alpha01 0 for no alpha slot, 1 to skip alpha slot per pixel.\n     * @returns {any} nothing.\n     */\n    _PdfColorPalette.prototype._getRgbBuffer = function (src, srcOffset, count, dest, destOffset, bits, alpha01) {\n        (0,_syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__._unreachable)('Should not call _PdfColorPalette._getRgbBuffer');\n    };\n    /* eslint-enable */\n    /**\n     * Computes the required output length in bytes for a given input length.\n     * Must be implemented by concrete color spaces.\n     *\n     * @private\n     * @param {number} inputLength Number of input scalar values.\n     * @param {number} alpha01 0 or 1 indicating extra alpha slot in output.\n     * @returns {void} nothing.\n     */\n    _PdfColorPalette.prototype._getOutputLength = function (inputLength, alpha01) {\n        (0,_syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__._unreachable)('Should not call _PdfColorPalette._getOutputLength');\n    };\n    /**\n     * Indicates whether the color space path can be passed through unchanged for given bpc.\n     *\n     * @private\n     * @param {any} bits Bits per component.\n     * @returns {boolean} `true` if passthrough is allowed otherwise, `false`.\n     */\n    _PdfColorPalette.prototype._isPass = function (bits) {\n        return false;\n    };\n    /**\n     * Fills an RGB destination buffer from component data, handling optional resize and alpha.\n     *\n     * @private\n     * @param {any} dest Destination buffer for RGB output.\n     * @param {number} originalWidth Source width.\n     * @param {number} originalHeight Source height.\n     * @param {number} width Target width.\n     * @param {number} height Target height.\n     * @param {number} actualHeight Actual rendered height.\n     * @param {number} bpc Bits per component.\n     * @param {Uint8Array | Uint16Array} comps Component data buffer.\n     * @param {number} alpha01 0 or 1 to control alpha slot.\n     * @returns {Promise<any>} Resolves to the destination buffer.\n     */\n    _PdfColorPalette.prototype._fillRgb = function (dest, originalWidth, originalHeight, width, height, // eslint-disable-line\n    actualHeight, bpc, comps, alpha01) {\n        return __awaiter(this, void 0, void 0, function () {\n            var count, numComponentColors, needsResizing, rgbBuf, allColors, i, colorMap, destPos, i, key, rgbPos, i, key, destPos, rgbPos, i, ii;\n            return __generator(this, function (_a) {\n                switch (_a.label) {\n                    case 0:\n                        count = originalWidth * originalHeight;\n                        numComponentColors = 1 << bpc;\n                        needsResizing = originalHeight !== height || originalWidth !== width;\n                        rgbBuf = null;\n                        if (!this._isPass(bpc)) return [3 /*break*/, 1];\n                        rgbBuf = comps;\n                        return [3 /*break*/, 7];\n                    case 1:\n                        if (!(this.numComps === 1 && count > numComponentColors && this.name !== 'DeviceGray' && this.name !== 'DeviceRGB')) return [3 /*break*/, 3];\n                        allColors = bpc <= 8 ? new Uint8Array(numComponentColors) : new Uint16Array(numComponentColors);\n                        for (i = 0; i < numComponentColors; i++) {\n                            allColors[i] = i;\n                        }\n                        colorMap = new Uint8ClampedArray(numComponentColors * 3);\n                        return [4 /*yield*/, this._getRgbBuffer(allColors, 0, numComponentColors, colorMap, 0, bpc, 0)];\n                    case 2:\n                        _a.sent();\n                        if (!needsResizing) {\n                            destPos = 0;\n                            for (i = 0; i < count; ++i) {\n                                key = comps[i] * 3;\n                                dest[destPos++] = colorMap[key];\n                                dest[destPos++] = colorMap[key + 1];\n                                dest[destPos++] = colorMap[key + 2];\n                                destPos += alpha01;\n                            }\n                        }\n                        else {\n                            rgbBuf = new Uint8Array(count * 3);\n                            rgbPos = 0;\n                            for (i = 0; i < count; ++i) {\n                                key = comps[i] * 3;\n                                rgbBuf[rgbPos++] = colorMap[key];\n                                rgbBuf[rgbPos++] = colorMap[key + 1];\n                                rgbBuf[rgbPos++] = colorMap[key + 2];\n                            }\n                        }\n                        return [3 /*break*/, 7];\n                    case 3:\n                        if (!!needsResizing) return [3 /*break*/, 5];\n                        return [4 /*yield*/, this._getRgbBuffer(comps, 0, width * actualHeight, dest, 0, bpc, alpha01)];\n                    case 4:\n                        _a.sent();\n                        return [3 /*break*/, 7];\n                    case 5:\n                        rgbBuf = new Uint8ClampedArray(count * 3);\n                        return [4 /*yield*/, this._getRgbBuffer(comps, 0, count, rgbBuf, 0, bpc, 0)];\n                    case 6:\n                        _a.sent();\n                        _a.label = 7;\n                    case 7:\n                        if (rgbBuf) {\n                            if (needsResizing) {\n                                this._resizeRgbImage(rgbBuf, dest, originalWidth, originalHeight, width, height, alpha01);\n                            }\n                            else {\n                                destPos = 0;\n                                rgbPos = 0;\n                                for (i = 0, ii = width * actualHeight; i < ii; i++) {\n                                    dest[destPos++] = rgbBuf[rgbPos++];\n                                    dest[destPos++] = rgbBuf[rgbPos++];\n                                    dest[destPos++] = rgbBuf[rgbPos++];\n                                    destPos += alpha01;\n                                }\n                            }\n                        }\n                        return [2 /*return*/, dest];\n                }\n            });\n        });\n    };\n    return _PdfColorPalette;\n}());\n\n/**\n * Alternate/tint color space mapping to a base color space.\n *\n * @private\n */\nvar _PdfAlternateCS = /** @class */ (function (_super) {\n    __extends(_PdfAlternateCS, _super);\n    /**\n     * Initializes a new instance of the `_PdfAlternateCS` class.\n     *\n     * @private\n     * @param {number} numComps Number of components in the tint space.\n     * @param {_PdfColorPalette} base Base color palette to map into.\n     * @returns {void} nothing.\n     */\n    function _PdfAlternateCS(numComps, base) {\n        var _this = _super.call(this, 'Alternate', numComps) || this;\n        _this.base = base;\n        _this.tmpBuf = new Float32Array(base.numComps);\n        return _this;\n    }\n    /**\n     * Converts a single tinted sample to RGB via the base color space.\n     *\n     * @private\n     * @param {Uint8Array} src Source buffer.\n     * @param {number} srcOffset Offset in source.\n     * @param {Uint8ClampedArray} dest Destination buffer for RGB output.\n     * @param {number} destOffset Destination offset.\n     * @returns {void} nothing.\n     */\n    _PdfAlternateCS.prototype._getRgbItem = function (src, srcOffset, dest, destOffset) {\n        var tmpBuf = this.tmpBuf; // eslint-disable-line\n        this.base._getRgbItem(tmpBuf, 0, dest, destOffset);\n    };\n    /**\n     * Converts a sequence of tinted samples to RGB via the base color space.\n     *\n     * @private\n     * @param {Uint8Array} src Source buffer.\n     * @param {number} srcOffset Offset in source.\n     * @param {number} count Number of samples.\n     * @param {Uint8Array} dest Destination buffer for RGB output.\n     * @param {number} destOffset Destination offset.\n     * @param {number} bits Bits per component.\n     * @param {number} alpha01 Alpha slot flag (0 or 1).\n     * @returns {void} nothing.\n     */\n    _PdfAlternateCS.prototype._getRgbBuffer = function (src, srcOffset, count, dest, destOffset, bits, alpha01) {\n        var base = this.base; // eslint-disable-line\n        var scale = 1 / ((1 << bits) - 1);\n        var baseNumComps = base.numComps; // eslint-disable-line\n        var usesZeroToOneRange = base.usesZeroToOneRange; // eslint-disable-line\n        var isPass = (base._isPass(8) || !usesZeroToOneRange) && alpha01 === 0;\n        var pos = isPass ? destOffset : 0;\n        var baseBuf = isPass ? dest : new Uint8ClampedArray(baseNumComps * count); // eslint-disable-line;\n        var numComps = this.numComps;\n        var scaled = new Float32Array(numComps);\n        var tinted = new Float32Array(baseNumComps);\n        for (var i = 0; i < count; i++) {\n            for (var j = 0; j < numComps; j++) {\n                scaled[j] = src[srcOffset++] * scale;\n            }\n            if (usesZeroToOneRange) {\n                for (var j = 0; j < baseNumComps; j++) {\n                    baseBuf[pos++] = tinted[j] * 255;\n                }\n            }\n            else {\n                base._getRgbItem(tinted, 0, baseBuf, pos);\n                pos += baseNumComps;\n            }\n        }\n        if (!isPass) {\n            base._getRgbBuffer(baseBuf, 0, count, dest, destOffset, 8, alpha01);\n        }\n    };\n    return _PdfAlternateCS;\n}(_PdfColorPalette));\n\n/**\n * Pattern color space that may refer to a base color space for uncolored tiling patterns.\n *\n * @private\n */\nvar _PdfPatternCS = /** @class */ (function (_super) {\n    __extends(_PdfPatternCS, _super);\n    function _PdfPatternCS(baseCS) {\n        var _this = _super.call(this, 'Pattern', null) || this;\n        _this.base = baseCS;\n        return _this;\n    }\n    /**\n     * Pattern color space does not use decode maps calling this is invalid.\n     *\n     * @private\n     * @param {any} decodeMap Decode map.\n     * @param {any} bpc Bits per component.\n     * @throws {Error} Always thrown since not applicable to PatternCS.\n     * @returns {boolean} true if it is default decode.\n     */\n    /* eslint-enable */\n    _PdfPatternCS.prototype._isDefaultDecode = function (decodeMap, bpc) {\n        throw new Error('PatternCS._isDefaultDecode should not be called.');\n    };\n    return _PdfPatternCS;\n}(_PdfColorPalette));\n\n/**\n * Indexed color space using a lookup table to map indices to base-space colors.\n *\n * @private\n */\nvar _PdfIndexedCS = /** @class */ (function (_super) {\n    __extends(_PdfIndexedCS, _super);\n    /**\n     * Initializes a new instance of the `_PdfIndexedCS` class.\n     *\n     * @private\n     * @param {any} base Base color space.\n     * @param {number} highVal Highest index value.\n     * @param {any} lookup Lookup source.\n     * @returns {void} nothing.\n     * @throws {FormatError} When lookup source is not recognized.\n     */\n    function _PdfIndexedCS(base, highVal, lookup) {\n        var _this = _super.call(this, 'Indexed', 1) || this;\n        _this.base = base;\n        _this.highVal = highVal;\n        var length = base.numComps * (highVal + 1);\n        _this.lookup = new Uint8Array(length);\n        if (lookup instanceof _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__._PdfBaseStream) {\n            var bytes = lookup.getBytes(length);\n            _this.lookup.set(bytes);\n        }\n        else if (typeof lookup === 'string') {\n            for (var i = 0; i < length; ++i) {\n                _this.lookup[i] = lookup.charCodeAt(i) & 0xff;\n            }\n        }\n        else {\n            throw new _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__.FormatError(\"IndexedCS - unrecognized lookup table: \" + lookup);\n        }\n        return _this;\n    }\n    /**\n     * Converts a single index to RGB using the lookup table.\n     *\n     * @private\n     * @param {any} src Source buffer containing indices.\n     * @param {number} srcOffset Offset in source.\n     * @param {Uint8ClampedArray} dest Destination RGB buffer.\n     * @param {number} destOffset Destination offset.\n     * @returns {void} nothing.\n     */\n    _PdfIndexedCS.prototype._getRgbItem = function (src, srcOffset, dest, destOffset) {\n        var _a = this, base = _a.base, highVal = _a.highVal, lookup = _a.lookup;\n        var start = (0,_syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__._mathClamp)(Math.round(src[srcOffset]), 0, highVal) * base.numComps;\n        base._getRgbBuffer(lookup, start, 1, dest, destOffset, 8, 0);\n    };\n    /**\n     * Converts a sequence of indices to RGB using the lookup table.\n     *\n     * @private\n     * @param {any} src Source indices.\n     * @param {number} srcOffset Source offset.\n     * @param {number} count Number of indices to process.\n     * @param {Uint8ClampedArray} dest Destination RGB buffer.\n     * @param {number} destOffset Destination offset.\n     * @param {number} bits Bits per component (ignored; indices are 8-bit after clamp).\n     * @param {number} alpha01 Alpha slot flag (0 or 1).\n     * @returns {void} nothing.\n     */\n    _PdfIndexedCS.prototype._getRgbBuffer = function (src, srcOffset, count, dest, destOffset, // eslint-disable-line\n    bits, alpha01) {\n        var _a = this, base = _a.base, highVal = _a.highVal, lookup = _a.lookup;\n        var numComps = base.numComps;\n        var outputDelta = base._getOutputLength(numComps, alpha01); // eslint-disable-line\n        for (var i = 0; i < count; ++i) {\n            var lookupPos = (0,_syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__._mathClamp)(Math.round(src[srcOffset++]), 0, highVal) * numComps;\n            base._getRgbBuffer(lookup, lookupPos, 1, dest, destOffset, 8, alpha01);\n            destOffset += outputDelta;\n        }\n    };\n    /**\n     * Computes output byte length for the given input length, considering the base space.\n     *\n     * @private\n     * @param {number} inputLength Number of input elements.\n     * @param {number} alpha01 Alpha slot flag.\n     * @returns {number} Bytes required in output.\n     */\n    _PdfIndexedCS.prototype._getOutputLength = function (inputLength, alpha01) {\n        return this.base._getOutputLength(inputLength * this.base.numComps, alpha01);\n    };\n    /**\n     * Determines if the provided decode map equals the default mapping for indexed space.\n     *\n     * @private\n     * @param {any[]} decodeMap Decode array.\n     * @param {number} bpc Bits per component.\n     * @returns {boolean} `true` if default otherwise, `false`.\n     */\n    _PdfIndexedCS.prototype._isDefaultDecode = function (decodeMap, bpc) {\n        if (!Array.isArray(decodeMap)) {\n            return true;\n        }\n        if (decodeMap.length !== 2) {\n            return true;\n        }\n        if (!Number.isInteger(bpc) || bpc < 1) {\n            return true;\n        }\n        return decodeMap[0] === 0 && decodeMap[1] === (1 << bpc) - 1;\n    };\n    return _PdfIndexedCS;\n}(_PdfColorPalette));\n\nvar _PdfDeviceGrayCS = /** @class */ (function (_super) {\n    __extends(_PdfDeviceGrayCS, _super);\n    function _PdfDeviceGrayCS() {\n        return _super.call(this, 'DeviceGray', 1) || this;\n    }\n    /**\n     * Converts one gray sample to RGB.\n     *\n     * @private\n     * @param {any} src Source buffer.\n     * @param {number} srcOffset Source offset.\n     * @param {Uint8ClampedArray} dest Destination RGB.\n     * @param {number} destOffset Destination offset.\n     * @returns {void} nothing.\n     */\n    _PdfDeviceGrayCS.prototype._getRgbItem = function (src, srcOffset, dest, destOffset) {\n        var c = src[srcOffset] * 255;\n        dest[destOffset] = dest[destOffset + 1] = dest[destOffset + 2] = c;\n    };\n    /**\n     * Converts multiple gray samples to RGB.\n     *\n     * @private\n     * @param {any} src Source samples.\n     * @param {number} srcOffset Source offset.\n     * @param {number} count Number of samples.\n     * @param {Uint8ClampedArray} dest Destination RGB.\n     * @param {number} destOffset Destination offset.\n     * @param {number} bits Bits per component.\n     * @param {number} alpha01 Alpha slot flag.\n     * @returns {void} nothing.\n     */\n    _PdfDeviceGrayCS.prototype._getRgbBuffer = function (src, srcOffset, count, dest, destOffset, // eslint-disable-line\n    bits, alpha01) {\n        var scale = 255 / ((1 << bits) - 1);\n        var j = srcOffset;\n        var q = destOffset;\n        for (var i = 0; i < count; ++i) {\n            var c = scale * src[j++];\n            dest[q++] = c;\n            dest[q++] = c;\n            dest[q++] = c;\n            q += alpha01;\n        }\n    };\n    /**\n     * Computes the output byte length for a given input length.\n     *\n     * @private\n     * @param {number} inputLength Number of gray samples.\n     * @param {number} alpha01 Alpha slot flag (0 or 1).\n     * @returns {number} Number of bytes required in output.\n     */\n    _PdfDeviceGrayCS.prototype._getOutputLength = function (inputLength, alpha01) {\n        return inputLength * (3 + alpha01);\n    };\n    return _PdfDeviceGrayCS;\n}(_PdfColorPalette));\n\n/**\n * DeviceRGB color space implementation.\n *\n * @private\n */\nvar _PdfDeviceRgbCS = /** @class */ (function (_super) {\n    __extends(_PdfDeviceRgbCS, _super);\n    /**\n     * Initializes a new instance of the `_PdfDeviceRgbCS` class.\n     *\n     * @private\n     * @returns {void} nothing.\n     */\n    function _PdfDeviceRgbCS() {\n        return _super.call(this, 'DeviceRGB', 3) || this;\n    }\n    /**\n     * Copies one RGB sample to destination .\n     *\n     * @private\n     * @param {any} src Source buffer.\n     * @param {number} srcOffset Source offset.\n     * @param {Uint8ClampedArray} dest Destination RGB.\n     * @param {number} destOffset Destination offset.\n     * @returns {void} nothing.\n     */\n    _PdfDeviceRgbCS.prototype._getRgbItem = function (src, srcOffset, dest, destOffset) {\n        dest[destOffset] = src[srcOffset] * 255;\n        dest[destOffset + 1] = src[srcOffset + 1] * 255;\n        dest[destOffset + 2] = src[srcOffset + 2] * 255;\n    };\n    /**\n     * Copies many RGB samples, scaling by bits when needed.\n     *\n     * @private\n     * @param {any} src Source samples.\n     * @param {number} srcOffset Source offset.\n     * @param {number} count Number of RGB pixels.\n     * @param {Uint8ClampedArray} dest Destination RGB.\n     * @param {number} destOffset Destination offset.\n     * @param {number} bits Bits per component.\n     * @param {number} alpha01 Alpha slot flag (0 or 1).\n     * @returns {void} nothing.\n     */\n    _PdfDeviceRgbCS.prototype._getRgbBuffer = function (src, srcOffset, count, dest, destOffset, // eslint-disable-line\n    bits, alpha01) {\n        if (bits === 8 && alpha01 === 0) {\n            dest.set(src.subarray(srcOffset, srcOffset + count * 3), destOffset);\n            return;\n        }\n        var scale = 255 / ((1 << bits) - 1);\n        var j = srcOffset;\n        var q = destOffset;\n        for (var i = 0; i < count; ++i) {\n            dest[q++] = scale * src[j++];\n            dest[q++] = scale * src[j++];\n            dest[q++] = scale * src[j++];\n            q += alpha01;\n        }\n    };\n    /**\n     * Computes the output byte length for the given input RGB scalar count.\n     *\n     * @private\n     * @param {number} inputLength Number of input scalars.\n     * @param {number} alpha01 Alpha slot flag.\n     * @returns {number} Number of bytes required.\n     */\n    _PdfDeviceRgbCS.prototype._getOutputLength = function (inputLength, alpha01) {\n        return ((inputLength * (3 + alpha01)) / 3) | 0;\n    };\n    /**\n     * Indicates passthrough is allowed for 8-bit components.\n     *\n     * @private\n     * @param {number} bits Bits per component.\n     * @returns {boolean} `true` if 8 bits; otherwise, `false`.\n     */\n    _PdfDeviceRgbCS.prototype._isPass = function (bits) {\n        return bits === 8;\n    };\n    return _PdfDeviceRgbCS;\n}(_PdfColorPalette));\n\n/**\n * DeviceRGBA color space implementation with resize/copy helpers.\n *\n * @private\n */\nvar _PdfDeviceRgbaCS = /** @class */ (function (_super) {\n    __extends(_PdfDeviceRgbaCS, _super);\n    /**\n     * Initializes a new instance of the `_PdfDeviceRgbaCS` class.\n     *\n     * @private\n     * @returns {void} nothing.\n     */\n    function _PdfDeviceRgbaCS() {\n        return _super.call(this, 'DeviceRGBA', 4) || this;\n    }\n    /**\n     * Resizes an RGBA source image into a destination, optionally stripping alpha.\n     *\n     * @private\n     * @param {Uint8Array} src Source RGBA buffer.\n     * @param {Uint8Array} dest Destination buffer.\n     * @param {number} w1 Source width.\n     * @param {number} h1 Source height.\n     * @param {number} w2 Target width.\n     * @param {number} h2 Target height.\n     * @param {number} alpha01 1 to keep alpha in layout (mask write), 0 to strip.\n     * @returns {void} nothing.\n     */\n    _PdfDeviceRgbaCS.prototype._resizeRgbaImage = function (src, dest, w1, h1, w2, h2, alpha01) {\n        var xRatio = w1 / w2;\n        var yRatio = h1 / h2;\n        var newIndex = 0;\n        var xScaled = new Uint16Array(w2);\n        if (alpha01 === 1) {\n            for (var i = 0; i < w2; i++) {\n                xScaled[i] = Math.floor(i * xRatio);\n            }\n            var src32 = new Uint32Array(src.buffer);\n            var dest32 = new Uint32Array(dest.buffer);\n            var rgbMask = (0,_syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__._isLittleEndian)() ? 0x00ffffff : 0xffffff00; // eslint-disable-line\n            for (var i = 0; i < h2; i++) {\n                var buf = src32.subarray(Math.floor(i * yRatio) * w1); // eslint-disable-line\n                for (var j = 0; j < w2; j++) {\n                    dest32[newIndex++] |= buf[xScaled[j]] & rgbMask;\n                }\n            }\n        }\n        else {\n            var components = 4;\n            var w1Scanline = w1 * components;\n            for (var i = 0; i < w2; i++) {\n                xScaled[i] = Math.floor(i * xRatio) * components;\n            }\n            for (var i = 0; i < h2; i++) {\n                var buf = src.subarray(Math.floor(i * yRatio) * w1Scanline); // eslint-disable-line\n                for (var j = 0; j < w2; j++) {\n                    var oldIndex = xScaled[j];\n                    dest[newIndex++] = buf[oldIndex];\n                    dest[newIndex++] = buf[oldIndex + 1];\n                    dest[newIndex++] = buf[oldIndex + 2];\n                }\n            }\n        }\n    };\n    /**\n     * Copies an RGBA buffer into destination, optionally stripping alpha.\n     *\n     * @private\n     * @param {Uint8Array} src Source RGBA buffer.\n     * @param {Uint8Array} dest Destination buffer.\n     * @param {number} alpha01 1 to keep alpha in layout, 0 to strip.\n     * @returns {void} nothing.\n     */\n    _PdfDeviceRgbaCS.prototype._copyRgbaImage = function (src, dest, alpha01) {\n        if (alpha01 === 1) {\n            var src32 = new Uint32Array(src.buffer);\n            var dest32 = new Uint32Array(dest.buffer);\n            var rgbMask = (0,_syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__._isLittleEndian)() ? 0x00ffffff : 0xffffff00; // eslint-disable-line\n            for (var i = 0, ii = src32.length; i < ii; i++) {\n                dest32[i] |= src32[i] & rgbMask;\n            }\n        }\n        else {\n            var j = 0;\n            for (var i = 0, ii = src.length; i < ii; i += 4) {\n                dest[j++] = src[i];\n                dest[j++] = src[i + 1];\n                dest[j++] = src[i + 2];\n            }\n        }\n    };\n    /**\n     * Computes the output byte length for RGBA.\n     *\n     * @private\n     * @param {number} inputLength Number of input scalars.\n     * @param {number} _alpha01 Alpha slot flag.\n     * @returns {number} Number of output bytes.\n     */\n    _PdfDeviceRgbaCS.prototype._getOutputLength = function (inputLength, _alpha01) {\n        return inputLength * 4;\n    };\n    /**\n     * Indicates passthrough is allowed for 8-bit components.\n     *\n     * @private\n     * @param {number} bits Bits per component.\n     * @returns {boolean} `true` if 8 bits; otherwise, `false`.\n     */\n    _PdfDeviceRgbaCS.prototype._isPass = function (bits) {\n        return bits === 8;\n    };\n    /**\n     * Fills RGB(A) destination buffer, resizing or copying RGBA as needed.\n     *\n     * @private\n     * @param {any} dest Destination buffer.\n     * @param {number} originalWidth Source width.\n     * @param {number} originalHeight Source height.\n     * @param {number} width Target width.\n     * @param {number} height Target height.\n     * @param {number} actualHeight Actual drawn height.\n     * @param {number} bpc Bits per component.\n     * @param {any} comps Component data (RGBA).\n     * @param {number} alpha01 1 to keep alpha in layout, 0 to strip.\n     * @returns {Promise<any>} Resolves when fill is complete.\n     */\n    _PdfDeviceRgbaCS.prototype._fillRgb = function (dest, originalWidth, originalHeight, width, height, // eslint-disable-line\n    actualHeight, bpc, comps, alpha01) {\n        return __awaiter(this, void 0, void 0, function () {\n            return __generator(this, function (_a) {\n                if (originalHeight !== height || originalWidth !== width) {\n                    this._resizeRgbaImage(comps, dest, originalWidth, originalHeight, width, height, alpha01);\n                }\n                else {\n                    this._copyRgbaImage(comps, dest, alpha01);\n                }\n                return [2 /*return*/];\n            });\n        });\n    };\n    return _PdfDeviceRgbaCS;\n}(_PdfColorPalette));\n\n/**\n * DeviceCMYK color space implementation with polynomial conversion to sRGB.\n *\n * @private\n */\nvar _PdfDeviceCmykCS = /** @class */ (function (_super) {\n    __extends(_PdfDeviceCmykCS, _super);\n    function _PdfDeviceCmykCS() {\n        return _super.call(this, 'DeviceCMYK', 4) || this;\n    }\n    /**\n     * Converts one CMYK sample to RGB using a polynomial approximation.\n     *\n     * @private\n     * @param {any} src Source CMYK buffer.\n     * @param {number} srcOffset Source offset.\n     * @param {number} srcScale Scale factor for components.\n     * @param {any} dest Destination RGB buffer.\n     * @param {number} destOffset Destination offset.\n     * @returns {void} nothing.\n     */\n    _PdfDeviceCmykCS.prototype._toRgb = function (src, srcOffset, srcScale, dest, destOffset) {\n        var c = src[srcOffset] * srcScale;\n        var m = src[srcOffset + 1] * srcScale;\n        var y = src[srcOffset + 2] * srcScale;\n        var k = src[srcOffset + 3] * srcScale;\n        dest[destOffset] = 255 + c * (-4.387332384609988 * c + 54.48615194189176 * m + 18.82290502165302 *\n            y + 212.25662451639585 * k - 285.2331026137004) +\n            m * (1.7149763477362134 * m - 5.6096736904047315 * y - 17.873870861415444 * k - 5.497006427196366) +\n            y * (-2.5217340131683033 * y - 21.248923337353073 * k + 17.5119270841813) +\n            k * (-21.86122147463605 * k - 189.48180835922747);\n        dest[destOffset + 1] = 255 + c * (8.841041422036149 * c + 60.118027045597366 * m + 6.871425592049007 * y + 31.159100130055922 *\n            k - 79.2970844816548) +\n            m * (-15.310361306967817 * m + 17.575251261109482 * y + 131.35250912493976 * k - 190.9453302588951) +\n            y * (4.444339102852739 * y + 9.8632861493405 * k - 24.86741582555878) +\n            k * (-20.737325471181034 * k - 187.80453709719578);\n        dest[destOffset + 2] = 255 + c * (0.8842522430003296 * c + 8.078677503112928 * m + 30.89978309703729 * y - 0.23883238689178934 *\n            k - 14.183576799673286) +\n            m * (10.49593273432072 * m + 63.02378494754052 * y + 50.606957656360734 * k - 112.23884253719248) +\n            y * (0.03296041114873217 * y + 115.60384449646641 * k - 193.58209356861505) +\n            k * (-22.33816807309886 * k - 180.12613974708367);\n    };\n    /**\n     * Converts one CMYK sample to RGB.\n     *\n     * @private\n     * @param {any} src Source buffer.\n     * @param {number} srcOffset Source offset.\n     * @param {Uint8ClampedArray} dest Destination RGB buffer.\n     * @param {number} destOffset Destination offset.\n     * @returns {void} nothing.\n     */\n    _PdfDeviceCmykCS.prototype._getRgbItem = function (src, srcOffset, dest, destOffset) {\n        this._toRgb(src, srcOffset, 1, dest, destOffset);\n    };\n    /**\n     * Converts multiple CMYK samples to RGB.\n     *\n     * @private\n     * @param {any} src Source buffer.\n     * @param {number} srcOffset Source offset.\n     * @param {number} count Number of pixels.\n     * @param {Uint8ClampedArray} dest Destination RGB buffer.\n     * @param {number} destOffset Destination offset.\n     * @param {number} bits Bits per component.\n     * @param {number} alpha01 Alpha slot flag.\n     * @returns {void} nothing.\n     */\n    _PdfDeviceCmykCS.prototype._getRgbBuffer = function (src, srcOffset, count, dest, destOffset, // eslint-disable-line\n    bits, alpha01) {\n        var scale = 1 / ((1 << bits) - 1);\n        for (var i = 0; i < count; i++) {\n            this._toRgb(src, srcOffset, scale, dest, destOffset);\n            srcOffset += 4;\n            destOffset += 3 + alpha01;\n        }\n    };\n    /**\n     * Computes output length for CMYK source scalars.\n     *\n     * @private\n     * @param {number} inputLength Number of input scalars.\n     * @param {number} alpha01 Alpha slot flag.\n     * @returns {number} Number of bytes required.\n     */\n    _PdfDeviceCmykCS.prototype._getOutputLength = function (inputLength, alpha01) {\n        return ((inputLength / 4) * (3 + alpha01)) | 0;\n    };\n    return _PdfDeviceCmykCS;\n}(_PdfColorPalette));\n\n/**\n * CIE Lab color space implementation.\n *\n * @private\n */\nvar _PdfLabCS = /** @class */ (function (_super) {\n    __extends(_PdfLabCS, _super);\n    /**\n     * Initializes a new instance of the `_PdfLabCS` class.\n     *\n     * @private\n     * @param {number[]} [whitePoint] Required white point [X, Y, Z].\n     * @param {number[]} [blackPoint] Optional black point [X, Y, Z].\n     * @param {number[]} [range] Optional range [aMin, aMax, bMin, bMax].\n     * @returns {void} nothing.\n     * @throws {FormatError} When whitePoint is missing or invalid.\n     */\n    function _PdfLabCS(whitePoint, blackPoint, range) {\n        var _a, _b;\n        var _this = _super.call(this, 'Lab', 3) || this;\n        if (!whitePoint) {\n            throw new _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__.FormatError('WhitePoint missing - required for color space Lab');\n        }\n        _this._xw = whitePoint[0], _this._yw = whitePoint[1], _this._zw = whitePoint[2];\n        _a = range || [-100, 100, -100, 100], _this._amin = _a[0], _this._amax = _a[1], _this._bmin = _a[2], _this._bmax = _a[3];\n        _b = blackPoint || [0, 0, 0], _this._xb = _b[0], _this._yb = _b[1], _this._zb = _b[2];\n        if (_this._xw < 0 || _this._zw < 0 || _this._yw !== 1) {\n            throw new _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__.FormatError('Invalid WhitePoint components, no fallback available');\n        }\n        if (_this._xb < 0 || _this._yb < 0 || _this._zb < 0) {\n            _this._xb = _this._yb = _this._zb = 0;\n        }\n        if (_this._amin > _this._amax || _this._bmin > _this._bmax) {\n            _this._amin = -100;\n            _this._amax = 100;\n            _this._bmin = -100;\n            _this._bmax = 100;\n        }\n        return _this;\n    }\n    _PdfLabCS.prototype._fng = function (x) {\n        return x >= 6 / 29 ? Math.pow(x, 3) : (108 / 841) * (x - 4 / 29);\n    };\n    _PdfLabCS.prototype._decode = function (value, high1, low2, high2) {\n        return low2 + (value * (high2 - low2)) / high1;\n    };\n    _PdfLabCS.prototype._toRgb = function (src, srcOffset, maxVal, dest, destOffset) {\n        var ls = src[srcOffset];\n        var as = src[srcOffset + 1];\n        var bs = src[srcOffset + 2];\n        if (maxVal !== false) {\n            ls = this._decode(ls, maxVal, 0, 100);\n            as = this._decode(as, maxVal, this._amin, this._amax);\n            bs = this._decode(bs, maxVal, this._bmin, this._bmax);\n        }\n        if (as > this._amax) {\n            as = this._amax;\n        }\n        else if (as < this._amin) {\n            as = this._amin;\n        }\n        if (bs > this._bmax) {\n            bs = this._bmax;\n        }\n        else if (bs < this._bmin) {\n            bs = this._bmin;\n        }\n        var m = (ls + 16) / 116;\n        var l = m + as / 500;\n        var n = m - bs / 200;\n        var x = this._xw * this._fng(l);\n        var y = this._yw * this._fng(m);\n        var z = this._zw * this._fng(n);\n        var r;\n        var g;\n        var b;\n        if (this._zw < 1) {\n            r = x * 3.1339 + y * -1.617 + z * -0.4906;\n            g = x * -0.9785 + y * 1.916 + z * 0.0333;\n            b = x * 0.072 + y * -0.229 + z * 1.4057;\n        }\n        else {\n            r = x * 3.2406 + y * -1.5372 + z * -0.4986;\n            g = x * -0.9689 + y * 1.8758 + z * 0.0415;\n            b = x * 0.0557 + y * -0.204 + z * 1.057;\n        }\n        dest[destOffset] = Math.sqrt(r) * 255;\n        dest[destOffset + 1] = Math.sqrt(g) * 255;\n        dest[destOffset + 2] = Math.sqrt(b) * 255;\n    };\n    /**\n     * Converts a single Lab sample to RGB.\n     *\n     * @private\n     * @param {Uint8Array} src Source buffer.\n     * @param {number} srcOffset Source offset.\n     * @param {any} dest Destination buffer for RGB output.\n     * @param {number} destOffset Destination offset.\n     * @returns {void} nothing.\n     */\n    _PdfLabCS.prototype._getRgbItem = function (src, srcOffset, dest, destOffset) {\n        this._toRgb(src, srcOffset, false, dest, destOffset);\n    };\n    /**\n     * Converts multiple Lab samples to RGB.\n     *\n     * @private\n     * @param {Uint8Array} src Source buffer.\n     * @param {number} srcOffset Source offset.\n     * @param {number} count Number of samples.\n     * @param {any} dest Destination buffer for RGB output.\n     * @param {number} destOffset Destination offset.\n     * @param {number} bits Bits per component.\n     * @param {number} alpha01 Alpha slot flag (0 or 1).\n     * @returns {void} nothing.\n     */\n    _PdfLabCS.prototype._getRgbBuffer = function (src, srcOffset, count, dest, destOffset, // eslint-disable-line\n    bits, alpha01) {\n        var maxVal = (1 << bits) - 1;\n        for (var i = 0; i < count; i++) {\n            this._toRgb(src, srcOffset, maxVal, dest, destOffset);\n            srcOffset += 3;\n            destOffset += 3 + alpha01;\n        }\n    };\n    /**\n     * Computes the output byte length for a set of Lab samples.\n     *\n     * @private\n     * @param {number} inputLength Number of input scalars.\n     * @param {number} alpha01 Alpha slot flag (0 or 1).\n     * @returns {number} Number of bytes required.\n     */\n    _PdfLabCS.prototype._getOutputLength = function (inputLength, alpha01) {\n        return ((inputLength * (3 + alpha01)) / 3) | 0;\n    };\n    /**\n     * Indicates whether the provided decode map equals the default mapping (always `true` for Lab).\n     *\n     * @private\n     * @param {any} decodeMap Decode map.\n     * @param {number} bpc Bits per component.\n     * @returns {boolean} Always `true` for Lab.\n     */\n    _PdfLabCS.prototype._isDefaultDecode = function (decodeMap, bpc) {\n        return true;\n    };\n    Object.defineProperty(_PdfLabCS.prototype, \"usesZeroToOneRange\", {\n        get: function () {\n            return false;\n        },\n        enumerable: true,\n        configurable: true\n    });\n    return _PdfLabCS;\n}(_PdfColorPalette));\n\n/**\n * CalGray color space implementation.\n *\n * @private\n */\nvar _PdfCalGrayCS = /** @class */ (function (_super) {\n    __extends(_PdfCalGrayCS, _super);\n    function _PdfCalGrayCS(whitePoint, blackPoint, gamma) {\n        var _a;\n        var _this = _super.call(this, 'CalGray', 1) || this;\n        if (!whitePoint) {\n            throw new _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__.FormatError('WhitePoint missing - required for color space CalGray');\n        }\n        _this._xw = whitePoint[0], _this._yw = whitePoint[1], _this._zw = whitePoint[2];\n        _a = blackPoint || [0, 0, 0], _this._xb = _a[0], _this._yb = _a[1], _this._zb = _a[2];\n        _this._g = gamma || 1;\n        if (_this._xw < 0 || _this._zw < 0 || _this._yw !== 1) {\n            throw new _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__.FormatError(\"Invalid WhitePoint components for \" + _this.name + \", no fallback available\");\n        }\n        if (_this._xb < 0 || _this._yb < 0 || _this._zb < 0) {\n            _this._xb = _this._yb = _this._zb = 0;\n        }\n        if (_this._g < 1) {\n            _this._g = 1;\n        }\n        return _this;\n    }\n    /**\n     * Converts one CalGray sample to RGB using gamma and white point.\n     *\n     * @private\n     * @param {number[]} src Source buffer.\n     * @param {number} srcOffset Source offset.\n     * @param {number[]} dest Destination buffer.\n     * @param {number} destOffset Destination offset.\n     * @param {number} scale Scale factor for input sample.\n     * @returns {void} nothing.\n     */\n    _PdfCalGrayCS.prototype._toRgb = function (src, srcOffset, dest, destOffset, scale) {\n        var a = src[srcOffset] * scale;\n        var ag = Math.pow(a, this._g);\n        var l = this._yw * ag;\n        var val = Math.max(295.8 * Math.pow(l, 0.3333333333333333) - 40.8, 0);\n        dest[destOffset] = val;\n        dest[destOffset + 1] = val;\n        dest[destOffset + 2] = val;\n    };\n    /**\n     * Converts a single CalGray sample to RGB.\n     *\n     * @private\n     * @param {number[]} src Source buffer.\n     * @param {number} srcOffset Source offset.\n     * @param {any} dest Destination RGB buffer.\n     * @param {number} destOffset Destination offset.\n     * @returns {void} nothing.\n     */\n    _PdfCalGrayCS.prototype._getRgbItem = function (src, srcOffset, dest, destOffset) {\n        this._toRgb(src, srcOffset, dest, destOffset, 1);\n    };\n    /**\n     * Converts multiple CalGray samples to RGB.\n     *\n     * @private\n     * @param {number[]} src Source buffer.\n     * @param {number} srcOffset Source offset.\n     * @param {number} count Number of samples.\n     * @param {any} dest Destination RGB buffer.\n     * @param {number} destOffset Destination offset.\n     * @param {number} bits Bits per component.\n     * @param {number} alpha01 Alpha slot flag .\n     * @returns {void} nothing.\n     */\n    _PdfCalGrayCS.prototype._getRgbBuffer = function (src, srcOffset, count, dest, destOffset, bits, alpha01) {\n        var scale = 1 / ((1 << bits) - 1);\n        for (var i = 0; i < count; ++i) {\n            this._toRgb(src, srcOffset, dest, destOffset, scale);\n            srcOffset += 1;\n            destOffset += 3 + alpha01;\n        }\n    };\n    /**\n     * Computes output byte length for CalGray input.\n     *\n     * @private\n     * @param {number} inputLength Number of input samples.\n     * @param {number} alpha01 Alpha slot flag (0 or 1).\n     * @returns {number} Output byte count.\n     */\n    _PdfCalGrayCS.prototype._getOutputLength = function (inputLength, alpha01) {\n        return inputLength * (3 + alpha01);\n    };\n    return _PdfCalGrayCS;\n}(_PdfColorPalette));\n\n/**\n * CalRGB color space implementation with white/black point and gamma.\n *\n * @private\n */\nvar _PdfColorRgbConverter = /** @class */ (function (_super) {\n    __extends(_PdfColorRgbConverter, _super);\n    function _PdfColorRgbConverter(whitePoint, blackPoint, gamma, matrix) {\n        var _a, _b;\n        var _this = _super.call(this, 'CalRGB', 3) || this;\n        _this.scaleMatrix = new Float32Array([\n            0.8951, 0.2664, -0.1614,\n            -0.7502, 1.7135, 0.0367,\n            0.0389, -0.0685, 1.0296\n        ]);\n        _this.inverseMatrix = new Float32Array([\n            0.9869929, -0.1470543, 0.1599627,\n            0.4323053, 0.5183603, 0.0492912,\n            -0.0085287, 0.0400428, 0.9684867\n        ]);\n        _this.rgbMatrix = new Float32Array([\n            3.2404542, -1.5371385, -0.4985314,\n            -0.9692660, 1.8760108, 0.0415560,\n            0.0556434, -0.2040259, 1.0572252\n        ]);\n        _this.whitePointMatrix = new Float32Array([1, 1, 1]);\n        _this.tempNormalizeMatrix = new Float32Array(3);\n        _this.tempConvertMatrix1 = new Float32Array(3);\n        _this.tempConvertMatrix2 = new Float32Array(3);\n        _this.decodeConstant = Math.pow(((8 + 16) / 116), 3) / 8.0;\n        if (!whitePoint) {\n            throw new _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__.FormatError('WhitePoint missing - required for color space CalRGB');\n        }\n        var _c = (_this._whitePoint = whitePoint), xw = _c[0], yw = _c[1], zw = _c[2];\n        var _d = (_this._blackPoint = blackPoint || new Float32Array(3)), xb = _d[0], yb = _d[1], zb = _d[2];\n        _a = gamma || new Float32Array([1, 1, 1]), _this.gr = _a[0], _this.gg = _a[1], _this.gb = _a[2];\n        _b = matrix || new Float32Array([1, 0, 0, 0, 1, 0, 0, 0, 1]), _this.mxa = _b[0], _this.mya = _b[1], _this.mza = _b[2], _this.mxb = _b[3], _this.myb = _b[4], _this.mzb = _b[5], _this.mxc = _b[6], _this.myc = _b[7], _this.mzc = _b[8];\n        if (xw < 0 || zw < 0 || yw !== 1) {\n            throw new _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__.FormatError(\"Invalid WhitePoint components for \" + _this.name + \", no fallback available\");\n        }\n        if (xb < 0 || yb < 0 || zb < 0) {\n            _this._blackPoint = new Float32Array(3);\n        }\n        if (_this.gr < 0 || _this.gg < 0 || _this.gb < 0) {\n            _this.gr = _this.gg = _this.gb = 1;\n        }\n        return _this;\n    }\n    /**\n     * Multiplies a 3x3 matrix by a 3x1 vector.\n     *\n     * @private\n     * @param {Float32Array} a 3x3 matrix.\n     * @param {Float32Array} b 3x1 vector.\n     * @param {Float32Array} result 3x1 output vector.\n     * @returns {void} nothing.\n     */\n    _PdfColorRgbConverter.prototype._matrixProduct = function (a, b, result) {\n        result[0] = a[0] * b[0] + a[1] * b[1] + a[2] * b[2];\n        result[1] = a[3] * b[0] + a[4] * b[1] + a[5] * b[2];\n        result[2] = a[6] * b[0] + a[7] * b[1] + a[8] * b[2];\n    };\n    /**\n     * Normalizes LMS values to a flat white point.\n     *\n     * @private\n     * @param {Float32Array} sourceWhitePoint Source white point [L,M,S].\n     * @param {Float32Array} lms Input LMS vector.\n     * @param {Float32Array} result Output normalized LMS.\n     * @returns {void} nothing.\n     */\n    _PdfColorRgbConverter.prototype._toFlat = function (sourceWhitePoint, lms, result) {\n        result[0] = (lms[0] * 1) / sourceWhitePoint[0];\n        result[1] = (lms[1] * 1) / sourceWhitePoint[1];\n        result[2] = (lms[2] * 1) / sourceWhitePoint[2];\n    };\n    /**\n     * Normalizes LMS values to D65 white point.\n     *\n     * @private\n     * @param {Float32Array} sourceWhitePoint Source white point [L,M,S].\n     * @param {Float32Array} lms Input LMS vector.\n     * @param {Float32Array} result Output normalized LMS for D65.\n     * @returns {void} nothing.\n     */\n    _PdfColorRgbConverter.prototype._toD65 = function (sourceWhitePoint, lms, result) {\n        var d65X = 0.95047;\n        var d65Y = 1;\n        var d65Z = 1.08883;\n        result[0] = (lms[0] * d65X) / sourceWhitePoint[0];\n        result[1] = (lms[1] * d65Y) / sourceWhitePoint[1];\n        result[2] = (lms[2] * d65Z) / sourceWhitePoint[2];\n    };\n    /**\n     * sRGB transfer function.\n     *\n     * @private\n     * @param {number} color Linear color channel value.\n     * @returns {number} Gamma-corrected channel .\n     */\n    _PdfColorRgbConverter.prototype._srgbTransferFunction = function (color) {\n        if (color <= 0.0031308) {\n            return (0,_syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__._mathClamp)(12.92 * color, 0, 1);\n        }\n        if (color >= 0.99554525) {\n            return 1;\n        }\n        return (0,_syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__._mathClamp)((1 + 0.055) * Math.pow(color, (1 / 2.4)) - 0.055, 0, 1);\n    };\n    /**\n     * Decodes L* value to linear lightness.\n     *\n     * @private\n     * @param {number} L L* value.\n     * @returns {number} Linear value.\n     */\n    _PdfColorRgbConverter.prototype._decodeL = function (L) {\n        if (L < 0) {\n            return -this._decodeL(-L);\n        }\n        if (L > 8.0) {\n            return Math.pow(((L + 16) / 116), 3);\n        }\n        return L * this.decodeConstant;\n    };\n    /**\n     * Applies black point compensation.\n     *\n     * @private\n     * @param {Float32Array} sourceBlackPoint Source black point XYZ.\n     * @param {Float32Array} xyzFlat Flat-normalized XYZ.\n     * @param {Float32Array} result Output compensated XYZ.\n     * @returns {void} nothing.\n     */\n    _PdfColorRgbConverter.prototype._compensateBlackPoint = function (sourceBlackPoint, xyzFlat, result) {\n        if (sourceBlackPoint[0] === 0 && sourceBlackPoint[1] === 0 && sourceBlackPoint[2] === 0) {\n            result[0] = xyzFlat[0];\n            result[1] = xyzFlat[1];\n            result[2] = xyzFlat[2];\n            return;\n        }\n        var zeroDecodeL = this._decodeL(0);\n        var xdST = zeroDecodeL;\n        var xsRC = this._decodeL(sourceBlackPoint[0]);\n        var ydST = zeroDecodeL;\n        var ysRC = this._decodeL(sourceBlackPoint[1]);\n        var zdST = zeroDecodeL;\n        var zsRC = this._decodeL(sourceBlackPoint[2]);\n        var xScale = (1 - xdST) / (1 - xsRC);\n        var xOffset = 1 - xScale;\n        var yScale = (1 - ydST) / (1 - ysRC);\n        var yOffset = 1 - yScale;\n        var zScale = (1 - zdST) / (1 - zsRC);\n        var zOffset = 1 - zScale;\n        result[0] = xyzFlat[0] * xScale + xOffset;\n        result[1] = xyzFlat[1] * yScale + yOffset;\n        result[2] = xyzFlat[2] * zScale + zOffset;\n    };\n    /**\n     * Normalizes XYZ to a flat white point from source white point.\n     *\n     * @private\n     * @param {Float32Array} sourceWhitePoint Source white point (XYZ).\n     * @param {Float32Array} xyzIn Input XYZ.\n     * @param {Float32Array} result Output XYZ (flat).\n     * @returns {void} nothing.\n     */\n    _PdfColorRgbConverter.prototype._normalizeWhitePointToFlat = function (sourceWhitePoint, xyzIn, result) {\n        if (sourceWhitePoint[0] === 1 && sourceWhitePoint[2] === 1) {\n            result[0] = xyzIn[0];\n            result[1] = xyzIn[1];\n            result[2] = xyzIn[2];\n            return;\n        }\n        var lms = result; // eslint-disable-line\n        this._matrixProduct(this.scaleMatrix, xyzIn, lms);\n        var lmsFlat = this.tempNormalizeMatrix; // eslint-disable-line\n        this._toFlat(sourceWhitePoint, lms, lmsFlat);\n        this._matrixProduct(this.inverseMatrix, lmsFlat, result);\n    };\n    /**\n     * Normalizes XYZ to D65 from source white point.\n     *\n     * @private\n     * @param {Float32Array} sourceWhitePoint Source white point.\n     * @param {Float32Array} xyzIn Input XYZ.\n     * @param {Float32Array} result Output XYZ.\n     * @returns {void} nothing.\n     */\n    _PdfColorRgbConverter.prototype._normalizeWhitePointToD65 = function (sourceWhitePoint, xyzIn, result) {\n        var lms = result; // eslint-disable-line\n        this._matrixProduct(this.scaleMatrix, xyzIn, lms);\n        var lmsD65 = this.tempNormalizeMatrix; // eslint-disable-line\n        this._toD65(sourceWhitePoint, lms, lmsD65);\n        this._matrixProduct(this.inverseMatrix, lmsD65, result);\n    };\n    /**\n     * Converts one CalRGB sample to sRGB.\n     *\n     * @private\n     * @param {number[]} src Source components.\n     * @param {number} srcOffset Source offset.\n     * @param {Uint8ClampedArray} dest Destination buffer.\n     * @param {number} destOffset Destination offset.\n     * @param {number} scale Scale factor for input sample.\n     * @returns {void} nothing.\n     */\n    _PdfColorRgbConverter.prototype._toRgb = function (src, srcOffset, dest, destOffset, scale) {\n        var a = (0,_syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__._mathClamp)(src[srcOffset] * scale, 0, 1);\n        var b = (0,_syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__._mathClamp)(src[srcOffset + 1] * scale, 0, 1);\n        var c = (0,_syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__._mathClamp)(src[srcOffset + 2] * scale, 0, 1);\n        var agr = a === 1 ? 1 : Math.pow(a, this.gr);\n        var bgg = b === 1 ? 1 : Math.pow(b, this.gg);\n        var cgb = c === 1 ? 1 : Math.pow(c, this.gb);\n        var x = this.mxa * agr + this.mxb * bgg + this.mxc * cgb;\n        var y = this.mya * agr + this.myb * bgg + this.myc * cgb;\n        var z = this.mza * agr + this.mzb * bgg + this.mzc * cgb;\n        var xyz = this.tempConvertMatrix1;\n        xyz[0] = x;\n        xyz[1] = y;\n        xyz[2] = z;\n        var xyzFlat = this.tempConvertMatrix2;\n        this._normalizeWhitePointToFlat(this._whitePoint, xyz, xyzFlat);\n        var xyzBlack = this.tempConvertMatrix1;\n        this._compensateBlackPoint(this._blackPoint, xyzFlat, xyzFlat);\n        var xyzD65 = this.tempConvertMatrix2;\n        this._normalizeWhitePointToD65(this.whitePointMatrix, xyzBlack, xyzD65);\n        var srgb = this.tempConvertMatrix1; // eslint-disable-line\n        this._matrixProduct(this.rgbMatrix, xyzD65, srgb);\n        dest[destOffset] = this._srgbTransferFunction(srgb[0]) * 255;\n        dest[destOffset + 1] = this._srgbTransferFunction(srgb[1]) * 255;\n        dest[destOffset + 2] = this._srgbTransferFunction(srgb[2]) * 255;\n    };\n    /**\n     * Converts one CalRGB sample to sRGB.\n     *\n     * @private\n     * @param {number[]} src Source buffer.\n     * @param {number} srcOffset Source offset.\n     * @param {Uint8ClampedArray} dest Destination buffer.\n     * @param {number} destOffset Destination offset.\n     * @returns {void} nothing.\n     */\n    _PdfColorRgbConverter.prototype._getRgbItem = function (src, srcOffset, dest, destOffset) {\n        this._toRgb(src, srcOffset, dest, destOffset, 1);\n    };\n    /**\n     * Converts multiple CalRGB samples to sRGB.\n     *\n     * @private\n     * @param {number[]} src Source buffer.\n     * @param {number} srcOffset Source offset.\n     * @param {number} count Number of samples.\n     * @param {Uint8ClampedArray} dest Destination buffer.\n     * @param {number} destOffset Destination offset.\n     * @param {number} bits Bits per component.\n     * @param {number} alpha01 Alpha slot flag .\n     * @returns {void} nothing.\n     */\n    _PdfColorRgbConverter.prototype._getRgbBuffer = function (src, srcOffset, count, dest, destOffset, bits, alpha01) {\n        var scale = 1 / ((1 << bits) - 1);\n        for (var i = 0; i < count; ++i) {\n            this._toRgb(src, srcOffset, dest, destOffset, scale);\n            srcOffset += 3;\n            destOffset += 3 + alpha01;\n        }\n    };\n    /**\n     * Computes output byte length for CalRGB source samples.\n     *\n     * @private\n     * @param {number} inputLength Number of input scalars.\n     * @param {number} alpha01 Alpha slot flag.\n     * @returns {number} Output byte count.\n     */\n    _PdfColorRgbConverter.prototype._getOutputLength = function (inputLength, alpha01) {\n        return ((inputLength * (3 + alpha01)) / 3) | 0;\n    };\n    return _PdfColorRgbConverter;\n}(_PdfColorPalette));\n\n\n\n//# sourceURL=webpack://@syncfusion/ej2-documents-vue-samples/./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/image-extraction/colorspace.js?");

/***/ })

}]);