"use strict";
/*
 * ATTENTION: The "eval" devtool has been used (maybe by default in mode: "development").
 * This devtool is neither made for production nor for readable output files.
 * It uses "eval()" calls to create a separate source file in the browser devtools.
 * If you are trying to read the output file, select a different devtool (https://webpack.js.org/configuration/devtool/)
 * or disable the default devtool with "devtool: false".
 * If you are looking for production-ready output files, see mode: "production" (https://webpack.js.org/configuration/mode/).
 */
(self["webpackChunk_syncfusion_ej2_documents_vue_samples"] = self["webpackChunk_syncfusion_ej2_documents_vue_samples"] || []).push([["chunk-vendors-node_modules_syncfusion_ej2-pdf-data-extract_src_pdf-data-extract_core_image-extractio-43f5c70d"],{

/***/ "./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/image-extraction/image-resizer.js":
/*!*******************************************************************************************************************!*\
  !*** ./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/image-extraction/image-resizer.js ***!
  \*******************************************************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   _PdfImageResizer: () => (/* binding */ _PdfImageResizer)\n/* harmony export */ });\n/* harmony import */ var _image_utils__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./image-utils */ \"./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/image-extraction/image-utils.js\");\n/* harmony import */ var _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! @syncfusion/ej2-pdf */ \"./node_modules/@syncfusion/ej2-pdf/index.js\");\n\n\n/**\n * Utility to downscale large images safely based on browser/canvas capabilities.\n *\n * @private\n */\nvar _PdfImageResizer = /** @class */ (function () {\n    function _PdfImageResizer(imgData, isMask) {\n        this.minimumImageDimension = 2048;\n        this.maximumImageDimension = 65537;\n        this.error = 128;\n        this.goodSquareLength = 2048;\n        this.isImageDecoderSupported = false; //FeatureTest.isImageDecoderSupported;\n        /**\n         * Indicates whether a maximum canvas area was already set/probed.\n         *\n         * @private\n         */\n        this._hasMaxArea = false;\n        if (typeof (imgData) !== 'undefined' && typeof (isMask) !== 'undefined') {\n            this._imgData = imgData;\n            this._isMask = isMask;\n        }\n    }\n    Object.defineProperty(_PdfImageResizer.prototype, \"canUseImageDecoder\", {\n        get: function () {\n            return this.isImageDecoderSupported ? Promise.resolve(false) : Promise.resolve(false);\n        },\n        enumerable: true,\n        configurable: true\n    });\n    /**\n     * Returns whether the image should be resized based on current constraints.\n     *\n     * @private\n     * @param {number} width Image width.\n     * @param {number} height Image height.\n     * @returns {boolean} `true` if a resize is required otherwise, `false`.\n     */\n    _PdfImageResizer.prototype._needsToBeResized = function (width, height) {\n        if (width <= this.goodSquareLength && height <= this.goodSquareLength) {\n            return false;\n        }\n        var _maximumDim = this._maximumDim;\n        if (width > _maximumDim || height > _maximumDim) {\n            return true;\n        }\n        var area = width * height;\n        if (this._hasMaxArea) {\n            return area > this._maximumArea;\n        }\n        if (area < Math.pow(this.goodSquareLength, 2)) {\n            return false;\n        }\n        if (this._areGoodDims(width, height)) {\n            this.goodSquareLength = Math.max(this.goodSquareLength, Math.floor(Math.sqrt(width * height)));\n            return false;\n        }\n        this.goodSquareLength = this._guessMax(this.goodSquareLength, _maximumDim, this.error, 0);\n        var maxArea = (this._maximumArea = Math.pow(this.goodSquareLength, 2));\n        return area > maxArea;\n    };\n    /**\n     * Computes the JPX reduce power based on constraints and JPX decoder limits.\n     *\n     * @private\n     * @param {number} width Image width.\n     * @param {number} height Image height.\n     * @param {number} componentsCount Number of components.\n     * @returns {number} Reduce power `n` such that decoder can use 2^n reduction.\n     */\n    _PdfImageResizer.prototype._getReducePowerForJPX = function (width, height, componentsCount) {\n        var area = width * height;\n        var maxJPXArea = Math.pow(2, 30) / (componentsCount * 4);\n        if (!this._needsToBeResized(width, height)) {\n            if (area > maxJPXArea) {\n                return Math.ceil(Math.log2(area / maxJPXArea));\n            }\n            return 0;\n        }\n        var _a = this, _maximumDim = _a._maximumDim, _maximumArea = _a._maximumArea;\n        var minFactor = Math.max(width / _maximumDim, height / _maximumDim, Math.sqrt(area / Math.min(maxJPXArea, _maximumArea)));\n        return Math.ceil(Math.log2(minFactor));\n    };\n    Object.defineProperty(_PdfImageResizer.prototype, \"_maximumDim\", {\n        /**\n         * Probed maximum acceptable dimension for either width or height.\n         *\n         * @private\n         * @returns {number} Maximum acceptable single dimension.\n         */\n        get: function () {\n            return this._guessMax(this.maximumImageDimension, this.maximumImageDimension, 0, 1);\n        },\n        enumerable: true,\n        configurable: true\n    });\n    Object.defineProperty(_PdfImageResizer.prototype, \"_maximumArea\", {\n        /**\n         * Probed maximum acceptable area derived from current constraints.\n         *\n         * @private\n         * @returns {number} Maximum acceptable area in pixels.\n         */\n        get: function () {\n            this._hasMaxArea = true;\n            return Math.pow(this._guessMax(this.goodSquareLength, this._maximumDim, this.error, 0), 2);\n        },\n        /**\n         * Marks that a maximum area constraint has been established.\n         *\n         * @private\n         * @param {number} area Area value used to mark presence of max area.\n         * @returns {void} nothing.\n         */\n        set: function (area) {\n            if (area >= 0) {\n                this._hasMaxArea = true;\n            }\n        },\n        enumerable: true,\n        configurable: true\n    });\n    /* eslint-disable */\n    /**\n     * Applies configuration options, like canvas max area and ImageDecoder support flag.\n     *\n     * @private\n     * @param {object} param0 Options object.\n     * @param {number} [param0.canvasMaxAreaInBytes=-1] Max canvas area in bytes.\n     * @param {boolean} [param0.isImageDecoderSupported=false] Feature flag for ImageDecoder.\n     * @returns {any} returns image flag.\n     */\n    _PdfImageResizer.prototype._setOptions = function (_a) {\n        var _b = _a.canvasMaxAreaInBytes, canvasMaxAreaInBytes = _b === void 0 ? -1 : _b, _c = _a.isImageDecoderSupported, isImageDecoderSupported = _c === void 0 ? false : _c;\n        if (!this._hasMaxArea) {\n            this._maximumArea = canvasMaxAreaInBytes >> 2;\n        }\n        this.isImageDecoderSupported = isImageDecoderSupported;\n    };\n    /* eslint-enable */\n    /**\n     * Feature test to check if a canvas of is usable for drawing.\n     *\n     * @private\n     * @param {number} width Target width.\n     * @param {number} height Target height.\n     * @returns {boolean} `true` if drawing succeeds; otherwise, `false`.\n     */\n    _PdfImageResizer.prototype._areGoodDims = function (width, height) {\n        try {\n            var canvas = document.createElement('canvas');\n            canvas.width = width;\n            canvas.height = height;\n            var ctx = canvas.getContext('2d');\n            ctx.fillRect(0, 0, 1, 1);\n            var opacity = ctx.getImageData(0, 0, 1, 1).data[3]; // eslint-disable-line\n            canvas.width = canvas.height = 1;\n            return opacity !== 0;\n        }\n        catch (_a) {\n            return false;\n        }\n    };\n    /**\n     * Binary-search-like probe to find maximum acceptable dimension between start and end.\n     *\n     * @private\n     * @param {number} start Lower bound dimension to test from.\n     * @param {number} end Upper bound dimension to test up to.\n     * @param {number} tolerance Allowed gap to stop probing.\n     * @param {number} defaultHeight If non-zero, height to test with.\n     * @returns {number} Maximum acceptable dimension found.\n     */\n    _PdfImageResizer.prototype._guessMax = function (start, end, tolerance, defaultHeight) {\n        while (start + tolerance + 1 < end) {\n            var middle = Math.floor((start + end) / 2);\n            var height = defaultHeight || middle;\n            if (this._areGoodDims(middle, height)) {\n                start = middle;\n            }\n            else {\n                end = middle;\n            }\n        }\n        return start;\n    };\n    /**\n     * Helper to build a resized image from external imgData.\n     *\n     * @private\n     * @param {any} imgData Image-like object with width/height/data|bitmap/kind.\n     * @param {boolean} [isMask=false] Whether the image is a mask.\n     * @returns {any} Resulting image data .\n     */\n    _PdfImageResizer.prototype._createImageData = function (imgData, isMask) {\n        if (isMask === void 0) { isMask = false; }\n        return new _PdfImageResizer(imgData, isMask)._createImage();\n    };\n    /**\n     * Resizes the current instance `_imgData` if needed; returns the updated image data.\n     *\n     * @private\n     * @returns {any} Updated image data possibly with `bitmap` and new dimensions.\n     */\n    _PdfImageResizer.prototype._createImage = function () {\n        var imgData = this._imgData;\n        var width = imgData.width, height = imgData.height;\n        if (width * height * 4 > _image_utils__WEBPACK_IMPORTED_MODULE_0__.maximumCount) {\n            var result_1 = this._rescaleImageData(); // eslint-disable-line\n            if (result_1) {\n                return result_1;\n            }\n        }\n        var data = this._encodeBMP(); // eslint-disable-line\n        var imagePromise; // eslint-disable-line\n        var _a = this, _maximumArea = _a._maximumArea, _maximumDim = _a._maximumDim;\n        var minFactor = Math.max(width / _maximumDim, height / _maximumDim, Math.sqrt((width * height) / _maximumArea));\n        var firstFactor = Math.max(minFactor, 2);\n        var factor = Math.round(10 * (minFactor + 1.25)) / 10 / firstFactor;\n        var n = Math.floor(Math.log2(factor));\n        var steps = new Array(n + 2).fill(2); // eslint-disable-line\n        steps[0] = firstFactor;\n        steps.splice(-1, 1, factor / (1 << n));\n        var newWidth = width;\n        var newHeight = height;\n        var result = imagePromise; // eslint-disable-line\n        var bitmap = result.image || result; // eslint-disable-line\n        for (var _i = 0, steps_1 = steps; _i < steps_1.length; _i++) {\n            var step = steps_1[_i];\n            var prevWidth = newWidth;\n            var prevHeight = newHeight;\n            newWidth = Math.floor(newWidth / step) - 1;\n            newHeight = Math.floor(newHeight / step) - 1;\n            var canvas = document.createElement('canvas');\n            canvas.width = newWidth;\n            canvas.height = newHeight;\n            var ctx = canvas.getContext('2d');\n            ctx.drawImage(bitmap, 0, 0, prevWidth, prevHeight, 0, 0, newWidth, newHeight);\n            bitmap.close();\n        }\n        imgData.data = null;\n        imgData.bitmap = bitmap;\n        imgData.width = newWidth;\n        imgData.height = newHeight;\n        return imgData;\n    };\n    /**\n     * Downscales raw image data to fit into maximum buffer constraints and returns data or null.\n     *\n     * @private\n     * @returns {any} Updated `imgData` when final image can be used directly; otherwise `null` to continue.\n     */\n    _PdfImageResizer.prototype._rescaleImageData = function () {\n        var imgData = this._imgData;\n        var data = imgData.data, width = imgData.width, height = imgData.height, kind = imgData.kind;\n        var rgbaSize = width * height * 4;\n        var k = Math.ceil(Math.log2(rgbaSize / _image_utils__WEBPACK_IMPORTED_MODULE_0__.maximumCount));\n        var newWidth = width >> k;\n        var newHeight = height >> k;\n        var rgbaData = null;\n        var maxHeight = height;\n        try {\n            rgbaData = new Uint8Array(rgbaSize);\n        }\n        catch (_a) {\n            var n = Math.floor(Math.log2(rgbaSize + 1));\n            while (true) { // eslint-disable-line\n                try {\n                    rgbaData = new Uint8Array(Math.pow(2, n) - 1);\n                    break;\n                }\n                catch (_b) {\n                    n -= 1;\n                }\n            }\n            maxHeight = Math.floor((Math.pow(2, n) - 1) / (width * 4));\n            var newSize = width * maxHeight * 4;\n            if (newSize < rgbaData.length) {\n                rgbaData = new Uint8Array(newSize);\n            }\n        }\n        if (rgbaData === null) {\n            return null;\n        }\n        var src32 = new Uint32Array(rgbaData.buffer);\n        var dest32 = new Uint32Array(newWidth * newHeight);\n        var newIndex = 0;\n        var step = Math.ceil(height / maxHeight);\n        var remainder = height % maxHeight === 0 ? height : height % maxHeight;\n        for (var k_1 = 0; k_1 < step; k_1++) {\n            var h = k_1 < step - 1 ? maxHeight : remainder;\n            (0,_image_utils__WEBPACK_IMPORTED_MODULE_0__._convertToRGBA)(kind, data, src32, width, h, this._isMask);\n            for (var i = 0, ii = h >> k_1; i < ii; i++) {\n                var buf = src32.subarray((i << k_1) * width);\n                for (var j = 0; j < newWidth; j++) {\n                    dest32[newIndex++] = buf[j << k_1];\n                }\n            }\n        }\n        if (this._needsToBeResized(newWidth, newHeight)) {\n            imgData.data = dest32;\n            imgData.width = newWidth;\n            imgData.height = newHeight;\n            imgData.kind = _image_utils__WEBPACK_IMPORTED_MODULE_0__.imageKind.rgba32BPP;\n            return null;\n        }\n        var canvas = document.createElement('canvas');\n        canvas.width = newWidth;\n        canvas.height = newHeight;\n        var ctx = canvas.getContext('2d');\n        ctx.putImageData(new ImageData(new Uint8ClampedArray(dest32.buffer), newWidth, newHeight), 0, 0);\n        imgData.data = null;\n        //imgData.bitmap = canvas.transferToImageBitmap();\n        imgData.width = newWidth;\n        imgData.height = newHeight;\n        return imgData;\n    };\n    /**\n     * Encodes the image as a BMP byte array.\n     *\n     * @private\n     * @returns {Uint8Array} BMP-encoded byte array.\n     * @throws {Error} On invalid format.\n     */\n    _PdfImageResizer.prototype._encodeBMP = function () {\n        var _a = this._imgData, width = _a.width, height = _a.height, kind = _a.kind;\n        var data = this._imgData.data; // eslint-disable-line\n        var bitPerPixel;\n        var view;\n        var colorTable = new Uint8Array(0);\n        var maskTable = colorTable;\n        var compression = 0;\n        switch (kind) {\n            case _image_utils__WEBPACK_IMPORTED_MODULE_0__.imageKind.grayScale1Bpp: {\n                bitPerPixel = 1;\n                colorTable = new Uint8Array(this._isMask ? [255, 255, 255, 255, 0, 0, 0, 0] : [0, 0, 0, 0, 255, 255, 255, 255]);\n                var rowLen = (width + 7) >> 3;\n                var rowSize = (rowLen + 3) & -4;\n                if (rowLen !== rowSize) {\n                    var newData = new Uint8Array(rowSize * height);\n                    var k = 0;\n                    for (var i_1 = 0, ii = height * rowLen; i_1 < ii; i_1 += rowLen, k += rowSize) {\n                        newData.set(data.subarray(i_1, i_1 + rowLen), k);\n                    }\n                    data = newData;\n                }\n                break;\n            }\n            case _image_utils__WEBPACK_IMPORTED_MODULE_0__.imageKind.rgb24BPP: {\n                bitPerPixel = 24;\n                if (width & 3) {\n                    var rowLen = 3 * width;\n                    var rowSize = (rowLen + 3) & -4;\n                    var extraLen = rowSize - rowLen;\n                    var newData = new Uint8Array(rowSize * height);\n                    var k = 0;\n                    for (var i_2 = 0, ii = height * rowLen; i_2 < ii; i_2 += rowLen) {\n                        var row = data.subarray(i_2, i_2 + rowLen); // eslint-disable-line\n                        for (var j = 0; j < rowLen; j += 3) {\n                            newData[k++] = row[j + 2];\n                            newData[k++] = row[j + 1];\n                            newData[k++] = row[j];\n                        }\n                        k += extraLen;\n                    }\n                    data = newData;\n                }\n                else {\n                    for (var i_3 = 0, ii = data.length; i_3 < ii; i_3 += 3) {\n                        var tmp = data[i_3]; // eslint-disable-line\n                        data[i_3] = data[i_3 + 2];\n                        data[i_3 + 2] = tmp;\n                    }\n                }\n                break;\n            }\n            case _image_utils__WEBPACK_IMPORTED_MODULE_0__.imageKind.rgba32BPP:\n                bitPerPixel = 32;\n                compression = 3;\n                maskTable = new Uint8Array(4 + 4 + 4 + 4 + 52);\n                view = new DataView(maskTable.buffer);\n                if ((0,_syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_1__._isLittleEndian)()) {\n                    view.setUint32(0, 0x000000ff, true);\n                    view.setUint32(4, 0x0000ff00, true);\n                    view.setUint32(8, 0x00ff0000, true);\n                    view.setUint32(12, 0xff000000, true);\n                }\n                else {\n                    view.setUint32(0, 0xff000000, true);\n                    view.setUint32(4, 0x00ff0000, true);\n                    view.setUint32(8, 0x0000ff00, true);\n                    view.setUint32(12, 0x000000ff, true);\n                }\n                break;\n            default:\n                throw new Error('invalid format');\n        }\n        var i = 0;\n        var headerLength = 40 + maskTable.length;\n        var fileLength = 14 + headerLength + colorTable.length + data.length;\n        var bmpData = new Uint8Array(fileLength);\n        view = new DataView(bmpData.buffer);\n        view.setUint16(i, 0x4d42, true);\n        i += 2;\n        view.setUint32(i, fileLength, true);\n        i += 4;\n        view.setUint32(i, 0, true);\n        i += 4;\n        view.setUint32(i, 14 + headerLength + colorTable.length, true);\n        i += 4;\n        view.setUint32(i, headerLength, true);\n        i += 4;\n        view.setInt32(i, width, true);\n        i += 4;\n        view.setInt32(i, -height, true);\n        i += 4;\n        view.setUint16(i, 1, true);\n        i += 2;\n        view.setUint16(i, bitPerPixel, true);\n        i += 2;\n        view.setUint32(i, compression, true);\n        i += 4;\n        view.setUint32(i, 0, true);\n        i += 4;\n        view.setInt32(i, 0, true);\n        i += 4;\n        view.setInt32(i, 0, true);\n        i += 4;\n        view.setUint32(i, colorTable.length / 4, true);\n        i += 4;\n        view.setUint32(i, 0, true);\n        i += 4;\n        bmpData.set(maskTable, i);\n        i += maskTable.length;\n        bmpData.set(colorTable, i);\n        i += colorTable.length;\n        bmpData.set(data, i);\n        return bmpData;\n    };\n    return _PdfImageResizer;\n}());\n\n\n\n//# sourceURL=webpack://@syncfusion/ej2-documents-vue-samples/./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/image-extraction/image-resizer.js?");

/***/ }),

/***/ "./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/image-extraction/image-structure.js":
/*!*********************************************************************************************************************!*\
  !*** ./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/image-extraction/image-structure.js ***!
  \*********************************************************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   _ImageStructure: () => (/* binding */ _ImageStructure)\n/* harmony export */ });\n/* harmony import */ var _enum__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../enum */ \"./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/enum.js\");\n\n/**\n * Holds parsed image stream metadata and references required for extraction/rendering.\n *\n * @private\n */\nvar _ImageStructure = /** @class */ (function () {\n    function _ImageStructure(stream, crossReference, page) {\n        /**\n         * Indicates whether the image has a hard mask entry.\n         *\n         * @private\n         */\n        this._isImageMasked = false;\n        /**\n         * Indicates whether the image has a soft mask entry.\n         *\n         * @private\n         */\n        this._isSoftMasked = false;\n        /**\n         * Indicates whether this image is an image mask .\n         *\n         * @private\n         */\n        this._isImageMask = false;\n        /**\n         * Indicates whether interpolation is requested for this image.\n         *\n         * @private\n         */\n        this._isImageInterpolated = false;\n        this._stream = stream;\n        this._crossReference = crossReference;\n        this._pageIndex = page._pageIndex;\n        this._initialize();\n    }\n    /**\n     * Parses the image dictionary to populate size, mask flags, and output format.\n     *\n     * @private\n     * @returns {void} nothing.\n     */\n    _ImageStructure.prototype._initialize = function () {\n        var dictionary = this._stream.dictionary;\n        if (dictionary.has('Width')) {\n            this._width = dictionary.get('Width');\n        }\n        if (dictionary.has('Height')) {\n            this._height = dictionary.get('Height');\n        }\n        if (dictionary.has('Mask')) {\n            this._isImageMasked = true;\n            this._smask = dictionary.get('Mask');\n            this._maskReference = dictionary.getRaw('Mask');\n        }\n        if (dictionary.has('ImageMask')) {\n            this._isImageMask = dictionary.get('ImageMask');\n        }\n        if (dictionary.has('SMask')) {\n            this._isSoftMasked = true;\n            this._smask = dictionary.get('SMask');\n            this._smaskReference = dictionary.getRaw('SMask');\n        }\n        if (dictionary.has('isImageInterpolate')) {\n            this._isImageInterpolated = true;\n        }\n        if (this._isImageMasked || this._isSoftMasked || this._isImageMask) {\n            this._imageFormat = _enum__WEBPACK_IMPORTED_MODULE_0__.ImageFormat.png;\n        }\n        else {\n            this._imageFormat = _enum__WEBPACK_IMPORTED_MODULE_0__.ImageFormat.jpeg;\n        }\n    };\n    return _ImageStructure;\n}());\n\n\n\n//# sourceURL=webpack://@syncfusion/ej2-documents-vue-samples/./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/image-extraction/image-structure.js?");

/***/ }),

/***/ "./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/image-extraction/image-utils.js":
/*!*****************************************************************************************************************!*\
  !*** ./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/image-extraction/image-utils.js ***!
  \*****************************************************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   _convertBlackAndWhiteToRGBA: () => (/* binding */ _convertBlackAndWhiteToRGBA),\n/* harmony export */   _convertRGBToRGBA: () => (/* binding */ _convertRGBToRGBA),\n/* harmony export */   _convertToRGBA: () => (/* binding */ _convertToRGBA),\n/* harmony export */   _grayToRGBA: () => (/* binding */ _grayToRGBA),\n/* harmony export */   imageKind: () => (/* binding */ imageKind),\n/* harmony export */   maximumCount: () => (/* binding */ maximumCount)\n/* harmony export */ });\n/* harmony import */ var _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @syncfusion/ej2-pdf */ \"./node_modules/@syncfusion/ej2-pdf/index.js\");\n\nvar imageKind = { grayScale1Bpp: 1, rgb24BPP: 2, rgba32BPP: 3 }; // eslint-disable-line\nvar maximumCount = Math.pow(2, 31) - 1;\n/**\n * Converts a source buffer into RGBA destination buffer based on the image kind.\n *\n * @private\n * @param {any} kind Image format kind.\n * @param {any} src Source pixel buffer .\n * @param {Uint32Array} dest Destination buffer.\n * @param {number} width Image width in pixels.\n * @param {number} height Image height in pixels.\n * @param {boolean} inverseDecode For 1bpp grayscale: whether to invert decode mapping.\n * @returns {Promise<any>} A promise resolving to conversion result or `null` if not handled here.\n */\nfunction _convertToRGBA(kind, src, dest, width, height, inverseDecode) {\n    switch (kind) {\n        case imageKind.grayScale1Bpp:\n            return _convertBlackAndWhiteToRGBA(src, 0, dest, width, height, 0xffffffff, inverseDecode);\n        case imageKind.rgb24BPP:\n            return _convertRGBToRGBA(src, 0, dest, 0, width, height);\n    }\n    return null;\n}\n/**\n * Converts a 1-bit grayscale source into RGBA.\n *\n * @private\n * @param {Uint8Array} src Source buffer.\n * @param {any} srcPos Start bit/byte position index in `src`.\n * @param {any} dest Destination buffer to write RGBA pixels into.\n * @param {number} width Image width in pixels.\n * @param {number} height Image height in pixels.\n * @param {number} [nonBlackColor=0xffffffff] Color used for '1' bits.\n * @param {boolean} [inverseDecode=false] If `true`, swaps mapping of 0/1 bits.\n * @returns {any} Object with updated srcPos, destPos.\n */\nfunction _convertBlackAndWhiteToRGBA(src, srcPos, dest, width, // eslint-disable-line\nheight, nonBlackColor, inverseDecode) {\n    if (nonBlackColor === void 0) { nonBlackColor = 0xffffffff; }\n    if (inverseDecode === void 0) { inverseDecode = false; }\n    var black = (0,_syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__._isLittleEndian)() ? 0xff000000 : 0x000000ff;\n    var _a = inverseDecode\n        ? [nonBlackColor, black]\n        : [black, nonBlackColor], zeroMapping = _a[0], oneMapping = _a[1];\n    var widthInSource = width >> 3;\n    var widthRemainder = width & 7;\n    var srcLength = src.length;\n    dest = new Uint32Array(dest.buffer);\n    var destPos = 0;\n    for (var i = 0; i < height; i++) {\n        for (var max = srcPos + widthInSource; srcPos < max; srcPos++) {\n            var elem_1 = srcPos < srcLength ? src[srcPos] : 255;\n            dest[destPos++] = elem_1 & 128 ? oneMapping : zeroMapping;\n            dest[destPos++] = elem_1 & 64 ? oneMapping : zeroMapping;\n            dest[destPos++] = elem_1 & 32 ? oneMapping : zeroMapping;\n            dest[destPos++] = elem_1 & 16 ? oneMapping : zeroMapping;\n            dest[destPos++] = elem_1 & 8 ? oneMapping : zeroMapping;\n            dest[destPos++] = elem_1 & 4 ? oneMapping : zeroMapping;\n            dest[destPos++] = elem_1 & 2 ? oneMapping : zeroMapping;\n            dest[destPos++] = elem_1 & 1 ? oneMapping : zeroMapping;\n        }\n        if (widthRemainder === 0) {\n            continue;\n        }\n        var elem = srcPos < srcLength ? src[srcPos++] : 255;\n        for (var j = 0; j < widthRemainder; j++) {\n            dest[destPos++] = elem & (1 << (7 - j)) ? oneMapping : zeroMapping;\n        }\n    }\n    return { srcPos: srcPos, destPos: destPos };\n}\n/**\n * Converts an RGB source buffer into an RGBA destination buffer .\n *\n * @private\n * @param {any} src Source buffer containing RGB bytes.\n * @param {number} [srcPos=0] Start offset in the source buffer.\n * @param {Uint32Array} dest Destination buffer to receive packed RGBA pixels.\n * @param {number} [destPos=0] Start index in the destination buffer (in 32-bit words).\n * @param {number} width Image width in pixels.\n * @param {number} height Image height in pixels.\n * @returns {any} Object with updated srcPos, destPos.\n */\nfunction _convertRGBToRGBA(src, srcPos, dest, destPos, width, // eslint-disable-line\nheight) {\n    if (srcPos === void 0) { srcPos = 0; }\n    if (destPos === void 0) { destPos = 0; }\n    var i = 0;\n    var len = width * height * 3;\n    var len32 = len >> 2;\n    var src32 = new Uint32Array(src.buffer, srcPos, len32);\n    if ((0,_syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__._isLittleEndian)()) {\n        for (; i < len32 - 2; i += 3, destPos += 4) {\n            var s1 = src32[i];\n            var s2 = src32[i + 1];\n            var s3 = src32[i + 2];\n            dest[destPos] = s1 | 0xff000000;\n            dest[destPos + 1] = (s1 >>> 24) | (s2 << 8) | 0xff000000;\n            dest[destPos + 2] = (s2 >>> 16) | (s3 << 16) | 0xff000000;\n            dest[destPos + 3] = (s3 >>> 8) | 0xff000000;\n        }\n        for (var j = i * 4, jj = srcPos + len; j < jj; j += 3) {\n            dest[destPos++] =\n                src[j] | (src[j + 1] << 8) | (src[j + 2] << 16) | 0xff000000;\n        }\n    }\n    else {\n        for (; i < len32 - 2; i += 3, destPos += 4) {\n            var s1 = src32[i];\n            var s2 = src32[i + 1];\n            var s3 = src32[i + 2];\n            dest[destPos] = s1 | 0xff;\n            dest[destPos + 1] = (s1 << 24) | (s2 >>> 8) | 0xff;\n            dest[destPos + 2] = (s2 << 16) | (s3 >>> 16) | 0xff;\n            dest[destPos + 3] = (s3 << 8) | 0xff;\n        }\n        for (var j = i * 4, jj = srcPos + len; j < jj; j += 3) {\n            dest[destPos++] =\n                (src[j] << 24) | (src[j + 1] << 16) | (src[j + 2] << 8) | 0xff;\n        }\n    }\n    return { srcPos: srcPos + len, destPos: destPos };\n}\n/**\n * Converts a grayscale image represented by a Uint8Array into an RGBA format stored in a Uint32Array.\n *\n * @private\n * @param {Uint8Array} src - The source array containing grayscale pixel values (0 to 255).\n * @param {Uint32Array} dest - The destination array where converted RGBA values will be stored.\n * @returns {void} This function does not return a value.\n */\nfunction _grayToRGBA(src, dest) {\n    if ((0,_syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__._isLittleEndian)()) {\n        for (var i = 0, ii = src.length; i < ii; i++) {\n            dest[i] = (src[i] * 0x10101) | 0xff000000;\n        }\n    }\n    else {\n        for (var i = 0, ii = src.length; i < ii; i++) {\n            dest[i] = (src[i] * 0x1010100) | 0x000000ff;\n        }\n    }\n}\n\n\n//# sourceURL=webpack://@syncfusion/ej2-documents-vue-samples/./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/image-extraction/image-utils.js?");

/***/ }),

/***/ "./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/image-extraction/pdf-embedded-image.js":
/*!************************************************************************************************************************!*\
  !*** ./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/image-extraction/pdf-embedded-image.js ***!
  \************************************************************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   PdfEmbeddedImage: () => (/* binding */ PdfEmbeddedImage)\n/* harmony export */ });\n/**\n * The class provides detailed metadata about an image that is embedded inside a PDF document.\n *\n * ```typescript\n * // Load an existing PDF document\n * let document: PdfDocument = new PdfDocument(data);\n * // Initialize a new instance of the `PdfDataExtractor` class\n * let extractor: PdfDataExtractor = new PdfDataExtractor(document);\n * // Extract collection of `PdfEmbeddedImage` from the PDF document.\n * let imageInfoCollection: PdfEmbeddedImage[] = extractor.extractImages({ startPageIndex: 0, endPageIndex: document.pageCount - 1});\n * let imageInfo: PdfEmbeddedImage = imageInfoCollection[0];\n * // Gets the raw image bytes of the embedded image.\n * let data: Uint8Array = imageInfo.data;\n * // Gets the image format.\n * let type: ImageFormat = imageInfo.type;\n * // Gets the page index of the image.\n * let pageIndex: number = imageInfo.pageIndex;\n * // Gets the index of the image occurrence within PDF page.\n * let index: number = imageInfo.index;\n * // Gets the bounds of the image.\n * let bounds: Rectangle = imageInfo.bounds;\n * // Gets the boolean flag indicating whether the image is interpolated or not.\n * let isImageInterpolated: boolean = imageInfo.isImageInterpolated;\n * // Gets the boolean flag indicating whether the image is masked or not.\n * let IsImageMasked: boolean = imageInfo.IsImageMasked;\n * // Gets the boolean flag indicating whether the image is soft masked or not.\n * let IsSoftMasked: boolean = imageInfo.IsSoftMasked;\n * // Destroy the documents\n * document.destroy();\n * ```\n */\nvar PdfEmbeddedImage = /** @class */ (function () {\n    function PdfEmbeddedImage() {\n        /**\n         * Raw decoded bytes of the embedded image.\n         *\n         * @private\n         */\n        this._data = new Uint8Array([]);\n    }\n    Object.defineProperty(PdfEmbeddedImage.prototype, \"data\", {\n        /**\n         * Gets the raw image bytes of the embedded image.\n         *\n         * @returns {Uint8Array} The image data.\n         *\n         * ```typescript\n         * // Load an existing PDF document\n         * let document: PdfDocument = new PdfDocument(data);\n         * // Initialize a new instance of the `PdfDataExtractor` class\n         * let extractor: PdfDataExtractor = new PdfDataExtractor(document);\n         * // Extract collection of `PdfEmbeddedImage` from the PDF document.\n         * let imageInfoCollection: PdfEmbeddedImage[] = extractor.extractImages({ startPageIndex: 0, endPageIndex: document.pageCount - 1});\n         * let imageInfo: PdfEmbeddedImage = imageInfoCollection[0];\n         * // Gets the raw image bytes of the embedded image.\n         * let data: Uint8Array = imageInfo.data;\n         * // Destroy the documents\n         * document.destroy();\n         * ```\n         */\n        get: function () {\n            return this._data;\n        },\n        enumerable: true,\n        configurable: true\n    });\n    Object.defineProperty(PdfEmbeddedImage.prototype, \"type\", {\n        /**\n         * Gets the image format of the embedded image.\n         *\n         * @returns {ImageFormat} The image format\n         *\n         * ```typescript\n         * // Load an existing PDF document\n         * let document: PdfDocument = new PdfDocument(data);\n         * // Initialize a new instance of the `PdfDataExtractor` class\n         * let extractor: PdfDataExtractor = new PdfDataExtractor(document);\n         * // Extract collection of `PdfEmbeddedImage` from the PDF document.\n         * let imageInfoCollection: PdfEmbeddedImage[] = extractor.extractImages({ startPageIndex: 0, endPageIndex: document.pageCount - 1});\n         * let imageInfo: PdfEmbeddedImage = imageInfoCollection[0];\n         * // Gets the image format of the embedded image.\n         * let type: ImageFormat = imageInfo.type;\n         * // Destroy the documents\n         * document.destroy();\n         * ```\n         */\n        get: function () {\n            return this._type;\n        },\n        enumerable: true,\n        configurable: true\n    });\n    Object.defineProperty(PdfEmbeddedImage.prototype, \"resourceName\", {\n        /**\n         * Gets the XObject resource name for this image\n         *\n         * @returns {string} The resource name.\n         *\n         * ```typescript\n         * // Load an existing PDF document\n         * let document: PdfDocument = new PdfDocument(data);\n         * // Initialize a new instance of the `PdfDataExtractor` class\n         * let extractor: PdfDataExtractor = new PdfDataExtractor(document);\n         * // Extract collection of `PdfEmbeddedImage` from the PDF document.\n         * let imageInfoCollection: PdfEmbeddedImage[] = extractor.extractImages({ startPageIndex: 0, endPageIndex: document.pageCount - 1});\n         * let imageInfo: PdfEmbeddedImage = imageInfoCollection[0];\n         * // Gets the XObject resource name for this image\n         * let name: string = imageInfo.resourceName;\n         * // Destroy the documents\n         * document.destroy();\n         * ```\n         */\n        get: function () {\n            return this._resourceName;\n        },\n        enumerable: true,\n        configurable: true\n    });\n    Object.defineProperty(PdfEmbeddedImage.prototype, \"physicalDimension\", {\n        /**\n         * Gets the native pixel dimensions of the image.\n         *\n         * @returns {Size} The width and height of the image in pixels.\n         *\n         * ```typescript\n         * // Load an existing PDF document\n         * let document: PdfDocument = new PdfDocument(data);\n         * // Initialize a new instance of the `PdfDataExtractor` class\n         * let extractor: PdfDataExtractor = new PdfDataExtractor(document);\n         * // Extract collection of `PdfEmbeddedImage` from the PDF document.\n         * let imageInfoCollection: PdfEmbeddedImage[] = extractor.extractImages({ startPageIndex: 0, endPageIndex: document.pageCount - 1});\n         * let imageInfo: PdfEmbeddedImage = imageInfoCollection[0];\n         * // Gets the image data.\n         * let physicalDimension: Size = imageInfo.physicalDimension;\n         * // Destroy the documents\n         * document.destroy();\n         * ```\n         */\n        get: function () {\n            return this._physicalDimension;\n        },\n        enumerable: true,\n        configurable: true\n    });\n    Object.defineProperty(PdfEmbeddedImage.prototype, \"pageIndex\", {\n        /**\n         * Gets the page index of the image.\n         *\n         * @returns {string} The page index of the image.\n         *\n         * ```typescript\n         * // Load an existing PDF document\n         * let document: PdfDocument = new PdfDocument(data);\n         * // Initialize a new instance of the `PdfDataExtractor` class\n         * let extractor: PdfDataExtractor = new PdfDataExtractor(document);\n         * // Extract collection of `PdfEmbeddedImage` from the PDF document.\n         * let imageInfoCollection: PdfEmbeddedImage[] = extractor.extractImages({ startPageIndex: 0, endPageIndex: document.pageCount - 1});\n         * let imageInfo: PdfEmbeddedImage = imageInfoCollection[0];\n         * // Gets the page index of the image.\n         * let pageIndex: number = imageInfo.pageIndex;\n         * // Destroy the documents\n         * document.destroy();\n         * ```\n         */\n        get: function () {\n            return this._pageIndex;\n        },\n        enumerable: true,\n        configurable: true\n    });\n    Object.defineProperty(PdfEmbeddedImage.prototype, \"index\", {\n        /**\n         * Gets the index of the image occurrence within PDF page.\n         *\n         * @returns {number} An index of the image.\n         *\n         * ```typescript\n         * // Load an existing PDF document\n         * let document: PdfDocument = new PdfDocument(data);\n         * // Initialize a new instance of the `PdfDataExtractor` class\n         * let extractor: PdfDataExtractor = new PdfDataExtractor(document);\n         * // Extract collection of `PdfEmbeddedImage` from the PDF document.\n         * let imageInfoCollection: PdfEmbeddedImage[] = extractor.extractImages({ startPageIndex: 0, endPageIndex: document.pageCount - 1});\n         * let imageInfo: PdfEmbeddedImage = imageInfoCollection[0];\n         * // Gets the index of the image occurrence within PDF page.\n         * let index: number = imageInfo.index;\n         * // Destroy the documents\n         * document.destroy();\n         * ```\n         */\n        get: function () {\n            return this._index;\n        },\n        enumerable: true,\n        configurable: true\n    });\n    Object.defineProperty(PdfEmbeddedImage.prototype, \"bounds\", {\n        /**\n         * Gets the bounds of the image.\n         *\n         * @returns {Rectangle} The bounds of the image.\n         *\n         * ```typescript\n         * // Load an existing PDF document\n         * let document: PdfDocument = new PdfDocument(data);\n         * // Initialize a new instance of the `PdfDataExtractor` class\n         * let extractor: PdfDataExtractor = new PdfDataExtractor(document);\n         * // Extract collection of `PdfEmbeddedImage` from the PDF document.\n         * let imageInfoCollection: PdfEmbeddedImage[] = extractor.extractImages({ startPageIndex: 0, endPageIndex: document.pageCount - 1});\n         * let imageInfo: PdfEmbeddedImage = imageInfoCollection[0];\n         * // Gets the bounds of the image.\n         * let bounds: Rectangle = imageInfo.bounds;\n         * // Destroy the documents\n         * document.destroy();\n         * ```\n         */\n        get: function () {\n            return this._bounds;\n        },\n        enumerable: true,\n        configurable: true\n    });\n    Object.defineProperty(PdfEmbeddedImage.prototype, \"isImageInterpolated\", {\n        /**\n         * Gets the boolean flag indicating whether the image is interpolated or not.\n         *\n         * @returns {boolean} Returns true, when the image property in the PDF document is set to undergo interpolation.\n         *\n         * ```typescript\n         * // Load an existing PDF document\n         * let document: PdfDocument = new PdfDocument(data);\n         * // Initialize a new instance of the `PdfDataExtractor` class\n         * let extractor: PdfDataExtractor = new PdfDataExtractor(document);\n         * // Extract collection of `PdfEmbeddedImage` from the PDF document.\n         * let imageInfoCollection: PdfEmbeddedImage[] = extractor.extractImages({ startPageIndex: 0, endPageIndex: document.pageCount - 1});\n         * let imageInfo: PdfEmbeddedImage = imageInfoCollection[0];\n         * // Gets the boolean flag indicating whether the image is interpolated or not.\n         * let isImageInterpolated: boolean = imageInfo.isImageInterpolated;\n         * // Destroy the documents\n         * document.destroy();\n         * ```\n         */\n        get: function () {\n            return this._isImageInterpolated;\n        },\n        enumerable: true,\n        configurable: true\n    });\n    Object.defineProperty(PdfEmbeddedImage.prototype, \"isImageMasked\", {\n        /**\n         * Gets the boolean flag indicating whether the image is masked or not.\n         *\n         * @returns {boolean} Returns true, if image masking is applied. Set to true when the image is undergone image masking.\n         *\n         * ```typescript\n         * // Load an existing PDF document\n         * let document: PdfDocument = new PdfDocument(data);\n         * // Initialize a new instance of the `PdfDataExtractor` class\n         * let extractor: PdfDataExtractor = new PdfDataExtractor(document);\n         * // Extract collection of `PdfEmbeddedImage` from the PDF document.\n         * let imageInfoCollection: PdfEmbeddedImage[] = extractor.extractImages({ startPageIndex: 0, endPageIndex: document.pageCount - 1});\n         * let imageInfo: PdfEmbeddedImage = imageInfoCollection[0];\n         * // Gets the boolean flag indicating whether the image is masked or not.\n         * let IsImageMasked: boolean = imageInfo.IsImageMasked;\n         * // Destroy the documents\n         * document.destroy();\n         * ```\n         */\n        get: function () {\n            return this._isImageMasked;\n        },\n        enumerable: true,\n        configurable: true\n    });\n    Object.defineProperty(PdfEmbeddedImage.prototype, \"isSoftMasked\", {\n        /**\n         * Gets the boolean flag indicating whether the image is masked or not.\n         *\n         * @returns {boolean} True if a soft mask is associated with the image; otherwise, false.\n         *\n         * ```typescript\n         * // Load an existing PDF document\n         * let document: PdfDocument = new PdfDocument(data);\n         * // Initialize a new instance of the `PdfDataExtractor` class\n         * let extractor: PdfDataExtractor = new PdfDataExtractor(document);\n         * // Extract collection of `PdfEmbeddedImage` from the PDF document.\n         * let imageInfoCollection: PdfEmbeddedImage[] = extractor.extractImages({ startPageIndex: 0, endPageIndex: document.pageCount - 1});\n         * let imageInfo: PdfEmbeddedImage = imageInfoCollection[0];\n         * // Gets the boolean flag indicating whether the image is soft masked or not.\n         * let IsSoftMasked: boolean = imageInfo.IsSoftMasked;\n         * // Destroy the documents\n         * document.destroy();\n         * ```\n         */\n        get: function () {\n            return this._isSoftMasked;\n        },\n        enumerable: true,\n        configurable: true\n    });\n    return PdfEmbeddedImage;\n}());\n\n\n\n//# sourceURL=webpack://@syncfusion/ej2-documents-vue-samples/./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/image-extraction/pdf-embedded-image.js?");

/***/ })

}]);