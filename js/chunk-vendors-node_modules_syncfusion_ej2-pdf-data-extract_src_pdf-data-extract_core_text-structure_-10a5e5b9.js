"use strict";
/*
 * ATTENTION: The "eval" devtool has been used (maybe by default in mode: "development").
 * This devtool is neither made for production nor for readable output files.
 * It uses "eval()" calls to create a separate source file in the browser devtools.
 * If you are trying to read the output file, select a different devtool (https://webpack.js.org/configuration/devtool/)
 * or disable the default devtool with "devtool: false".
 * If you are looking for production-ready output files, see mode: "production" (https://webpack.js.org/configuration/mode/).
 */
(self["webpackChunk_syncfusion_ej2_documents_vue_samples"] = self["webpackChunk_syncfusion_ej2_documents_vue_samples"] || []).push([["chunk-vendors-node_modules_syncfusion_ej2-pdf-data-extract_src_pdf-data-extract_core_text-structure_-10a5e5b9"],{

/***/ "./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/text-structure.js":
/*!***************************************************************************************************!*\
  !*** ./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/text-structure.js ***!
  \***************************************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   TextGlyph: () => (/* binding */ TextGlyph),\n/* harmony export */   TextLine: () => (/* binding */ TextLine),\n/* harmony export */   TextWord: () => (/* binding */ TextWord)\n/* harmony export */ });\n/**\n * Represents a single line of extracted text from the PDF page.\n * ```typescript\n * // Load an existing PDF document\n * let document: PdfDocument = new PdfDocument(data);\n * // Initialize a new instance of the `PdfDataExtractor` class\n * let extractor: PdfDataExtractor = new PdfDataExtractor(document);\n * // Extract `TextLine` from the PDF document.\n * let textLines: Array<TextLine> = extractor.extractTextLines({ startPageIndex: 0, endPageIndex: document.pageCount-1});\n * textLines.forEach((textLine: TextLine) => {\n *   // Gets the bounds of the text line.\n *   let lineBounds: Rectangle = textLine.bounds;\n *   // Gets the single line of extracted text from the PDF page.\n *   let line: string = textLine.text;\n *   // Gets the page index of the text line extracted.\n *   let pageIndex: number = textLine.pageIndex;\n *   // Gets the collection of text words extracted from a specified page in a PDF document.\n *   let words: TextWord[] = textLine.words;\n *   // Gets the name of the font used for a particular line of text.\n *   let fontName: string = textLine.fontName;\n *   // Gets the font style used for a particular line of text.\n *   let fontStyle: PdfFontStyle = textLine.fontStyle;\n *   // Gets the font size used for a particular line of text.\n *   let fontSize: number = textLine.fontSize;\n * });\n * // Save the document\n * document.save('output.pdf');\n * // Destroy the document\n * document.destroy();\n * ```\n */\nvar TextLine = /** @class */ (function () {\n    function TextLine() {\n        /**\n         * Internal: collection of words in this line.\n         *\n         * @private\n         */\n        this._wordCollection = [];\n    }\n    Object.defineProperty(TextLine.prototype, \"text\", {\n        /**\n         * Gets the single line of extracted text from the PDF page.\n         *\n         * @returns {string} The single line of extracted text from the PDF page.\n         *\n         * ```typescript\n         * // Load an existing PDF document\n         * let document: PdfDocument = new PdfDocument(data);\n         * // Initialize a new instance of the `PdfDataExtractor` class\n         * let extractor: PdfDataExtractor = new PdfDataExtractor(document);\n         * // Extract `TextLine` from the PDF document.\n         * let textLines: Array<TextLine> = extractor.extractTextLines({ startPageIndex: 0, endPageIndex: document.pageCount-1});\n         * textLines.forEach((textLine: TextLine) => {\n         *   // Gets the single line of extracted text from the PDF page.\n         *   let line: string = textLine.text;\n         * });\n         * // Save the document\n         * document.save('output.pdf');\n         * // Destroy the document\n         * document.destroy();\n         * ```\n         */\n        get: function () {\n            return this._text;\n        },\n        enumerable: true,\n        configurable: true\n    });\n    Object.defineProperty(TextLine.prototype, \"words\", {\n        /**\n         * Gets the collection of text words extracted from a specified page in a PDF document.\n         *\n         * @returns {TextWord[]} The collection of text words extracted from a specified page in a PDF document.\n         *\n         * ```typescript\n         * // Load an existing PDF document\n         * let document: PdfDocument = new PdfDocument(data);\n         * // Initialize a new instance of the `PdfDataExtractor` class\n         * let extractor: PdfDataExtractor = new PdfDataExtractor(document);\n         * // Extract `TextLine` from the PDF document.\n         * let textLines: Array<TextLine> = extractor.extractTextLines({ startPageIndex: 0, endPageIndex: document.pageCount-1});\n         * textLines.forEach((textLine: TextLine) => {\n         *   // Gets the collection of text words extracted from a specified page in a PDF document.\n         *   let words: TextWord[] = textLine.words;\n         * });\n         * // Save the document\n         * document.save('output.pdf');\n         * // Destroy the document\n         * document.destroy();\n         * ```\n         */\n        get: function () {\n            return this._wordCollection;\n        },\n        enumerable: true,\n        configurable: true\n    });\n    Object.defineProperty(TextLine.prototype, \"fontName\", {\n        /**\n         * Gets the name of the font used for a particular line of text.\n         *\n         * @returns {string} The name of the font used for a particular line of text.\n         *\n         * ```typescript\n         * // Load an existing PDF document\n         * let document: PdfDocument = new PdfDocument(data);\n         * // Initialize a new instance of the `PdfDataExtractor` class\n         * let extractor: PdfDataExtractor = new PdfDataExtractor(document);\n         * // Extract `TextLine` from the PDF document.\n         * let textLines: Array<TextLine> = extractor.extractTextLines({ startPageIndex: 0, endPageIndex: document.pageCount-1});\n         * textLines.forEach((textLine: TextLine) => {\n         *   // Gets the name of the font used for a particular line of text.\n         *   let fontName: string = textLine.fontName;\n         * // Save the document\n         * document.save('output.pdf');\n         * // Destroy the document\n         * document.destroy();\n         * ```\n         */\n        get: function () {\n            return this._fontName;\n        },\n        enumerable: true,\n        configurable: true\n    });\n    Object.defineProperty(TextLine.prototype, \"fontSize\", {\n        /**\n         * Gets the font size used for a particular line of text.\n         *\n         * @returns {number} The font size used for a particular line of text.\n         *\n         * ```typescript\n         * // Load an existing PDF document\n         * let document: PdfDocument = new PdfDocument(data);\n         * // Initialize a new instance of the `PdfDataExtractor` class\n         * let extractor: PdfDataExtractor = new PdfDataExtractor(document);\n         * // Extract `TextLine` from the PDF document.\n         * let textLines: Array<TextLine> = extractor.extractTextLines({ startPageIndex: 0, endPageIndex: document.pageCount-1});\n         * textLines.forEach((textLine: TextLine) => {\n         *   // Gets the font size used for a particular line of text.\n         *   let fontSize: number = textLine.fontSize;\n         * });\n         * // Save the document\n         * document.save('output.pdf');\n         * // Destroy the document\n         * document.destroy();\n         * ```\n         */\n        get: function () {\n            return this._fontSize;\n        },\n        enumerable: true,\n        configurable: true\n    });\n    Object.defineProperty(TextLine.prototype, \"fontStyle\", {\n        /**\n         * Gets the font style used for a particular line of text.\n         *\n         * @returns {PdfFontStyle} The font style used for a particular line of text.\n         *\n         * ```typescript\n         * // Load an existing PDF document\n         * let document: PdfDocument = new PdfDocument(data);\n         * // Initialize a new instance of the `PdfDataExtractor` class\n         * let extractor: PdfDataExtractor = new PdfDataExtractor(document);\n         * // Extract `TextLine` from the PDF document.\n         * let textLines: Array<TextLine> = extractor.extractTextLines({ startPageIndex: 0, endPageIndex: document.pageCount-1});\n         * textLines.forEach((textLine: TextLine) => {\n         *   // Gets the font style used for a particular line of text.\n         *   let fontStyle: number = textLine.fontStyle;\n         * });\n         * // Save the document\n         * document.save('output.pdf');\n         * // Destroy the document\n         * document.destroy();\n         * ```\n         */\n        get: function () {\n            return this._fontStyle;\n        },\n        enumerable: true,\n        configurable: true\n    });\n    Object.defineProperty(TextLine.prototype, \"bounds\", {\n        /**\n         * Gets the bounds of the text line.\n         *\n         *  @returns {Rectangle} The bounds of the text line.\n         *\n         * ```typescript\n         * // Load an existing PDF document\n         * let document: PdfDocument = new PdfDocument(data);\n         * // Initialize a new instance of the `PdfDataExtractor` class\n         * let extractor: PdfDataExtractor = new PdfDataExtractor(document);\n         * // Extract `TextLine` from the PDF document.\n         * let textLines: Array<TextLine> = extractor.extractTextLines({ startPageIndex: 0, endPageIndex: document.pageCount-1});\n         * textLines.forEach((textLine: TextLine) => {\n         *   // Gets the bounds of the text line.\n         *   let lineBounds: Rectangle = textLine.bounds;\n         * });\n         * // Save the document\n         * document.save('output.pdf');\n         * // Destroy the document\n         * document.destroy();\n         * ```\n         */\n        get: function () {\n            return this._bounds;\n        },\n        enumerable: true,\n        configurable: true\n    });\n    Object.defineProperty(TextLine.prototype, \"pageIndex\", {\n        /**\n         * Gets the page index of the text line extracted.\n         *\n         * @returns {number} Gets the page index of the text line extracted.\n         *\n         * ```typescript\n         * // Load an existing PDF document\n         * let document: PdfDocument = new PdfDocument(data);\n         * // Initialize a new instance of the `PdfDataExtractor` class\n         * let extractor: PdfDataExtractor = new PdfDataExtractor(document);\n         * // Extract `TextLine` from the PDF document.\n         * let textLines: Array<TextLine> = extractor.extractTextLines({ startPageIndex: 0, endPageIndex: document.pageCount-1});\n         * textLines.forEach((textLine: TextLine) => {\n         *   // Gets the page index of the text line extracted.\n         *   let pageIndex: number = textLine.pageIndex;\n         * });\n         * // Save the document\n         * document.save('output.pdf');\n         * // Destroy the document\n         * document.destroy();\n         * ```\n         */\n        get: function () {\n            return this._pageIndex;\n        },\n        enumerable: true,\n        configurable: true\n    });\n    return TextLine;\n}());\n\n/**\n * Represents a single word of extracted text from the PDF page.\n * ```typescript\n * // Load an existing PDF document\n * let document: PdfDocument = new PdfDocument(data);\n * // Initialize a new instance of the `PdfDataExtractor` class\n * let extractor: PdfDataExtractor = new PdfDataExtractor(document);\n * // Extract `TextLine` from the PDF document.\n * let textLines: Array<TextLine> = extractor.extractTextLines({ startPageIndex: 0, endPageIndex: document.pageCount-1});\n * textLines.forEach((textLine: TextLine) => {\n *   textLine.words.forEach((textWord: TextWord) => {\n *      // Gets the bounds of the text word.\n *      let wordBounds: Rectangle = textWord.bounds;\n *      // Gets the single word of extracted text from the PDF page.\n *      let word: string = textWord.text;\n *      // Gets the collection of text glyphs extracted from a specified page in a PDF document.\n *      let glyphs: TextGlyph[] = textword.glyphs;\n *      // Gets the name of the font used for a particular word.\n *      let wordFontName: string = textword.fontName;\n *      // Gets the style of the font used for a particular word.\n *      let wordFontStyle: PdfFontStyle = textword.fontStyle;\n *      // Gets the size of the font used for a particular word.\n *      let wordFontSize: number = textword.fontSize;\n *   });\n * });\n * // Save the document\n * document.save('output.pdf');\n * // Destroy the document\n * document.destroy();\n * ```\n */\nvar TextWord = /** @class */ (function () {\n    function TextWord() {\n        /**\n         * glyphs composing the word.\n         *\n         * @private\n         */\n        this._glyphs = [];\n    }\n    Object.defineProperty(TextWord.prototype, \"text\", {\n        /**\n         * Gets the single word of extracted text from the PDF page.\n         *\n         * @returns {string} The single word of extracted text from the PDF page.\n         *\n         * ```typescript\n         * // Load an existing PDF document\n         * let document: PdfDocument = new PdfDocument(data);\n         * // Initialize a new instance of the `PdfDataExtractor` class\n         * let extractor: PdfDataExtractor = new PdfDataExtractor(document);\n         * // Extract `TextLine` from the PDF document.\n         * let textLines: Array<TextLine> = extractor.extractTextLines({ startPageIndex: 0, endPageIndex: document.pageCount-1});\n         * textLines.forEach((textLine: TextLine) => {\n         *   textLine.words.forEach((textWord: TextWord) => {\n         *      // Gets the single word of extracted text from the PDF page.\n         *      let word: string = textWord.text;\n         *   });\n         * });\n         * // Save the document\n         * document.save('output.pdf');\n         * // Destroy the document\n         * document.destroy();\n         * ```\n         */\n        get: function () {\n            return this._text;\n        },\n        enumerable: true,\n        configurable: true\n    });\n    Object.defineProperty(TextWord.prototype, \"glyphs\", {\n        /**\n         * Gets the collection of text glyphs extracted from a specified page in a PDF document.\n         *\n         * @returns {TextGlyph[]} The collection of text glyphs extracted from a specified page in a PDF document.\n         *\n         * ```typescript\n         * // Load an existing PDF document\n         * let document: PdfDocument = new PdfDocument(data);\n         * // Initialize a new instance of the `PdfDataExtractor` class\n         * let extractor: PdfDataExtractor = new PdfDataExtractor(document);\n         * // Extract `TextLine` from the PDF document.\n         * let textLines: Array<TextLine> = extractor.extractTextLines({ startPageIndex: 0, endPageIndex: document.pageCount-1});\n         * textLines.forEach((textLine: TextLine) => {\n         *   textLine.words.forEach((textWord: TextWord) => {\n         *      // Gets the collection of text glyphs extracted from a specified page in a PDF document.\n         *      let glyphs: TextGlyph[] = textword.glyphs;\n         *   });\n         * });\n         * // Save the document\n         * document.save('output.pdf');\n         * // Destroy the document\n         * document.destroy();\n         * ```\n         */\n        get: function () {\n            return this._glyphs;\n        },\n        enumerable: true,\n        configurable: true\n    });\n    Object.defineProperty(TextWord.prototype, \"fontName\", {\n        /**\n         * Gets the name of the font used for a particular word.\n         *\n         * @returns {string} The name of the font used for a particular word.\n         *\n         * ```typescript\n         * // Load an existing PDF document\n         * let document: PdfDocument = new PdfDocument(data);\n         * // Initialize a new instance of the `PdfDataExtractor` class\n         * let extractor: PdfDataExtractor = new PdfDataExtractor(document);\n         * // Extract `TextLine` from the PDF document.\n         * let textLines: Array<TextLine> = extractor.extractTextLines({ startPageIndex: 0, endPageIndex: document.pageCount-1});\n         * textLines.forEach((textLine: TextLine) => {\n         *   textLine.words.forEach((textWord: TextWord) => {\n         *      // Gets the name of the font used for a particular word.\n         *      let wordFontName: string = textword.fontName;\n         *   });\n         * });\n         * // Save the document\n         * document.save('output.pdf');\n         * // Destroy the document\n         * document.destroy();\n         * ```\n         */\n        get: function () {\n            return this._fontName;\n        },\n        enumerable: true,\n        configurable: true\n    });\n    Object.defineProperty(TextWord.prototype, \"fontSize\", {\n        /**\n         *  Gets the size of the font used for a particular word.\n         *\n         * @returns {number} The size of the font used for a particular word.\n         *\n         * ```typescript\n         * // Load an existing PDF document\n         * let document: PdfDocument = new PdfDocument(data);\n         * // Initialize a new instance of the `PdfDataExtractor` class\n         * let extractor: PdfDataExtractor = new PdfDataExtractor(document);\n         * // Extract `TextLine` from the PDF document.\n         * let textLines: Array<TextLine> = extractor.extractTextLines({ startPageIndex: 0, endPageIndex: document.pageCount-1});\n         * textLines.forEach((textLine: TextLine) => {\n         *   textLine.words.forEach((textWord: TextWord) => {\n         *      // Gets the size of the font used for a particular word.\n         *      let wordFontSize: number = textword.fontSize;\n         *   });\n         * });\n         * // Save the document\n         * document.save('output.pdf');\n         * // Destroy the document\n         * document.destroy();\n         * ```\n         */\n        get: function () {\n            return this._fontSize;\n        },\n        enumerable: true,\n        configurable: true\n    });\n    Object.defineProperty(TextWord.prototype, \"fontStyle\", {\n        /**\n         * Gets the style of the font used for a particular word.\n         *\n         * @returns {PdfFontStyle} The style of the font used for a particular word.\n         *\n         * ```typescript\n         * // Load an existing PDF document\n         * let document: PdfDocument = new PdfDocument(data);\n         * // Initialize a new instance of the `PdfDataExtractor` class\n         * let extractor: PdfDataExtractor = new PdfDataExtractor(document);\n         * // Extract `TextLine` from the PDF document.\n         * let textLines: Array<TextLine> = extractor.extractTextLines({ startPageIndex: 0, endPageIndex: document.pageCount-1});\n         * textLines.forEach((textLine: TextLine) => {\n         *   textLine.words.forEach((textWord: TextWord) => {\n         *      // Gets the style of the font used for a particular word.\n         *      let wordFontStyle: PdfFontStyle = textword.fontStyle;\n         *   });\n         * });\n         * // Save the document\n         * document.save('output.pdf');\n         * // Destroy the document\n         * document.destroy();\n         * ```\n         */\n        get: function () {\n            return this._fontStyle;\n        },\n        enumerable: true,\n        configurable: true\n    });\n    Object.defineProperty(TextWord.prototype, \"bounds\", {\n        /**\n         * Gets the bounds of the text word.\n         *\n         * @returns {Rectangle} The bounds of the text word.\n         *\n         * ```typescript\n         * // Load an existing PDF document\n         * let document: PdfDocument = new PdfDocument(data);\n         * // Initialize a new instance of the `PdfDataExtractor` class\n         * let extractor: PdfDataExtractor = new PdfDataExtractor(document);\n         * // Extract `TextLine` from the PDF document.\n         * let textLines: Array<TextLine> = extractor.extractTextLines({ startPageIndex: 0, endPageIndex: document.pageCount-1});\n         * textLines.forEach((textLine: TextLine) => {\n         *   textLine.words.forEach((textWord: TextWord) => {\n         *      // Gets the bounds of the text word.\n         *      let wordBounds: Rectangle = textWord.bounds;\n         *   });\n         * });\n         * // Save the document\n         * document.save('output.pdf');\n         * // Destroy the document\n         * document.destroy();\n         * ```\n         */\n        get: function () {\n            return this._bounds;\n        },\n        enumerable: true,\n        configurable: true\n    });\n    return TextWord;\n}());\n\n/**\n * Represents a single glyph of extracted text from the PDF page.\n * ```typescript\n * // Load an existing PDF document\n * let document: PdfDocument = new PdfDocument(data);\n * // Initialize a new instance of the `PdfDataExtractor` class\n * let extractor: PdfDataExtractor = new PdfDataExtractor(document);\n * // Extract `TextLine` from the PDF document.\n * let textLines: Array<TextLine> = extractor.extractTextLines({ startPageIndex: 0, endPageIndex: document.pageCount-1});\n * textLines.forEach((textLine: TextLine) => {\n *   textLine.words.forEach((textWord: TextWord) => {\n *      textWord.glyphs.forEach((textGlyph: TextGlyph) => {\n *           // Gets the bounds of the text glyph\n *           let glyphBounds: Rectangle = textGlyph.bounds;\n *           // Gets the single character of extracted text from the PDF page.\n *           let character: string = textGlyph.text;\n *           // Gets the font size used for a particular character of the text.\n *           let fontSize: number = textGlyph.fontSize;\n *           // Gets the name of the font used for a particular character of the text.\n *           let fontName: string = textGlyph.fontName;\n *           // Gets the font style used for a particular character of the text.\n *           let fontStyle: PdfFontStyle = textGlyph.fontStyle;\n *           // Gets the text color of the text glyph.\n *           let color: PdfColor = textGlyph.color;\n *           // Gets the value indicating whether the glyph is rotated or not.\n *           let isRotated: boolean = textGlyph.isRotated;\n *      });\n *   });\n * });\n * // Save the document\n * document.save('output.pdf');\n * // Destroy the document\n * document.destroy();\n * ```\n */\nvar TextGlyph = /** @class */ (function () {\n    function TextGlyph() {\n        /**\n         * indicates whether glyph came from hex string.\n         *\n         * @private\n         */\n        this._isHex = false;\n        /**\n         * replacement marker used by redaction logic.\n         *\n         * @private\n         */\n        this._isReplace = false;\n    }\n    Object.defineProperty(TextGlyph.prototype, \"text\", {\n        /**\n         * Gets the single character of extracted text from the PDF page.\n         *\n         *  @returns {string} The single character of extracted text from the PDF page.\n         *\n         * ```typescript\n         * // Load an existing PDF document\n         * let document: PdfDocument = new PdfDocument(data);\n         * // Initialize a new instance of the `PdfDataExtractor` class\n         * let extractor: PdfDataExtractor = new PdfDataExtractor(document);\n         * // Extract `TextLine` from the PDF document.\n         * let textLines: Array<TextLine> = extractor.extractTextLines({ startPageIndex: 0, endPageIndex: document.pageCount-1});\n         * textLines.forEach((textLine: TextLine) => {\n         *   textLine.words.forEach((textWord: TextWord) => {\n         *      textWord.glyphs.forEach((textGlyph: TextGlyph) => {\n         *           // Gets the single character of extracted text from the PDF page.\n         *           let character: string = textGlyph.text;\n         *      });\n         *   });\n         * });\n         * // Save the document\n         * document.save('output.pdf');\n         * // Destroy the document\n         * document.destroy();\n         * ```\n         */\n        get: function () {\n            return this._text;\n        },\n        enumerable: true,\n        configurable: true\n    });\n    Object.defineProperty(TextGlyph.prototype, \"fontName\", {\n        /**\n         * Gets the name of the font used for a particular character of the text.\n         *\n         *  @returns {string} The name of the font used for a particular character of the text.\n         *\n         * ```typescript\n         * // Load an existing PDF document\n         * let document: PdfDocument = new PdfDocument(data);\n         * // Initialize a new instance of the `PdfDataExtractor` class\n         * let extractor: PdfDataExtractor = new PdfDataExtractor(document);\n         * // Extract `TextLine` from the PDF document.\n         * let textLines: Array<TextLine> = extractor.extractTextLines({ startPageIndex: 0, endPageIndex: document.pageCount-1});\n         * textLines.forEach((textLine: TextLine) => {\n         *   textLine.words.forEach((textWord: TextWord) => {\n         *      textWord.glyphs.forEach((textGlyph: TextGlyph) => {\n         *           // Gets the name of the font used for a particular character of the text.\n         *           let fontName: string = textGlyph.fontName;\n         *      });\n         *   });\n         * });\n         * // Save the document\n         * document.save('output.pdf');\n         * // Destroy the document\n         * document.destroy();\n         * ```\n         */\n        get: function () {\n            return this._fontName;\n        },\n        enumerable: true,\n        configurable: true\n    });\n    Object.defineProperty(TextGlyph.prototype, \"fontSize\", {\n        /**\n         * Gets the font size used for a particular character of the text.\n         *\n         *  @returns {number} The font size used for a particular character of the text.\n         *\n         * ```typescript\n         * // Load an existing PDF document\n         * let document: PdfDocument = new PdfDocument(data);\n         * // Initialize a new instance of the `PdfDataExtractor` class\n         * let extractor: PdfDataExtractor = new PdfDataExtractor(document);\n         * // Extract `TextLine` from the PDF document.\n         * let textLines: Array<TextLine> = extractor.extractTextLines({ startPageIndex: 0, endPageIndex: document.pageCount-1});\n         * textLines.forEach((textLine: TextLine) => {\n         *   textLine.words.forEach((textWord: TextWord) => {\n         *      textWord.glyphs.forEach((textGlyph: TextGlyph) => {\n         *           // Gets the font size used for a particular character of the text.\n         *           let fontSize: number = textGlyph.fontSize;\n         *      });\n         *   });\n         * });\n         * // Save the document\n         * document.save('output.pdf');\n         * // Destroy the document\n         * document.destroy();\n         * ```\n         */\n        get: function () {\n            return this._fontSize;\n        },\n        enumerable: true,\n        configurable: true\n    });\n    Object.defineProperty(TextGlyph.prototype, \"fontStyle\", {\n        /**\n         * Gets the font style used for a particular character of the text.\n         *\n         *  @returns {PdfFontStyle} The font style used for a particular character of the text.\n         *\n         * ```typescript\n         * // Load an existing PDF document\n         * let document: PdfDocument = new PdfDocument(data);\n         * // Initialize a new instance of the `PdfDataExtractor` class\n         * let extractor: PdfDataExtractor = new PdfDataExtractor(document);\n         * // Extract `TextLine` from the PDF document.\n         * let textLines: Array<TextLine> = extractor.extractTextLines({ startPageIndex: 0, endPageIndex: document.pageCount-1});\n         * textLines.forEach((textLine: TextLine) => {\n         *   textLine.words.forEach((textWord: TextWord) => {\n         *           // Gets the font style used for a particular character of the text.\n         *           let fontStyle: PdfFontStyle = textGlyph.fontStyle;\n         *      });\n         *   });\n         * });\n         * // Save the document\n         * document.save('output.pdf');\n         * // Destroy the document\n         * document.destroy();\n         * ```\n         */\n        get: function () {\n            return this._fontStyle;\n        },\n        enumerable: true,\n        configurable: true\n    });\n    Object.defineProperty(TextGlyph.prototype, \"bounds\", {\n        /**\n         * Gets the bounds of the text glyph.\n         *\n         *  @returns {string} The bounds of the text glyph.\n         *\n         * ```typescript\n         * // Load an existing PDF document\n         * let document: PdfDocument = new PdfDocument(data);\n         * // Initialize a new instance of the `PdfDataExtractor` class\n         * let extractor: PdfDataExtractor = new PdfDataExtractor(document);\n         * // Extract `TextLine` from the PDF document.\n         * let textLines: Array<TextLine> = extractor.extractTextLines({ startPageIndex: 0, endPageIndex: document.pageCount-1});\n         * textLines.forEach((textLine: TextLine) => {\n         *   textLine.words.forEach((textWord: TextWord) => {\n         *      textWord.glyphs.forEach((textGlyph: TextGlyph) => {\n         *           // Gets the bounds of the text glyph\n         *           let glyphBounds: Rectangle = textGlyph.bounds;\n         *      });\n         *   });\n         * });\n         * // Save the document\n         * document.save('output.pdf');\n         * // Destroy the document\n         * document.destroy();\n         * ```\n         */\n        get: function () {\n            return this._bounds;\n        },\n        enumerable: true,\n        configurable: true\n    });\n    Object.defineProperty(TextGlyph.prototype, \"color\", {\n        /**\n         * Gets the color of the text glyph.\n         *\n         *  @returns {PdfColor} Text color as an array of numbers.\n         *\n         * ```typescript\n         * // Load an existing PDF document\n         * let document: PdfDocument = new PdfDocument(data);\n         * // Initialize a new instance of the `PdfDataExtractor` class\n         * let extractor: PdfDataExtractor = new PdfDataExtractor(document);\n         * // Extract `TextLine` from the PDF document.\n         * let textLines: Array<TextLine> = extractor.extractTextLines({ startPageIndex: 0, endPageIndex: document.pageCount-1});\n         * textLines.forEach((textLine: TextLine) => {\n         *   textLine.words.forEach((textWord: TextWord) => {\n         *      textWord.glyphs.forEach((textGlyph: TextGlyph) => {\n         *           // Gets the text color of the text glyph.\n         *           let color: PdfColor = textGlyph.color;\n         *      });\n         *   });\n         * });\n         * // Save the document\n         * document.save('output.pdf');\n         * // Destroy the document\n         * document.destroy();\n         * ```\n         */\n        get: function () {\n            return this._color;\n        },\n        enumerable: true,\n        configurable: true\n    });\n    Object.defineProperty(TextGlyph.prototype, \"isRotated\", {\n        /**\n         * Gets the value indicating whether the glyph is rotated or not.\n         *\n         *  @returns {boolean} The rotated value of text glyph.\n         *\n         * ```typescript\n         * // Load an existing PDF document\n         * let document: PdfDocument = new PdfDocument(data);\n         * // Initialize a new instance of the `PdfDataExtractor` class\n         * let extractor: PdfDataExtractor = new PdfDataExtractor(document);\n         * // Extract `TextLine` from the PDF document.\n         * let textLines: Array<TextLine> = extractor.extractTextLines({ startPageIndex: 0, endPageIndex: document.pageCount-1});\n         * textLines.forEach((textLine: TextLine) => {\n         *   textLine.words.forEach((textWord: TextWord) => {\n         *      textWord.glyphs.forEach((textGlyph: TextGlyph) => {\n         *           // Gets the value indicating whether the glyph is rotated or not.\n         *           let isRotated: boolean = textGlyph.isRotated;\n         *      });\n         *   });\n         * });\n         * // Save the document\n         * document.save('output.pdf');\n         * // Destroy the document\n         * document.destroy();\n         * ```\n         */\n        get: function () {\n            return this._isRotated;\n        },\n        enumerable: true,\n        configurable: true\n    });\n    return TextGlyph;\n}());\n\n\n\n//# sourceURL=webpack://@syncfusion/ej2-documents-vue-samples/./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/text-structure.js?");

/***/ })

}]);