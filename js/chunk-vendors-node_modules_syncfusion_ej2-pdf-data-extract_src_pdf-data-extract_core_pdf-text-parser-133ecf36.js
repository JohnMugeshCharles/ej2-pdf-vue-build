"use strict";
/*
 * ATTENTION: The "eval" devtool has been used (maybe by default in mode: "development").
 * This devtool is neither made for production nor for readable output files.
 * It uses "eval()" calls to create a separate source file in the browser devtools.
 * If you are trying to read the output file, select a different devtool (https://webpack.js.org/configuration/devtool/)
 * or disable the default devtool with "devtool: false".
 * If you are looking for production-ready output files, see mode: "production" (https://webpack.js.org/configuration/mode/).
 */
(self["webpackChunk_syncfusion_ej2_documents_vue_samples"] = self["webpackChunk_syncfusion_ej2_documents_vue_samples"] || []).push([["chunk-vendors-node_modules_syncfusion_ej2-pdf-data-extract_src_pdf-data-extract_core_pdf-text-parser-133ecf36"],{

/***/ "./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/pdf-text-parser.js":
/*!****************************************************************************************************!*\
  !*** ./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/pdf-text-parser.js ***!
  \****************************************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   _PdfTextParser: () => (/* binding */ _PdfTextParser)\n/* harmony export */ });\n/* harmony import */ var _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @syncfusion/ej2-pdf */ \"./node_modules/@syncfusion/ej2-pdf/index.js\");\n/* harmony import */ var _utils__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./utils */ \"./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/utils.js\");\n/* harmony import */ var _text_extraction_font_structure__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./text-extraction/font-structure */ \"./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/text-extraction/font-structure.js\");\n/* harmony import */ var _text_structure__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./text-structure */ \"./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/text-structure.js\");\n/* harmony import */ var _text_extraction_matrix_helper__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./text-extraction/matrix-helper */ \"./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/text-extraction/matrix-helper.js\");\n\n\n\n\n\n/**\n * Internal PDF text parser responsible for extracting glyphs, words and lines.\n *\n * @private\n */\nvar _PdfTextParser = /** @class */ (function () {\n    function _PdfTextParser() {\n        /**\n         * Internal collection of extracted text glyphs.\n         *\n         * @private\n         */\n        this._textGlyph = [];\n        /**\n         * Internal collection of extracted text words.\n         *\n         * @private\n         */\n        this._textWord = [];\n        /**\n         * Internal collection of extracted text lines.\n         *\n         * @private\n         */\n        this._textLine = [];\n        /**\n         * Internal aggregated extracted text string.\n         *\n         * @private\n         */\n        this._extractedText = '';\n        /**\n         * Internal width accumulator.\n         *\n         * @private\n         */\n        this._width = 0;\n        /**\n         * Internal height accumulator.\n         *\n         * @private\n         */\n        this._height = 0;\n        /**\n         * Internal generic index counter.\n         *\n         * @private\n         */\n        this._index = 0;\n        /**\n         * Internal current font size.\n         *\n         * @private\n         */\n        this._fontSize = 0;\n        /**\n         * Internal array of encoded text tokens.\n         *\n         * @private\n         */\n        this._encodedText = [];\n        /**\n         * Internal previous glyph bounding rectangle.\n         *\n         * @private\n         */\n        this._previousRect = { x: 0, y: 0, width: 0, height: 0 };\n        /**\n         * Internal current bounding rectangle.\n         *\n         * @private\n         */\n        this._boundingRectangle = { x: 0, y: 0, width: 0, height: 0 };\n        /* eslint-enable */\n    }\n    /**\n     * Translate a matrix by (x,y) returning a new MatrixHelper.\n     *\n     * @private\n     * @param {number} x - translation x\n     * @param {number} y - translation y\n     * @param {_MatrixHelper} textMatrix - matrix to translate\n     * @returns {_MatrixHelper} translated matrix\n     */\n    _PdfTextParser.prototype._translateTextMatrix = function (x, y, textMatrix) {\n        var matrix = new _text_extraction_matrix_helper__WEBPACK_IMPORTED_MODULE_4__._MatrixHelper(textMatrix._m11, textMatrix._m12, textMatrix._m21, textMatrix._m22, textMatrix._m11 * x + textMatrix._m21 * y + textMatrix._offsetX, textMatrix._m12 * x + textMatrix._m22 * y + textMatrix._offsetY);\n        return matrix;\n    };\n    /**\n     * Get character width depending on font type.\n     *\n     * @private\n     * @param {number} width - raw width value\n     * @param {_FontStructure} currentFont - font structure\n     * @returns {number} advanced width\n     */\n    _PdfTextParser.prototype._getCharacterWidth = function (width, currentFont) {\n        var advancedWidth;\n        if (currentFont._type === 'TrueType') {\n            advancedWidth = width;\n        }\n        else {\n            advancedWidth = 1;\n        }\n        return advancedWidth;\n    };\n    /**\n     * Move to new line using current leading.\n     *\n     * @private\n     * @param {_TextState} textState - current text state\n     * @returns {void} nothing\n     */\n    _PdfTextParser.prototype._setNewLineWithLeading = function (textState) {\n        textState._carriageReturn();\n    };\n    /**\n     * Set text matrix and text line matrix from operand array.\n     *\n     * @private\n     * @param {string[]} element - matrix operands [a,b,c,d,e,f]\n     * @param {_TextState} textState - current text state\n     * @returns {void} nothing\n     */\n    _PdfTextParser.prototype._setTextMatrix = function (element, textState) {\n        textState._setTextMatrix(parseFloat(element[0]), parseFloat(element[1]), parseFloat(element[2]), parseFloat(element[3]), parseFloat(element[4]), parseFloat(element[5]));\n        textState._setTextLineMatrix(parseFloat(element[0]), parseFloat(element[1]), parseFloat(element[2]), parseFloat(element[3]), parseFloat(element[4]), parseFloat(element[5]));\n    };\n    /**\n     * Initialize text matrices to identity.\n     *\n     * @private\n     * @param {_TextState} textState - current text state\n     * @param {number[]} identityMatrix - identity matrix values\n     * @returns {void} nothing\n     */\n    _PdfTextParser.prototype._beginText = function (textState, identityMatrix) {\n        textState._textMatrix = identityMatrix.slice();\n        textState._textLineMatrix = identityMatrix.slice();\n    };\n    /**\n     * Set font name and size from operands.\n     *\n     * @private\n     * @param {string[]} element - font operands\n     * @param {_TextState} textState - current text state\n     * @returns {void} nothing\n     */\n    _PdfTextParser.prototype._setFont = function (element, textState) {\n        for (var j = 0; j < element.length; j++) {\n            if (element[Number.parseInt(j.toString(), 10)].indexOf('/') !== -1) {\n                textState._font = element[Number.parseInt(j.toString(), 10)].replace('/', '');\n                break;\n            }\n        }\n        textState._fontSize = Number(element[1]);\n    };\n    /**\n     * Move text placement and set leading.\n     *\n     * @private\n     * @param {string[]} element - operands [tx, ty]\n     * @param {_TextState} textState - current text state\n     * @returns {void} nothing\n     */\n    _PdfTextParser.prototype._moveTextPlacementAndSetLeading = function (element, textState) {\n        textState._leading = -Number(element[1]);\n        textState._translateTextLineMatrix(Number(element[0]), Number(element[1]));\n        textState._textMatrix = textState._textLineMatrix.slice();\n    };\n    /**\n     * Set text rise value.\n     *\n     * @private\n     * @param {string[]} element - operand array\n     * @param {_TextState} textState - current text state\n     * @returns {void} nothing\n     */\n    _PdfTextParser.prototype._setTextRise = function (element, textState) {\n        textState._textRise = Number(element[0]);\n    };\n    /**\n     * Set character spacing.\n     *\n     * @private\n     * @param {string[]} element - operand array\n     * @param {_TextState} textState - current text stat\n     * @returns {void} nothing\n     */\n    _PdfTextParser.prototype._setCharSpacing = function (element, textState) {\n        textState._charSpacing = Number(element[0]);\n    };\n    /**\n     * Set word spacing.\n     *\n     * @private\n     * @param {string[]} element - operand array\n     * @param {_TextState} textState - current text state\n     * @returns {void} nothing\n     */\n    _PdfTextParser.prototype._setWordSpacing = function (element, textState) {\n        textState._wordSpacing = Number(element[0]);\n    };\n    /**\n     * Set horizontal text scale.\n     *\n     * @private\n     * @param {string[]} element - operand array\n     * @param {_TextState} textState - current text state\n     * @returns {void} nothing\n     */\n    _PdfTextParser.prototype._setTextHorizontalScale = function (element, textState) {\n        textState._textHScale = Number(element[0]) / 100;\n    };\n    /**\n     * Update text leading.\n     *\n     * @private\n     * @param {string[]} element - operand array\n     * @param {_TextState} textState - current text state\n     * @returns {void} nothing\n     */\n    _PdfTextParser.prototype._updateTextLeading = function (element, textState) {\n        textState._leading = Number(element[0]);\n    };\n    /**\n     * Move text placement by operands.\n     *\n     * @private\n     * @param {string[]} element - operands [tx, ty]\n     * @param {_TextState} textState - current text state\n     * @returns {void} nothing\n     */\n    _PdfTextParser.prototype._moveTextPlacement = function (element, textState) {\n        textState._translateTextLineMatrix(Number(element[0]), Number(element[1]));\n        textState._textMatrix = textState._textLineMatrix.slice();\n    };\n    /**\n     * Compute current transformation matrix for glyph placement.\n     *\n     * @private\n     * @param {_FontStructure} font - current font\n     * @param {number[]} fontMatrix - font matrix values\n     * @param {_TextState} textState - current text state\n     * @returns {number[]} transform matrix [a,b,c,d,e,f]\n     */\n    _PdfTextParser.prototype._getCurrentTransform = function (font, fontMatrix, textState) {\n        var tsm = [textState._fontSize * textState._textHScale, 0, 0, textState._fontSize, 0, textState._textRise];\n        if (font._isType3Font && textState._fontSize <= 1 && !(0,_utils__WEBPACK_IMPORTED_MODULE_1__._isArrayEqual)(fontMatrix, [0.001, 0, 0, 0.001, 0, 0])) {\n            var glyphHeight = font._boundingBox[3] - font._boundingBox[1];\n            if (glyphHeight > 0) {\n                tsm[3] *= glyphHeight * fontMatrix[3];\n            }\n        }\n        return this._transform(textState._ctm, this._transform(textState._textMatrix, tsm));\n    };\n    /**\n     * Determine whether a coordinate intersects any redaction region.\n     *\n     * @private\n     * @param {number} x - x coordinate\n     * @param {number} y - y coordinate\n     * @param {PdfPage} page - page reference\n     * @param {PdfRedactionRegion[]} redactBounds - redaction regions\n     * @returns {boolean} true when coordinate intersects a redaction region\n     */\n    _PdfTextParser.prototype._isFoundText = function (x, y, page, redactBounds) {\n        var isFound = false;\n        var location = [];\n        if (y < 0) {\n            y = -y;\n        }\n        var rectValue = { x: 0, y: 0, width: 0, height: 0 };\n        var redactionBounds = redactBounds;\n        location = this._getRelativeLocation(x, y, page);\n        var yPosition = Math.floor(location[1]);\n        var count = redactionBounds.length;\n        for (var i = 0; i < count; i++) {\n            var bounds = redactionBounds[i]._bounds;\n            var ypos = Math.floor(redactionBounds[Number.parseInt(i.toString(), 10)]._bounds.y);\n            if (ypos === yPosition || (ypos === (yPosition - 1)) || (ypos === (yPosition + 1))) {\n                isFound = true;\n                break;\n            }\n            if ((bounds.y >= yPosition && yPosition >= (bounds.y - bounds.height)) || (bounds.y <= yPosition && yPosition <= (bounds.y\n                + bounds.height))) {\n                isFound = true;\n                break;\n            }\n            var size = page.size;\n            yPosition = Math.floor(size.height - y);\n            if ((bounds.y >= yPosition && yPosition >= (bounds.y - bounds.height)) || (bounds.y <= yPosition && yPosition <= (bounds.y +\n                bounds.height))) {\n                isFound = true;\n                break;\n            }\n            if (rectValue.y !== 0) {\n                if (rectValue.y < 0) {\n                    rectValue.y = -rectValue.y;\n                }\n                yPosition = Math.floor(y + rectValue.y);\n                if ((bounds.y >= yPosition && yPosition >= (bounds.y - bounds.height)) || (bounds.y <= yPosition && yPosition <= (bounds.y +\n                    bounds.height))) {\n                    isFound = true;\n                    break;\n                }\n                if (rectValue.height < 0) {\n                    rectValue.height = -rectValue.height;\n                }\n                yPosition = Math.floor(rectValue.height - yPosition);\n                if ((bounds.y >= yPosition && yPosition >= (bounds.y - bounds.height)) || (bounds.y <= yPosition && yPosition <= (bounds.y +\n                    bounds.height))) {\n                    isFound = true;\n                    break;\n                }\n            }\n        }\n        return isFound;\n    };\n    /**\n     * Convert page-space coordinates according to page rotation.\n     *\n     * @private\n     * @param {number} x - x coordinate\n     * @param {number} y - y coordinate\n     * @param {PdfPage} page - page reference\n     * @returns {number[]} rotated location [x,y]\n     */\n    _PdfTextParser.prototype._getRelativeLocation = function (x, y, page) {\n        var location = [x, y];\n        if (page.rotation === _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__.PdfRotationAngle.angle90) {\n            location[0] = page.size.height - y;\n            location[1] = x;\n        }\n        else if (page.rotation === _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__.PdfRotationAngle.angle270) {\n            location[0] = page.size.width - x;\n            location[1] = y;\n        }\n        return location;\n    };\n    /**\n     * Multiply two affine transform matrices.\n     *\n     * @private\n     * @param {number[]} m1 - left matrix\n     * @param {number[]} m2 - right matrix\n     * @returns {number[]} product matrix\n     */\n    _PdfTextParser.prototype._transform = function (m1, m2) {\n        return [m1[0] * m2[0] + m1[2] * m2[1], m1[1] * m2[0] + m1[3] * m2[1], m1[0] * m2[2] + m1[2] * m2[3], m1[1] * m2[2] + m1[3] * m2[3],\n            m1[0] * m2[4] + m1[2] * m2[5] + m1[4], m1[1] * m2[4] + m1[3] * m2[5] + m1[5]];\n    };\n    /**\n     * Get crop or media box values for page coordinate adjustments.\n     *\n     * @private\n     * @param {PdfPage} page - page reference\n     * @returns {number[]} [x0, y0, height] crop or media box values\n     */\n    _PdfTextParser.prototype._getCropOrMediaBox = function (page) {\n        var cropOrMediaBox = [];\n        if (page.cropBox[0] !== 0 || page.cropBox[3] !== 0) {\n            cropOrMediaBox[0] = page.cropBox[0];\n            cropOrMediaBox[1] = page.cropBox[2];\n            cropOrMediaBox[2] = page.cropBox[3];\n        }\n        else if (page.mediaBox[0] !== 0 || page.mediaBox[3] !== 0) {\n            cropOrMediaBox[0] = page.mediaBox[0];\n            cropOrMediaBox[1] = page.mediaBox[2];\n            cropOrMediaBox[2] = page.mediaBox[3];\n        }\n        return cropOrMediaBox;\n    };\n    /* eslint-disable */\n    /**\n     * Split encoded text tokens into decoded pieces for processing.\n     *\n     * @private\n     * @param {string} encodedText - raw encoded token\n     * @param {_FontStructure} font - current font structure\n     * @param {string[]} inputText - tokenized input text array\n     * @param {boolean} [isForRedaction=false] - include inputType for redaction\n     * @returns {{decodedList: string[], inputType?: string[]}} decoded segments (and optional inputType)\n     */\n    _PdfTextParser.prototype._getSplitText = function (encodedText, font, inputText, isForRedaction) {\n        if (isForRedaction === void 0) { isForRedaction = false; }\n        var decodedList = [];\n        var key = '';\n        var encoding = font._encoding;\n        var inputType = isForRedaction ? [] : undefined;\n        var tempString = '';\n        var splittedText;\n        var isHex = false;\n        var isWidth;\n        switch (encodedText[0]) {\n            case '(':\n                encodedText = encodedText.substring(1, encodedText.length - 1);\n                if (encodedText.indexOf('\\\\\\n') !== -1 || encodedText.indexOf('\\\\(') !== -1 || encodedText.indexOf('\\\\)') !== -1) {\n                    if (encodedText.indexOf('\\\\\\n') !== -1) {\n                        encodedText = encodedText.replace(/\\\\\\n/g, '');\n                    }\n                    if (encodedText.indexOf('\\\\(') !== -1) {\n                        encodedText = encodedText.replace(/\\\\\\(/g, '(');\n                    }\n                    if (encodedText.indexOf('\\\\)') !== -1) {\n                        encodedText = encodedText.replace(/\\\\\\)/g, ')');\n                    }\n                }\n                if (encodedText.indexOf('\\\\n') !== -1) {\n                    encodedText = encodedText.replace(/\\\\n/g, '\\n');\n                }\n                if (encodedText.indexOf('\\\\r') !== -1) {\n                    encodedText = encodedText.replace(/\\\\r/g, '\\r');\n                }\n                encodedText = (0,_utils__WEBPACK_IMPORTED_MODULE_1__._getLiteralString)(encodedText, encoding);\n                encodedText = (0,_utils__WEBPACK_IMPORTED_MODULE_1__._skipEscapeSequence)(encodedText);\n                key = encodedText + 's';\n                decodedList.push(key);\n                if (isForRedaction) {\n                    inputType.push(' ');\n                }\n                break;\n            case '[':\n                splittedText = inputText;\n                isHex = false;\n                for (var i = 0; i < splittedText.length; i++) {\n                    var input = splittedText[Number.parseInt(i.toString(), 10)];\n                    isWidth = false;\n                    if (input.indexOf('\\\\\\n') !== -1) {\n                        input = input.replace(/\\\\\\n/g, '');\n                    }\n                    if (input[0] === '<') {\n                        isHex = true;\n                        if (isForRedaction) {\n                            inputType.push(input);\n                        }\n                        input = input.slice(1, -1);\n                    }\n                    else if (input[0] === '(') {\n                        input = input.slice(1, -1);\n                    }\n                    else if (input.length > 0) {\n                        input = input.replace('\\n', '');\n                        decodedList.push(input);\n                        if (isForRedaction) {\n                            inputType.push(' ');\n                        }\n                        continue;\n                    }\n                    if (isHex) {\n                        tempString = (0,_utils__WEBPACK_IMPORTED_MODULE_1__._hexToChar)(input);\n                    }\n                    else {\n                        tempString = (0,_utils__WEBPACK_IMPORTED_MODULE_1__._getLiteralString)(input, encoding);\n                        if (tempString.indexOf('\\\\') !== -1) {\n                            tempString = (0,_utils__WEBPACK_IMPORTED_MODULE_1__._skipEscapeSequence)(tempString);\n                        }\n                    }\n                    if (!isWidth && input.length > 0) {\n                        key = tempString + 's';\n                        decodedList.push(key);\n                        key = '';\n                        if (isForRedaction && !isHex) {\n                            inputType.push(' ');\n                        }\n                        isHex = false;\n                    }\n                }\n                break;\n            case '<':\n                encodedText = encodedText.substring(1, encodedText.length - 1);\n                tempString = (0,_utils__WEBPACK_IMPORTED_MODULE_1__._hexToChar)(encodedText);\n                key = tempString + 's';\n                decodedList.push(key);\n                break;\n        }\n        return isForRedaction ? { decodedList: decodedList, inputType: inputType } : { decodedList: decodedList };\n    };\n    /* eslint-enable */\n    /**\n     * Compute page-rotation from current text matrix.\n     *\n     * @private\n     * @param {_TextState} textState - current text state\n     * @returns {number} rotation in degrees (0, 90, 270)\n     */\n    _PdfTextParser.prototype._getPageRotation = function (textState) {\n        var rotation = 0;\n        if (textState._textMatrix[0] === 0 && textState._textMatrix[1] > 0 && textState._textMatrix[2] < 0 &&\n            textState._textMatrix[3] >= 0) {\n            rotation = 90;\n        }\n        else if (textState._textMatrix[0] === 0 && textState._textMatrix[1] < 0 && textState._textMatrix[2] > 0\n            && textState._textMatrix[3] === 0) {\n            rotation = 270;\n        }\n        return rotation;\n    };\n    /**\n     * Split a hex string into chunks for glyph decoding.\n     *\n     * @private\n     * @param {string} hexString - hex literal string (e.g. <....>)\n     * @returns {string[]} array of hex chunks\n     */\n    _PdfTextParser.prototype._splitHexString = function (hexString) {\n        var hexList = [];\n        if (typeof (hexString) === 'undefined') {\n            return hexList;\n        }\n        hexString = hexString.slice(1, -1);\n        var size = hexString.startsWith('0') ? 4 : 2;\n        for (var i = 0; i < hexString.length; i += size) {\n            var chunk = hexString.substring(i, i + size);\n            if (chunk.indexOf('\\n') !== -1) {\n                var extraChar = hexString.charAt(i + size);\n                chunk += extraChar;\n                i++;\n            }\n            hexList.push(chunk);\n        }\n        return hexList;\n    };\n    /**\n     * Create a fallback font dictionary used when font lookup fails.\n     *\n     * @private\n     * @param {_PdfCrossReference} crossReference - cross-reference table\n     * @returns {_PdfDictionary} fallback font dictionary\n     */\n    _PdfTextParser.prototype._getFallBackFontDictionary = function (crossReference) {\n        var dictionary = new _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__._PdfDictionary(crossReference);\n        dictionary.set('BaseFont', new _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__._PdfName('Helvetica'));\n        dictionary.set('Type', new _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__._PdfName('FallbackType'));\n        dictionary.set('Subtype', new _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__._PdfName('FallbackType'));\n        dictionary.set('Encoding', new _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__._PdfName('WinAnsiEncoding'));\n        return dictionary;\n    };\n    /**\n     * Process basic graphics/text commands affecting graphic state.\n     *\n     * @private\n     * @param {string} token - command token (e.g. 'q', 'Q', 'cm')\n     * @param {string[]} element - operand array\n     * @param {_GraphicState} state - current graphic state\n     * @returns {void} nothing\n     */\n    _PdfTextParser.prototype._processCommand = function (token, element, state) {\n        var args = [];\n        switch (token) {\n            case 'q':\n                state._save();\n                break;\n            case 'Q':\n                state._restore();\n                break;\n            case 'cm':\n                args = [parseFloat(element[0]), parseFloat(element[1]), parseFloat(element[2]), parseFloat(element[3]), parseFloat(element[4]), parseFloat(element[5])];\n                state._transform(args);\n        }\n    };\n    /**\n     * Resolve the current font structure from a font collection or fallback.\n     *\n     * @private\n     * @param {Map<string,_FontStructure>} fontCollection - page font resources\n     * @param {_TextState} textState - current text state\n     * @param {_PdfCrossReference} crossReference - cross-reference table\n     * @returns {_FontStructure} resolved font structure\n     */\n    _PdfTextParser.prototype._getTextFont = function (fontCollection, textState, crossReference) {\n        var currentFont;\n        if (fontCollection.get(textState._font)) {\n            currentFont = fontCollection.get(textState._font);\n        }\n        else {\n            var dictionary = this._getFallBackFontDictionary(crossReference);\n            currentFont = new _text_extraction_font_structure__WEBPACK_IMPORTED_MODULE_2__._FontStructure(dictionary, crossReference);\n        }\n        return currentFont;\n    };\n    /* eslint-disable */\n    /**\n     * Build text content item(s) for a text token (glyphs, bounds, encoded text).\n     *\n     * @private\n     * @param {_FontStructure} currentFont - resolved font\n     * @param {string} text - input text token\n     * @param {number} extraSpacing - extra spacing to apply\n     * @param {_TextState} textState - current text state\n     * @param {PdfPage} page - page reference\n     * @param {string} tempString - temporary accumulator string\n     * @param {{x:number,y:number,width:number,height:number}} previousRect - previous bounding rect\n     * @param {string} extractedText - accumulated extracted text\n     * @param {_PdfContentParserHelper} [parser] - optional content parser helper\n     * @param {TextGlyph[]} [textGlyphs] - optional output glyph array\n     * @param {string[]} [hex] - optional hex chunks array\n     * @param {number} [index] - optional start index for encodedText\n     * @param {string[]} [encodedText] - optional encoded text output\n     * @returns {any} glyph/text data or parser results depending on call (object)\n     */\n    _PdfTextParser.prototype._getTextContentItem = function (currentFont, text, extraSpacing, textState, page, tempString, previousRect, extractedText, parser, textGlyphs, hex, index, encodedText) {\n        text = text.slice(0, -1);\n        var fontSize = 0;\n        var isHex = false;\n        var textBounds = { x: 0, y: 0, width: 0, height: 0 };\n        if (typeof (hex) !== 'undefined' && hex.length > 0) {\n            isHex = true;\n        }\n        var glyphs = currentFont._charsToGlyphs(text);\n        var ii = glyphs.length;\n        var scale = 0;\n        if (currentFont._fontMatrix) {\n            scale = currentFont._fontMatrix[0];\n        }\n        else {\n            currentFont._fontMatrix = [0.001, 0, 0, 0.001, 0, 0];\n            scale = 0.001;\n        }\n        var rotation = this._getPageRotation(textState);\n        for (var i = 0; i < ii; i++) {\n            var glyph = glyphs[Number.parseInt(i.toString(), 10)];\n            // this._resultantText += glyph._unicode;\n            var width = void 0;\n            var charSpacing = textState._charSpacing + (i + 1 === ii ? extraSpacing : 0);\n            var glyphWidth = glyph._width;\n            if (currentFont._vertical) {\n                glyphWidth = glyph.vmetric ? glyph.vmetric[0] : -glyphWidth;\n            }\n            var scaledDim = glyphWidth * scale * textState._fontSize;\n            width = scale * glyphWidth;\n            var height = 0;\n            var tempFontSize = 0;\n            var transform = this._getCurrentTransform(currentFont, currentFont._fontMatrix, textState);\n            var cropOrMediaBox = this._getCropOrMediaBox(page);\n            var x = transform[4];\n            var y = 0;\n            if (page.rotation === _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__.PdfRotationAngle.angle180 || page.rotation === _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__.PdfRotationAngle.angle270) {\n                x = cropOrMediaBox[1] - x;\n            }\n            else {\n                x = x - cropOrMediaBox[0];\n            }\n            if (page.rotation === _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__.PdfRotationAngle.angle90 || page.rotation === _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__.PdfRotationAngle.angle180) {\n                y = transform[5];\n            }\n            else {\n                y = cropOrMediaBox[2] - transform[5];\n            }\n            if (transform[0] > 0 && !currentFont._dictionary.has('DescendantFonts')) {\n                tempFontSize = transform[0];\n            }\n            else if (transform[3] > 0 && currentFont._dictionary.has('DescendantFonts')) {\n                tempFontSize = transform[3];\n            }\n            else if (transform[1] !== 0 && transform[2] !== 0) {\n                if (transform[1] < 0) {\n                    tempFontSize = -transform[1];\n                }\n                else {\n                    tempFontSize = transform[1];\n                }\n            }\n            if (!currentFont._vertical) {\n                height = transform[0] > 0 && transform[0] > 1 ? tempFontSize : Math.hypot(transform[2], transform[3]);\n            }\n            else {\n                width = Math.hypot(transform[0], transform[1]);\n            }\n            if (glyph._unicode === ' ') {\n                if (!currentFont._vertical) {\n                    charSpacing += scaledDim + textState._wordSpacing;\n                    textState._translateTextMatrix(charSpacing * (textState._textHScale), 0);\n                    width *= tempFontSize;\n                    if (rotation === 90) {\n                        y = y - width;\n                        x = x - height + height / 4;\n                    }\n                    else if (page.rotation === _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__.PdfRotationAngle.angle90 || page.rotation === _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__.PdfRotationAngle.angle180) {\n                        y = y - height + (height / 1.3333333333333333);\n                    }\n                    else {\n                        y = y - height / 1.3333333333333333;\n                    }\n                }\n                else {\n                    charSpacing += -scaledDim + textState._wordSpacing;\n                    textState._translateTextMatrix(0, -charSpacing);\n                    height += tempFontSize;\n                    x = x - width / 2;\n                }\n            }\n            if (glyph._unicode !== ' ') {\n                if (!currentFont._vertical) {\n                    scaledDim *= textState._textHScale;\n                    textState._translateTextMatrix(scaledDim, 0);\n                    width *= tempFontSize;\n                    if (rotation === 90) {\n                        y = y - width;\n                        x = x - height + height / 4;\n                    }\n                    else if (page.rotation === _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__.PdfRotationAngle.angle90 || page.rotation === _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__.PdfRotationAngle.angle180) {\n                        y = y - height + (height / 1.3333333333333333);\n                    }\n                    else {\n                        y = y - (height / 1.3333333333333333);\n                    }\n                }\n                else {\n                    textState._translateTextMatrix(0, scaledDim);\n                    scaledDim = Math.abs(scaledDim);\n                    height += scaledDim + tempFontSize;\n                    x = x - width / 2;\n                }\n            }\n            if (page.rotation === _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__.PdfRotationAngle.angle0 && rotation === 90) {\n                textBounds = { x: x, y: y, width: height, height: width };\n            }\n            else if (page.rotation === _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__.PdfRotationAngle.angle90) {\n                textBounds = { x: y, y: x, width: height, height: width };\n            }\n            else if (page.rotation === _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__.PdfRotationAngle.angle180) {\n                textBounds = { x: x - width, y: y, width: width, height: height };\n            }\n            else if (page.rotation === _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__.PdfRotationAngle.angle270) {\n                textBounds = { x: y, y: x - width, width: height, height: width };\n            }\n            else {\n                textBounds = { x: x, y: y, width: width, height: height };\n            }\n            extractedText += glyph._unicode;\n            if (typeof (textGlyphs) !== 'undefined') {\n                var bounds = textBounds;\n                var textGlyph = new _text_structure__WEBPACK_IMPORTED_MODULE_3__.TextGlyph();\n                if (isHex && hex.length >= i) {\n                    textGlyph._isHex = true;\n                    encodedText[Number.parseInt(index.toString(), 10)] = hex[Number.parseInt(i.toString(), 10)];\n                }\n                else {\n                    encodedText[Number.parseInt(index.toString(), 10)] = glyph._fontCharacter;\n                }\n                textGlyph._text = glyph._unicode;\n                textGlyph._bounds = bounds;\n                textGlyph._fontName = currentFont._name;\n                textGlyph._fontStyle = currentFont._fontStyle;\n                textGlyph._fontSize = textState._fontSize;\n                textGlyph._color = textState._textColor;\n                textGlyph._width = glyph._width;\n                textGlyph._charSpacing = textState._charSpacing;\n                textGlyph._wordSpacing = textState._wordSpacing;\n                index++;\n                if (page.rotation !== _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__.PdfRotationAngle.angle0) {\n                    textGlyph._isRotated = true;\n                }\n                else {\n                    textGlyph._isRotated = false;\n                }\n                textGlyphs.push(textGlyph);\n            }\n            else {\n                fontSize = textState._fontSize;\n                var result = parser._splitWords(glyph._unicode, tempString, currentFont._name, currentFont._fontStyle, page, rotation, textState._textColor, textState._fontSize, textBounds, previousRect);\n                previousRect = result.previousRect;\n                tempString = result.tempString;\n                if (previousRect) {\n                    previousRect = { x: textBounds.x, y: textBounds.y,\n                        width: textBounds.width, height: textBounds.height };\n                }\n                else {\n                    previousRect = { x: 0, y: 0, width: 0, height: 0 };\n                }\n            }\n            if (charSpacing && glyph._unicode !== ' ') {\n                if (!currentFont._vertical) {\n                    textState._translateTextMatrix(charSpacing * textState._textHScale, 0);\n                }\n                else {\n                    textState._translateTextMatrix(0, -charSpacing);\n                }\n            }\n        }\n        if (typeof (textGlyphs) !== 'undefined') {\n            return { textGlyphs: textGlyphs, extractedText: extractedText, encodedText: encodedText, index: index };\n        }\n        else {\n            return { tempString: tempString, extractedText: extractedText, fontSize: fontSize, previousRect: previousRect };\n        }\n    };\n    return _PdfTextParser;\n}());\n\n\n\n//# sourceURL=webpack://@syncfusion/ej2-documents-vue-samples/./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/pdf-text-parser.js?");

/***/ })

}]);