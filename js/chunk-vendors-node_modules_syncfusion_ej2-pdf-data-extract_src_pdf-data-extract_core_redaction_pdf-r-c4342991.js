"use strict";
/*
 * ATTENTION: The "eval" devtool has been used (maybe by default in mode: "development").
 * This devtool is neither made for production nor for readable output files.
 * It uses "eval()" calls to create a separate source file in the browser devtools.
 * If you are trying to read the output file, select a different devtool (https://webpack.js.org/configuration/devtool/)
 * or disable the default devtool with "devtool: false".
 * If you are looking for production-ready output files, see mode: "production" (https://webpack.js.org/configuration/mode/).
 */
(self["webpackChunk_syncfusion_ej2_documents_vue_samples"] = self["webpackChunk_syncfusion_ej2_documents_vue_samples"] || []).push([["chunk-vendors-node_modules_syncfusion_ej2-pdf-data-extract_src_pdf-data-extract_core_redaction_pdf-r-c4342991"],{

/***/ "./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/redaction/pdf-redactor.js":
/*!***********************************************************************************************************!*\
  !*** ./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/redaction/pdf-redactor.js ***!
  \***********************************************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   PdfRedactor: () => (/* binding */ PdfRedactor)\n/* harmony export */ });\n/* harmony import */ var _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @syncfusion/ej2-pdf */ \"./node_modules/@syncfusion/ej2-pdf/index.js\");\n/* harmony import */ var _graphic_state__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../graphic-state */ \"./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/graphic-state.js\");\n/* harmony import */ var _content_parser_helper__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../content-parser-helper */ \"./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/content-parser-helper.js\");\n/* harmony import */ var _enum__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../enum */ \"./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/enum.js\");\n/* harmony import */ var _utils__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../utils */ \"./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/utils.js\");\n/* harmony import */ var _text_glyph_mapper__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./text-glyph-mapper */ \"./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/redaction/text-glyph-mapper.js\");\n/* harmony import */ var _pdf_redaction_processor__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ./pdf-redaction-processor */ \"./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/redaction/pdf-redaction-processor.js\");\n/* harmony import */ var _pdf_redaction_region__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ./pdf-redaction-region */ \"./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/redaction/pdf-redaction-region.js\");\nvar __awaiter = (undefined && undefined.__awaiter) || function (thisArg, _arguments, P, generator) {\n    return new (P || (P = Promise))(function (resolve, reject) {\n        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }\n        function rejected(value) { try { step(generator[\"throw\"](value)); } catch (e) { reject(e); } }\n        function step(result) { result.done ? resolve(result.value) : new P(function (resolve) { resolve(result.value); }).then(fulfilled, rejected); }\n        step((generator = generator.apply(thisArg, _arguments || [])).next());\n    });\n};\nvar __generator = (undefined && undefined.__generator) || function (thisArg, body) {\n    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g;\n    return g = { next: verb(0), \"throw\": verb(1), \"return\": verb(2) }, typeof Symbol === \"function\" && (g[Symbol.iterator] = function() { return this; }), g;\n    function verb(n) { return function (v) { return step([n, v]); }; }\n    function step(op) {\n        if (f) throw new TypeError(\"Generator is already executing.\");\n        while (_) try {\n            if (f = 1, y && (t = op[0] & 2 ? y[\"return\"] : op[0] ? y[\"throw\"] || ((t = y[\"return\"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;\n            if (y = 0, t) op = [op[0] & 2, t.value];\n            switch (op[0]) {\n                case 0: case 1: t = op; break;\n                case 4: _.label++; return { value: op[1], done: false };\n                case 5: _.label++; y = op[1]; op = [0]; continue;\n                case 7: op = _.ops.pop(); _.trys.pop(); continue;\n                default:\n                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }\n                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }\n                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }\n                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }\n                    if (t[2]) _.ops.pop();\n                    _.trys.pop(); continue;\n            }\n            op = body.call(thisArg, _);\n        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }\n        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };\n    }\n};\n\n\n\n\n\n\n\n\n/**\n * Represents a content redactor from an existing PDF document.\n *\n * ```typescript\n * let document: PdfDocument = new PdfDocument(data, password);\n * // Create a new text extractor\n * let redactor: PdfRedactor = new PdfRedactor(document);\n * // Add redactions to the collection\n * let redactions: PdfRedactionRegion[] = [];\n * redactions.push(new PdfRedactionRegion(0, {x: 10, y: 10, width: 100, height: 50}));\n * redactions.push(new PdfRedactionRegion(2, {x: 10, y: 10, width: 100, height: 50}, true, {r: 255, g: 0, b: 0}));\n * redactor.add(redactions);\n * // Apply redactions on the PDF document\n * redactor.redactSync();\n * // Save the document\n * document.save('output.pdf');\n * // Destroy the document\n * document.destroy();\n * ```\n */\nvar PdfRedactor = /** @class */ (function () {\n    function PdfRedactor(document, redactions) {\n        /**\n         * Internal flag indicating hex-string context during text replacement.\n         *\n         * @private\n         */\n        this._isHex = false;\n        /**\n         * Accumulated redaction regions for the current page batch.\n         *\n         * @private\n         */\n        this._redactionRegion = [];\n        /**\n         * Page-indexed map of pending redaction regions.\n         *\n         * @private\n         */\n        this._redaction = new Map();\n        /**\n         * Internal low-level processor to update page contents / annotations.\n         *\n         * @private\n         */\n        this._object = new _pdf_redaction_processor__WEBPACK_IMPORTED_MODULE_6__._PdfRedactionProcessor();\n        if (document) {\n            if (document._isLoaded) {\n                this._document = document;\n                this._crossReference = document._crossReference;\n            }\n            else {\n                throw new Error('Redaction cannot be applied to a newly created document.');\n            }\n            this._document.fileStructure.isIncrementalUpdate = false;\n            this._parser = new _content_parser_helper__WEBPACK_IMPORTED_MODULE_2__._PdfContentParserHelper(_enum__WEBPACK_IMPORTED_MODULE_3__._TextProcessingMode.redaction, this);\n            if (redactions && redactions.length > 0) {\n                this.add(redactions);\n            }\n        }\n        else {\n            throw new Error('PDF document instance cannot be null or undefined');\n        }\n    }\n    /**\n     * Add redactions with specified options.\n     *\n     * @param {PdfRedactionRegion[]} redactions An array of redaction objects to specify the page index, bounds and appearance of the redaction to be applied.\n     * @returns {void} Nothing.\n     *\n     * ```typescript\n     * // Load an existing PDF document\n     * let document: PdfDocument = new PdfDocument(data, password);\n     * // Add redactions to the collection\n     * let redactions: PdfRedactionRegion[] = [];\n     * // Initialize a new instance of the `PdfRedactor` class\n     * let redactor: PdfRedactor = new PdfRedactor(document);\n     * // Initialize a new instance of the `PdfRedactionRegion` class.\n     * let redaction: PdfRedactionRegion = new PdfRedactionRegion(0, {x: 40, y: 41.809, width: 80, height: 90});\n     * // Sets the fill color used to fill the redacted area.\n     * redaction.fillColor = {r: 255, g: 0, b: 0};\n     * redactions.push(redaction);\n     * // Add redactions with specified options.\n     * redactor.add(redactions);\n     * // Apply redactions on the PDF document\n     * redactor.redactSync();\n     * // Save the document\n     * document.save('output.pdf');\n     * // Destroy the document\n     * document.destroy();\n     * ```\n     */\n    PdfRedactor.prototype.add = function (redactions) {\n        var options = redactions;\n        for (var i = 0; i < options.length; i++) {\n            var pageIndex = options[Number.parseInt(i.toString(), 10)].pageIndex;\n            if (!this._redaction.has(pageIndex)) {\n                this._redaction.set(pageIndex, []);\n            }\n            var redactionArray = this._redaction.get(pageIndex);\n            if (redactionArray) {\n                redactionArray.push(options[Number.parseInt(i.toString(), 10)]);\n            }\n        }\n    };\n    /**\n     * Redacts all marked regions in the PDF document synchronously.\n     *\n     * This method removes text and vector content within the specified redaction regions. It does **not** support image redaction.To redact images inside the PDF, use the asynchronous `redact()` method instead.\n     *\n     * @returns {void} Nothing.\n     *\n     * ```typescript\n     * // Load an existing PDF document\n     * let document: PdfDocument = new PdfDocument(data, password);\n     * // Add redactions to the collection\n     * let redactions: PdfRedactionRegion[] = [];\n     * // Initialize a new instance of the `PdfRedactor` class\n     * let redactor: PdfRedactor = new PdfRedactor(document);\n     * // Initialize a new instance of the `PdfRedactionRegion` class.\n     * let redaction: PdfRedactionRegion = new PdfRedactionRegion(0, {x: 40, y: 41.809, width: 80, height: 90});\n     * // Sets the fill color used to fill the redacted area.\n     * redaction.fillColor = {r: 255, g: 0, b: 0};\n     * redactions.push(redaction);\n     * // Add redactions with specified options.\n     * redactor.add(redactions);\n     * // Apply redactions on the PDF document\n     * redactor.redactSync();\n     * // Save the document\n     * document.save('output.pdf');\n     * // Destroy the document\n     * document.destroy();\n     * ```\n     */\n    PdfRedactor.prototype.redactSync = function () {\n        var _this = this;\n        if (this._document && this._document.pageCount > 0) {\n            for (var i = 0; i < this._document.pageCount; i++) {\n                var page = this._document.getPage(i);\n                if (page && page.annotations.count > 0) {\n                    this._applyRedaction(page);\n                }\n            }\n        }\n        this._redaction.forEach(function (value, key) {\n            _this._redactionRegion = [];\n            _this._combineBounds(value);\n            var option = value;\n            var page = _this._document.getPage(key);\n            var graphicState = new _graphic_state__WEBPACK_IMPORTED_MODULE_1__._GraphicState();\n            var recordCollection = _this._parser._getPageRecordCollection(page);\n            var resource = page._pageDictionary.get('Resources');\n            var fontCollection;\n            var xObjectCollection;\n            if (typeof (resource) !== 'undefined') {\n                fontCollection = (0,_utils__WEBPACK_IMPORTED_MODULE_4__._addFontResources)(resource, page._pageDictionary._crossReference);\n                xObjectCollection = (0,_utils__WEBPACK_IMPORTED_MODULE_4__._getXObjectResources)(resource, page._pageDictionary._crossReference);\n            }\n            var stream; // eslint-disable-line\n            if (_this._redactionRegion.length > 0) {\n                stream = _this._parser._processRecordCollection(recordCollection, page, fontCollection, xObjectCollection, graphicState);\n            }\n            page._needInitializeGraphics = true;\n            _this._object._updateContentStream(page, stream, option, _this._document);\n        });\n    };\n    /**\n     * Redacts all marked regions in the PDF document.\n     *\n     * @param {Function} callBack - A function that supplies a canvas element along with the current application platform.The canvas is used internally to render and redact image content.\n     * @returns {void} A promise that resolves once all redactions have been applied.\n     *\n     * ```typescript\n     * // Load an existing PDF document\n     * let doc: PdfDocument = new PdfDocument(data, password);\n     * // Add redactions to the collection\n     * let redactions: PdfRedactionRegion[] = [];\n     * // Initialize a new instance of the `PdfRedactor` class\n     * let redactor: PdfRedactor = new PdfRedactor(doc);\n     * // Initialize a new instance of the `PdfRedactionRegion` class.\n     * let redaction: PdfRedactionRegion = new PdfRedactionRegion(0, {x: 40, y: 41.809, width: 80, height: 90});\n     * // Sets the fill color used to fill the redacted area.\n     * redaction.fillColor = {r: 255, g: 0, b: 0};\n     * redactions.push(redaction);\n     * // Add redactions with specified options.\n     * redactor.add(redactions);\n     * // Define a canvas render callback that returns a canvas element and the application platform.\n     * const canvasRenderCallback = (): {canvas: any, applicationPlatform: ApplicationPlatform} => {\n     *     const canvas = document.createElement('canvas');\n     *     return { canvas: canvas, applicationPlatform: ApplicationPlatform.typescript };\n     * };\n     * // Apply redactions on the PDF document\n     * await redactor.redact(callBack: canvasRenderCallback);\n     * // Save the document\n     * doc.save('output.pdf');\n     * // Destroy the document\n     * doc.destroy();\n     * ```\n     */\n    PdfRedactor.prototype.redact = function (callBack) {\n        return __awaiter(this, void 0, void 0, function () {\n            var i, page, canvas, isValidCanvas, entries, i, _a, key, value, option, page, graphicState, recordCollection, resource, fontCollection, xObjectCollection, stream, mode;\n            return __generator(this, function (_b) {\n                switch (_b.label) {\n                    case 0:\n                        if (this._document && this._document.pageCount > 0) {\n                            this._document.fileStructure.isIncrementalUpdate = false;\n                            for (i = 0; i < this._document.pageCount; i++) {\n                                page = this._document.getPage(i);\n                                if (page && page.annotations.count > 0) {\n                                    this._applyRedaction(page);\n                                }\n                            }\n                        }\n                        isValidCanvas = false;\n                        if (typeof (callBack) !== 'undefined') {\n                            canvas = callBack();\n                            if (typeof (canvas) !== 'undefined' && typeof (canvas.canvas) !== 'undefined') {\n                                isValidCanvas = true;\n                            }\n                        }\n                        entries = Array.from(this._redaction.entries());\n                        i = 0;\n                        _b.label = 1;\n                    case 1:\n                        if (!(i < entries.length)) return [3 /*break*/, 5];\n                        _a = entries[i], key = _a[0], value = _a[1];\n                        this._redactionRegion = [];\n                        this._combineBounds(value);\n                        option = value;\n                        page = this._document.getPage(key);\n                        this._document._crossReference._isDecoderSupport = true;\n                        graphicState = new _graphic_state__WEBPACK_IMPORTED_MODULE_1__._GraphicState();\n                        recordCollection = this._parser._getPageRecordCollection(page);\n                        resource = page._pageDictionary.get('Resources');\n                        fontCollection = void 0;\n                        xObjectCollection = void 0;\n                        if (typeof (resource) !== 'undefined') {\n                            fontCollection = (0,_utils__WEBPACK_IMPORTED_MODULE_4__._addFontResources)(resource, page._pageDictionary._crossReference);\n                            xObjectCollection = (0,_utils__WEBPACK_IMPORTED_MODULE_4__._getXObjectResources)(resource, this._crossReference, _enum__WEBPACK_IMPORTED_MODULE_3__._TextProcessingMode.imageRedaction, page);\n                        }\n                        stream = void 0;\n                        mode = void 0;\n                        if (!(this._redactionRegion.length > 0)) return [3 /*break*/, 3];\n                        mode = _enum__WEBPACK_IMPORTED_MODULE_3__._TextProcessingMode.imageRedaction;\n                        return [4 /*yield*/, this._parser._processImageRecordCollection(recordCollection, page, fontCollection, xObjectCollection, graphicState, canvas, mode, option, isValidCanvas)];\n                    case 2:\n                        stream = _b.sent();\n                        _b.label = 3;\n                    case 3:\n                        page._needInitializeGraphics = true;\n                        this._object._updateContentStream(page, stream, option, this._document);\n                        _b.label = 4;\n                    case 4:\n                        i++;\n                        return [3 /*break*/, 1];\n                    case 5: return [2 /*return*/];\n                }\n            });\n        });\n    };\n    /**\n     * Converts any found redaction annotations into internal redaction regions,\n     * creating their appearance streams and removing the original annotations.\n     *\n     * @private\n     * @param {PdfPage} page The page whose annotations are analyzed.\n     * @returns {void} nothing.\n     */\n    PdfRedactor.prototype._applyRedaction = function (page) {\n        var _this = this;\n        var redactRegions = [];\n        var _loop_1 = function (k) {\n            var annotation = page.annotations.at(k);\n            if (annotation && annotation instanceof _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__.PdfRedactionAnnotation) {\n                var redactionAnnotation_1 = annotation;\n                redactionAnnotation_1.flatten = true;\n                var rotation_1 = page.rotation;\n                if (redactionAnnotation_1.boundsCollection && redactionAnnotation_1.boundsCollection.length > 1) {\n                    redactionAnnotation_1.boundsCollection.forEach(function (rawRect, index) {\n                        var normRect = _this._calculateRotatedBounds(page, rawRect);\n                        var appearance = redactionAnnotation_1._createNormalAppearance(index, normRect, rotation_1);\n                        var redact = new _pdf_redaction_region__WEBPACK_IMPORTED_MODULE_7__.PdfRedactionRegion(page._pageIndex, normRect);\n                        redact.appearance.normal = appearance;\n                        redactRegions.push(redact);\n                    });\n                }\n                else {\n                    var normRect = this_1._calculateRotatedBounds(page, redactionAnnotation_1.bounds);\n                    var appearance = redactionAnnotation_1._createNormalAppearance(undefined, normRect, rotation_1);\n                    var redact = new _pdf_redaction_region__WEBPACK_IMPORTED_MODULE_7__.PdfRedactionRegion(page._pageIndex, normRect);\n                    redact.appearance.normal = appearance;\n                    redactRegions.push(redact);\n                }\n                page.annotations.removeAt(k);\n                k--;\n            }\n            out_k_1 = k;\n        };\n        var this_1 = this, out_k_1;\n        for (var k = 0; k < page.annotations.count; k++) {\n            _loop_1(k);\n            k = out_k_1;\n        }\n        if (redactRegions.length > 0) {\n            var existingRedactions = this._redaction.get(page._pageIndex) || [];\n            this._redaction.set(page._pageIndex, existingRedactions.concat(redactRegions));\n        }\n    };\n    /**\n     * Computes page-space bounds for a rectangle after accounting for page rotation.\n     *\n     * @private\n     * @param {PdfPage} page The page containing the bounds.\n     * @param {Rectangle} bounds Unrotated rectangle.\n     * @returns {Rectangle} Rotated-normalized rectangle.\n     */\n    PdfRedactor.prototype._calculateRotatedBounds = function (page, bounds) {\n        var rotation = page.rotation;\n        var pageWidth = page.size.width;\n        var pageHeight = page.size.height;\n        switch (rotation) {\n            case _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__.PdfRotationAngle.angle0:\n                return bounds;\n            case _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__.PdfRotationAngle.angle90:\n                return {\n                    x: pageHeight - (bounds.y + bounds.height),\n                    y: bounds.x,\n                    width: bounds.height,\n                    height: bounds.width\n                };\n            case _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__.PdfRotationAngle.angle180:\n                return {\n                    x: pageWidth - (bounds.x + bounds.width),\n                    y: pageHeight - (bounds.y + bounds.height),\n                    width: bounds.width,\n                    height: bounds.height\n                };\n            case _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__.PdfRotationAngle.angle270:\n                return {\n                    x: bounds.y,\n                    y: pageWidth - bounds.x - bounds.width,\n                    width: bounds.height,\n                    height: bounds.width\n                };\n            default:\n                return bounds;\n        }\n    };\n    /**\n     * Appends the given list of redaction regions into the class-wide accumulator.\n     *\n     * @private\n     * @param {PdfRedactionRegion[]} options Redaction regions for a page.\n     * @returns {void} nothing.\n     */\n    PdfRedactor.prototype._combineBounds = function (options) {\n        for (var i = 0; i < options.length; i++) {\n            this._redactionRegion.push(options[Number.parseInt(i.toString(), 10)]);\n        }\n    };\n    /**\n     * Writes a record back to a content stream, optionally replacing text showing operands.\n     *\n     * @private\n     * @param {_PdfRecord[]} recordCollection The record collection for the page content.\n     * @param {number} index Current record index.\n     * @param {string} updatedText Updated text.\n     * @param {_PdfContentStream} stream The destination stream to write to.\n     * @returns {void} nothing.\n     */\n    PdfRedactor.prototype._optimizeContent = function (recordCollection, index, updatedText, stream) {\n        var record = recordCollection[Number.parseInt(index.toString(), 10)];\n        if (record) {\n            if (typeof (record._operands) !== 'undefined' && record._operands.length >= 1) {\n                if (record._operator === 'ID') {\n                    var builder = [];\n                    for (var k = 0; k < record._operands.length; k++) {\n                        if (k + 1 < record._operands.length && record._operands[k].indexOf(\"/\") !== -1 && record._operands[k + 1].indexOf(\"/\") !== -1) { // eslint-disable-line\n                            builder.push(record._operands[Number.parseInt(k.toString(), 10)], ' ', record._operands[k + 1], '\\r\\n');\n                            k = k + 1;\n                        }\n                        else if (k + 1 < record._operands.length && record._operands[k].indexOf(\"/\") !== -1) { // eslint-disable-line\n                            builder.push(record._operands[Number.parseInt(k.toString(), 10)], ' ', record._operands[k + 1], '\\r\\n');\n                            k = k + 1;\n                        }\n                        else {\n                            builder.push(record._operands[Number.parseInt(k.toString(), 10)], ' ');\n                        }\n                    }\n                    var text = builder.join(\"\"); // eslint-disable-line\n                    var bytes = this._getBytes(text);\n                    stream.write(bytes);\n                }\n                else {\n                    for (var i = 0; i < record._operands.length; i++) {\n                        var operand = record._operands[Number.parseInt(i.toString(), 10)];\n                        if (record._operator === 'Tj' || record._operator === \"'\" || record._operator === '\\\"' || record._operator === 'TJ') { // eslint-disable-line\n                            if (updatedText !== '') {\n                                operand = updatedText;\n                                if (record._operator === \"'\" || record._operator === '\\\"') { // eslint-disable-line\n                                    stream.write('T*');\n                                    stream.write(' ');\n                                    if (record._operator === '\\\"') { // eslint-disable-line\n                                        i += 2;\n                                    }\n                                }\n                                record._operator = 'TJ';\n                            }\n                        }\n                        var bytes = this._getBytes(operand);\n                        stream.write(bytes);\n                        if (record._operator !== 'Tj' && record._operator !== \"'\" && record._operator !== '\\\"' && record._operator !== 'TJ') { // eslint-disable-line\n                            stream.write(' ');\n                        }\n                    }\n                }\n            }\n            else if (typeof (record._operands) === 'undefined' && typeof (record._inlineImageBytes) !== 'undefined') {\n                var numberArray = Array.from(record._inlineImageBytes);\n                stream.write(numberArray);\n                stream.write(' ');\n            }\n            stream.write(record._operator);\n        }\n        var count = recordCollection.length;\n        if ((index) < count) {\n            if (record._operator === 'ID') {\n                stream.write('\\n');\n            }\n            else if ((record._operator === 'W' || record._operator === 'W*') && recordCollection[index + 1]._operator === 'n') {\n                stream.write(' ');\n            }\n            else if (record._operator === 'w' || record._operator === 'EI') {\n                stream.write(' ');\n            }\n            else {\n                stream.write('\\r\\n');\n            }\n        }\n    };\n    /**\n     * Encodes a JavaScript string into a byte array (UTF-16 code units cast down).\n     *\n     * @private\n     * @param {string} text Input text.\n     * @returns {number[]} Byte array of char codes.\n     */\n    PdfRedactor.prototype._getBytes = function (text) {\n        var bytes = [];\n        for (var i = 0; i < text.length; i++) {\n            var charCode = text.charCodeAt(i);\n            bytes.push(charCode);\n        }\n        return bytes;\n    };\n    /**\n     * Returns whether the given rectangle intersects or lies within any of the provided redaction regions.\n     *\n     * @private\n     * @param {Rectangle} values Candidate rectangle.\n     * @param {PdfRedactionRegion[]} redactionBounds Redaction regions list.\n     * @returns {boolean} `true` if found; otherwise, `false`.\n     */\n    PdfRedactor.prototype._isFoundBounds = function (values, redactionBounds) {\n        for (var _i = 0, redactionBounds_1 = redactionBounds; _i < redactionBounds_1.length; _i++) {\n            var bounds = redactionBounds_1[_i];\n            if (this._contains(bounds._bounds, [values.x, values.y]) || this._intersectsWith(bounds._bounds, values)) {\n                return true;\n            }\n        }\n        return false;\n    };\n    /* eslint-disable */\n    /**\n     * Checks if a point lies within an axis-aligned rectangle (inclusive edges).\n     *\n     * @private\n     * @param {{x: number, y: number, width: number, height: number}} bounds Rectangle bounds.\n     * @param {number[]} point Point as `[x, y]`.\n     * @returns {boolean} `true` if the point is inside; otherwise, `false`.\n     */\n    PdfRedactor.prototype._contains = function (bounds, point) {\n        return (point[0] >= bounds.x &&\n            point[0] <= bounds.x + bounds.width &&\n            point[1] >= bounds.y &&\n            point[1] <= bounds.y + bounds.height);\n    };\n    /**\n     * Tests two rectangles for intersection.\n     *\n     * @private\n     * @param {{x: number, y: number, width: number, height: number}} rect1 First rectangle.\n     * @param {{x: number, y: number, width: number, height: number}} rect2 Second rectangle.\n     * @returns {boolean} `true` if they intersect; otherwise, `false`.\n     */\n    PdfRedactor.prototype._intersectsWith = function (rect1, rect2) {\n        return (rect2.x < rect1.x + rect1.width) && (rect1.x < (rect2.x + rect2.width)) && (rect2.y < rect1.y + rect1.height) &&\n            (rect1.y < rect2.y + rect2.height);\n    };\n    /* eslint-enable */\n    /**\n     * Splits a hex string literal into chunks, accounting for line-break artifacts.\n     *\n     * @private\n     * @param {string} hexString Hex string literal.\n     * @returns {string[]} Array of hex chunks without wrapper.\n     */\n    PdfRedactor.prototype._splitHexString = function (hexString) {\n        var hexList = [];\n        hexString = hexString.slice(1, -1);\n        var size = hexString.startsWith('0') ? 4 : 2;\n        for (var i = 0; i < hexString.length; i += size) {\n            var chunk = hexString.substring(i, i + size);\n            if (chunk.indexOf('\\n') !== -1) {\n                var extraChar = hexString.charAt(i + size);\n                chunk += extraChar;\n                i++;\n            }\n            hexList.push(chunk);\n        }\n        return hexList;\n    };\n    /**\n     * Builds replacement text for glyphs intersecting redaction regions and returns a new TJ array text.\n     *\n     * @private\n     * @param {TextGlyph[]} glyph Glyphs extracted from the operator.\n     * @param {string[]} text Decoded text units.\n     * @param {string} originalText Original operand text.\n     * @param {string[]} decodeText Decoded segments mapped from original text.\n     * @returns {string} Updated TJ array text or original if nothing replaced.\n     */\n    PdfRedactor.prototype._replacedText = function (glyph, text, originalText, decodeText) {\n        var isReplacedText = false;\n        var isOtherText = false;\n        for (var i = 0; i < glyph.length; i++) {\n            if (this._isFoundBounds(glyph[Number.parseInt(i.toString(), 10)]._bounds, this._redactionRegion)) {\n                isReplacedText = true;\n                glyph[Number.parseInt(i.toString(), 10)]._isReplace = true;\n            }\n            else {\n                isOtherText = true;\n                glyph[Number.parseInt(i.toString(), 10)]._text = text[Number.parseInt(i.toString(), 10)];\n            }\n        }\n        var updatedText = '';\n        if (!isReplacedText && isOtherText) {\n            return originalText;\n        }\n        else {\n            var mainTextCollection = [];\n            if (originalText[0] === '(') {\n                mainTextCollection = decodeText;\n            }\n            else if (originalText[0] === '[') {\n                mainTextCollection = decodeText;\n            }\n            else if (originalText[0] === '<') {\n                mainTextCollection = decodeText;\n                this._isHex = true;\n            }\n            var map = this._mapString(mainTextCollection, glyph);\n            for (var i = 0; i < map.length; i++) {\n                map[Number.parseInt(i.toString(), 10)]._isHex = this._isHex;\n                updatedText += map[Number.parseInt(i.toString(), 10)]._getText();\n            }\n            updatedText = '[' + updatedText + ']';\n            this._isHex = false;\n        }\n        return updatedText;\n    };\n    /**\n     * Maps decoded text segments to glyph slices while preserving substring boundaries.\n     *\n     * @private\n     * @param {string[]} mainTextCollection Text segments.\n     * @param {TextGlyph[]} imageGlyph Full glyph list aligned with extracted text.\n     * @returns {_TextGlyphMapper[]} Mapped text-glyph segments.\n     */\n    PdfRedactor.prototype._mapString = function (mainTextCollection, imageGlyph) {\n        var mappedString = [];\n        var glyphList = imageGlyph;\n        var startIndex = 0;\n        for (var i = 0; i < mainTextCollection.length; i++) {\n            var endChar = mainTextCollection[Number.parseInt(i.toString(), 10)].length - 1;\n            if (mainTextCollection[Number.parseInt(i.toString(), 10)][0] !== '(' && mainTextCollection[Number.parseInt(i.toString(), 10)][Number.parseInt(endChar.toString(), 10)] !== ')') {\n                var mapping = new _text_glyph_mapper__WEBPACK_IMPORTED_MODULE_5__._TextGlyphMapper();\n                mapping.text = mainTextCollection[Number.parseInt(i.toString(), 10)];\n                mappedString.push(mapping);\n            }\n            else {\n                var mapping = new _text_glyph_mapper__WEBPACK_IMPORTED_MODULE_5__._TextGlyphMapper();\n                mapping.text = mainTextCollection[Number.parseInt(i.toString(), 10)];\n                var text = mainTextCollection[Number.parseInt(i.toString(), 10)];\n                var subString = text.length >= 2;\n                var start = text.startsWith('(');\n                var end = text.endsWith(')');\n                if (subString && start && !end) {\n                    text = text.substring(1, text.length);\n                }\n                else if (subString && !start && end) {\n                    text = text.substring(0, text.length - 1);\n                }\n                else if (subString) {\n                    text = text.substring(1, text.length - 1);\n                }\n                else {\n                    continue;\n                }\n                var length_1 = text.length;\n                mapping.glyph = glyphList.slice(startIndex, startIndex + length_1);\n                startIndex += length_1;\n                mappedString.push(mapping);\n            }\n        }\n        return mappedString;\n    };\n    return PdfRedactor;\n}());\n\n\n\n//# sourceURL=webpack://@syncfusion/ej2-documents-vue-samples/./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/redaction/pdf-redactor.js?");

/***/ })

}]);