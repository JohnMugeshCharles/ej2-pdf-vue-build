"use strict";
/*
 * ATTENTION: The "eval" devtool has been used (maybe by default in mode: "development").
 * This devtool is neither made for production nor for readable output files.
 * It uses "eval()" calls to create a separate source file in the browser devtools.
 * If you are trying to read the output file, select a different devtool (https://webpack.js.org/configuration/devtool/)
 * or disable the default devtool with "devtool: false".
 * If you are looking for production-ready output files, see mode: "production" (https://webpack.js.org/configuration/mode/).
 */
(self["webpackChunk_syncfusion_ej2_documents_vue_samples"] = self["webpackChunk_syncfusion_ej2_documents_vue_samples"] || []).push([["chunk-vendors-node_modules_syncfusion_ej2-pdf-data-extract_src_pdf-data-extract_core_image-extractio-b6b010fb"],{

/***/ "./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/image-extraction/image.js":
/*!***********************************************************************************************************!*\
  !*** ./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/image-extraction/image.js ***!
  \***********************************************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   ApplicationPlatform: () => (/* binding */ ApplicationPlatform),\n/* harmony export */   _PdfImage: () => (/* binding */ _PdfImage)\n/* harmony export */ });\n/* harmony import */ var _image_resizer__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./image-resizer */ \"./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/image-extraction/image-resizer.js\");\n/* harmony import */ var _jpx_image__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../jpx-image */ \"./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/jpx-image.js\");\n/* harmony import */ var _import_decode_image__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../import/decode-image */ \"./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/import/decode-image.js\");\n/* harmony import */ var _enum__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../enum */ \"./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/enum.js\");\n/* harmony import */ var _image_utils__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./image-utils */ \"./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/image-extraction/image-utils.js\");\n/* harmony import */ var _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! @syncfusion/ej2-pdf */ \"./node_modules/@syncfusion/ej2-pdf/index.js\");\n/* harmony import */ var _colorspace_utils__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ./colorspace-utils */ \"./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/image-extraction/colorspace-utils.js\");\n/* harmony import */ var _colorspace__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ./colorspace */ \"./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/image-extraction/colorspace.js\");\n/* harmony import */ var _utils__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ../utils */ \"./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/utils.js\");\n/* harmony import */ var _jpeg_image__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ./jpeg-image */ \"./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/image-extraction/jpeg-image.js\");\nvar __awaiter = (undefined && undefined.__awaiter) || function (thisArg, _arguments, P, generator) {\n    return new (P || (P = Promise))(function (resolve, reject) {\n        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }\n        function rejected(value) { try { step(generator[\"throw\"](value)); } catch (e) { reject(e); } }\n        function step(result) { result.done ? resolve(result.value) : new P(function (resolve) { resolve(result.value); }).then(fulfilled, rejected); }\n        step((generator = generator.apply(thisArg, _arguments || [])).next());\n    });\n};\nvar __generator = (undefined && undefined.__generator) || function (thisArg, body) {\n    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g;\n    return g = { next: verb(0), \"throw\": verb(1), \"return\": verb(2) }, typeof Symbol === \"function\" && (g[Symbol.iterator] = function() { return this; }), g;\n    function verb(n) { return function (v) { return step([n, v]); }; }\n    function step(op) {\n        if (f) throw new TypeError(\"Generator is already executing.\");\n        while (_) try {\n            if (f = 1, y && (t = op[0] & 2 ? y[\"return\"] : op[0] ? y[\"throw\"] || ((t = y[\"return\"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;\n            if (y = 0, t) op = [op[0] & 2, t.value];\n            switch (op[0]) {\n                case 0: case 1: t = op; break;\n                case 4: _.label++; return { value: op[1], done: false };\n                case 5: _.label++; y = op[1]; op = [0]; continue;\n                case 7: op = _.ops.pop(); _.trys.pop(); continue;\n                default:\n                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }\n                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }\n                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }\n                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }\n                    if (t[2]) _.ops.pop();\n                    _.trys.pop(); continue;\n            }\n            op = body.call(thisArg, _);\n        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }\n        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };\n    }\n};\n\n\n\n\n\n\n\n\n\n\n/**\n * Internal PDF image helper that decodes, colors, resizes, and exports image data.\n *\n * @private\n */\nvar _PdfImage = /** @class */ (function () {\n    function _PdfImage() {\n        this.imageResizer = new _image_resizer__WEBPACK_IMPORTED_MODULE_0__._PdfImageResizer();\n    }\n    /**\n     * Initializes the image from the given PDF image stream/dictionary, resolving color space, masks, and dimensions.\n     *\n     * @private\n     * @param {any} xref Cross-reference for object fetching.\n     * @param {any} image Source image stream or wrapper.\n     * @param {boolean} isInline Whether the image is inline.\n     * @param {any} callBack Canvas/platform callback provider.\n     * @param {any} [smask] Optional soft mask stream.\n     * @param {any} [mask] Optional hard mask stream or range.\n     * @param {boolean} [isMask] Whether this call initializes an image mask.\n     * @returns {Promise<any>} Resolves to the current `_PdfImage` instance.\n     * @throws {FormatError} When required image dimensions are invalid.\n     */\n    _PdfImage.prototype._initializeFromImage = function (xref, image, isInline, callBack, // eslint-disable-line \n    smask, mask, isMask) {\n        return __awaiter(this, void 0, void 0, function () {\n            var _a, dict, filter, filterName, filterZero, jpx, reducePower, factor, width, height, validWidth, validHeight, bitsPerComponent, colorSpace, hasColorSpace, color, _b, max, isIndexed, i, j, dmin, dmax, _c, maskDict, imageMask, _d;\n            return __generator(this, function (_e) {\n                switch (_e.label) {\n                    case 0:\n                        if (!image) return [3 /*break*/, 8];\n                        this.image = image;\n                        dict = image.dictionary;\n                        filter = dict.get('F', 'Filter');\n                        filterName = void 0;\n                        if (filter instanceof _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_5__._PdfName) {\n                            filterName = filter.name;\n                        }\n                        else if (Array.isArray(filter)) {\n                            filterZero = void 0;\n                            if (filter[0] instanceof _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_5__._PdfReference) {\n                                filterZero = xref._fetch(filter[0]);\n                            }\n                            else {\n                                filterZero = filter[0];\n                            }\n                            if (filterZero instanceof _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_5__._PdfName) {\n                                filterName = filterZero.name;\n                            }\n                        }\n                        jpx = void 0;\n                        reducePower = void 0;\n                        switch (filterName) {\n                            case 'JPXDecode':\n                                jpx = new _jpx_image__WEBPACK_IMPORTED_MODULE_1__._PdfJpxImage();\n                                (_a = jpx._parseImageProperties(image.stream), image.width = _a.width, image.height = _a.height, image.numComps = _a.componentsCount, image.bitsPerComponent = _a.bitsPerComponent);\n                                image.stream.reset();\n                                reducePower = this.imageResizer._getReducePowerForJPX(image.width, image.height, image.numComps);\n                                this.jpxDecoderOptions = { numComponents: 0, isIndexedColormap: false, smaskInData: dict.has('SMaskInData'), reducePower: reducePower };\n                                if (reducePower) {\n                                    factor = Math.pow(2, reducePower);\n                                    image.width = Math.ceil(image.width / factor);\n                                    image.height = Math.ceil(image.height / factor);\n                                }\n                                break;\n                            case 'JBIG2Decode':\n                                image.bitsPerComponent = 1;\n                                image.numComps = 1;\n                                break;\n                        }\n                        width = dict.get('W', 'Width');\n                        height = dict.get('H', 'Height');\n                        if (Number.isInteger(image.width) && image.width > 0 && Number.isInteger(image.height) && image.height > 0 &&\n                            (image.width !== width || image.height !== height)) {\n                            width = image.width;\n                            height = image.height;\n                        }\n                        else {\n                            validWidth = typeof width === 'number' && width > 0;\n                            validHeight = typeof height === 'number' && height > 0;\n                            if (!validWidth || !validHeight) {\n                                if (!image.fallbackDims) {\n                                    throw new _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_5__.FormatError(\"Invalid image width: \" + width + \" or height: \" + height);\n                                }\n                                if (!validWidth) {\n                                    width = image.fallbackDims.width;\n                                }\n                                if (!validHeight) {\n                                    height = image.fallbackDims.height;\n                                }\n                            }\n                        }\n                        this.width = width;\n                        this.height = height;\n                        this.interpolate = dict.get('I', 'Interpolate');\n                        this.imageMask = dict.get('IM', 'ImageMask') || false;\n                        this.matte = dict.get('Matte') || false;\n                        bitsPerComponent = image.bitsPerComponent;\n                        if (!bitsPerComponent) {\n                            bitsPerComponent = dict.get('BPC', 'BitsPerComponent');\n                            if (!bitsPerComponent) {\n                                if (this.imageMask) {\n                                    bitsPerComponent = 1;\n                                }\n                                else {\n                                    throw new _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_5__.FormatError(\"Bits per component missing in image: \" + this.imageMask);\n                                }\n                            }\n                        }\n                        this.bpc = bitsPerComponent;\n                        if (!!this.imageMask) return [3 /*break*/, 2];\n                        colorSpace = dict.getRaw('CS') || dict.getRaw('ColorSpace');\n                        hasColorSpace = colorSpace !== undefined;\n                        if (!hasColorSpace) {\n                            if (this.jpxDecoderOptions) {\n                                colorSpace = _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_5__._PdfName.get('DeviceRGBA');\n                            }\n                            else {\n                                switch (image.numComps) {\n                                    case 1:\n                                        colorSpace = _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_5__._PdfName.get('DeviceGray');\n                                        break;\n                                    case 3:\n                                        colorSpace = _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_5__._PdfName.get('DeviceRGB');\n                                        break;\n                                    case 4:\n                                        colorSpace = _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_5__._PdfName.get('DeviceCMYK');\n                                        break;\n                                    default:\n                                        throw new Error(\"Images with \" + image.numComps + \" color components not supported.\");\n                                }\n                            }\n                        }\n                        else if (this.jpxDecoderOptions && this.jpxDecoderOptions.smaskInData) {\n                            colorSpace = _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_5__._PdfName.get('DeviceRGBA');\n                        }\n                        color = new _colorspace_utils__WEBPACK_IMPORTED_MODULE_6__._PdfColorSpaceUtils(callBack);\n                        _b = this;\n                        return [4 /*yield*/, color._parse(colorSpace, xref, null, null, null, false, null)];\n                    case 1:\n                        _b.colorSpace = _e.sent();\n                        this.numComps = this.colorSpace.numComps;\n                        if (this.jpxDecoderOptions) {\n                            this.jpxDecoderOptions.numComponents = hasColorSpace\n                                ? this.numComps\n                                : 0;\n                            this.jpxDecoderOptions.isIndexedColormap =\n                                this.colorSpace.name === 'Indexed';\n                        }\n                        _e.label = 2;\n                    case 2:\n                        this.decode = dict.getArray('D', 'Decode') || null;\n                        this.needsDecode = false;\n                        if (this.decode && ((this.colorSpace && !this.colorSpace._isDefaultDecode(this.decode, bitsPerComponent)) || (isMask && !this._isDefaultDecode()))) {\n                            this.needsDecode = true;\n                            max = (1 << bitsPerComponent) - 1;\n                            this.decodeCoefficients = [];\n                            this.decodeAddends = [];\n                            isIndexed = this.colorSpace && this.colorSpace.name === 'Indexed';\n                            for (i = 0, j = 0; i < this.decode.length; i += 2, ++j) {\n                                dmin = this.decode[i];\n                                dmax = this.decode[i + 1];\n                                this.decodeCoefficients[j] = isIndexed\n                                    ? (dmax - dmin) / max\n                                    : dmax - dmin;\n                                this.decodeAddends[j] = isIndexed ? dmin : max * dmin;\n                            }\n                        }\n                        if (!smask) return [3 /*break*/, 4];\n                        if (smask.fallbackDims === null) {\n                            smask.fallbackDims = { width: width, height: height };\n                        }\n                        _c = this;\n                        return [4 /*yield*/, new _PdfImage()._initializeFromImage(xref, smask, isInline, callBack, null, null, false)];\n                    case 3:\n                        _c.smask = _e.sent();\n                        return [3 /*break*/, 8];\n                    case 4:\n                        if (!mask) return [3 /*break*/, 8];\n                        if (!(mask instanceof _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_5__._PdfBaseStream)) return [3 /*break*/, 7];\n                        maskDict = mask.dictionary;\n                        imageMask = maskDict.get('IM', 'ImageMask');\n                        if (!imageMask) return [3 /*break*/, 6];\n                        _d = this;\n                        return [4 /*yield*/, new _PdfImage()._initializeFromImage(xref, mask, isInline, callBack, null, null, true)];\n                    case 5:\n                        _d.mask = _e.sent();\n                        _e.label = 6;\n                    case 6: return [3 /*break*/, 8];\n                    case 7:\n                        this.mask = mask;\n                        _e.label = 8;\n                    case 8: return [2 /*return*/, this];\n                }\n            });\n        });\n    };\n    /** Resizes a single-channel buffer using nearest-neighbor sampling.\n     *\n     * @private\n     * @param {Uint8Array | Uint16Array | Uint32Array} src Source mask buffer.\n     * @param {number} bpc Bits per component.\n     * @param {number} w1 Source width.\n     * @param {number} h1 Source height.\n     * @param {number} w2 Target width.\n     * @param {number} h2 Target height.\n     * @returns {Uint8Array | Uint16Array | Uint32Array} Resized buffer with same component width.\n     */\n    _PdfImage.prototype._resizeImageMask = function (src, bpc, w1, h1, w2, h2) {\n        var length = w2 * h2;\n        var dest;\n        if (bpc <= 8) {\n            dest = new Uint8Array(length);\n        }\n        else if (bpc <= 16) {\n            dest = new Uint16Array(length);\n        }\n        else {\n            dest = new Uint32Array(length);\n        }\n        var xRatio = w1 / w2;\n        var yRatio = h1 / h2;\n        var i;\n        var j;\n        var py;\n        var newIndex = 0;\n        var oldIndex;\n        var xScaled = new Uint16Array(w2);\n        var w1Scanline = w1;\n        for (i = 0; i < w2; i++) {\n            xScaled[i] = Math.floor(i * xRatio);\n        }\n        for (i = 0; i < h2; i++) {\n            py = Math.floor(i * yRatio) * w1Scanline;\n            for (j = 0; j < w2; j++) {\n                oldIndex = py + xScaled[j];\n                dest[newIndex] = src[oldIndex];\n                newIndex++;\n            }\n        }\n        return dest;\n    };\n    /**\n     * Determines whether the current decode array is default for masks when color space is not resolved.\n     *\n     * @private\n     * @returns {boolean} `true` if decode map is default otherwise, `false`.\n     */\n    _PdfImage.prototype._isDefaultDecode = function () {\n        if (typeof (this.colorSpace) === 'undefined') {\n            var colorPalette = new _colorspace__WEBPACK_IMPORTED_MODULE_7__._PdfColorPalette('ColorSpace', undefined);\n            return colorPalette._isDefaultDecode(this.decode, 1);\n        }\n        return false;\n    };\n    /**\n     * Retrieves image raw data bytes based on the underlying stream type.\n     *\n     * @private\n     * @param {number} length Expected number of bytes to read.\n     * @param {any} decoderOptions Decoder options and image context.\n     * @returns {any} Raw bytes buffer or decoded result depending on stream type.\n     */\n    _PdfImage.prototype._getImageData = function (length, decoderOptions) {\n        var stream = decoderOptions.image; // eslint-disable-line\n        if (stream instanceof _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_5__._PdfJpegStream) {\n            var values = decoderOptions.image.getBytes(length, decoderOptions); // eslint-disable-line\n            var jpegImage = new _jpeg_image__WEBPACK_IMPORTED_MODULE_9__._PdfJpegImage();\n            jpegImage.parse(values);\n            var data_1 = jpegImage._getData(this.drawWidth, this.drawHeight, this.forceRgba, this.forceRgb, // eslint-disable-line \n            true);\n            decoderOptions.image.buffer = data_1;\n            decoderOptions.bufferLength = data_1.length;\n            return data_1;\n        }\n        if (!(stream instanceof _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_5__._PdfFlateStream)) {\n            if (stream instanceof _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_5__._PdfJpxStream) {\n                return decoderOptions.image.decodeImage(null, decoderOptions);\n            }\n            return decoderOptions.image.getBytes(length, decoderOptions);\n        }\n        var data = decoderOptions.image.getBytes(length, decoderOptions); // eslint-disable-line\n        if (stream instanceof _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_5__._PdfFlateStream) {\n            return data;\n        }\n        return decoderOptions.image.decodeImage(data, decoderOptions);\n    };\n    /**\n     * Creates RGBA/RGB image data for drawing/encoding; handles color conversion, decode array, smask/mask, resize, and export.\n     *\n     * @private\n     * @param {boolean} [forceRGBA=false] If `true`, allocate RGBA output even without masks.\n     * @param {boolean} [isOffscreenCanvasSupported=false] If `true`, use canvas to encode/export when available.\n     * @returns {Promise<any>} Image data or encoded bytes depending on environment and options.\n     */\n    _PdfImage.prototype._createImageData = function (forceRGBA, isOffscreenCanvasSupported) {\n        if (forceRGBA === void 0) { forceRGBA = false; }\n        if (isOffscreenCanvasSupported === void 0) { isOffscreenCanvasSupported = false; }\n        return __awaiter(this, void 0, void 0, function () {\n            var drawWidth, drawHeight, imgData, numComps, originalWidth, originalHeight, bpc, rowBytes, mustBeResized, imgArray_1, _a, kind, image, data_2, buffer, i, ii, imgArray, actualHeight, comps, alpha01, maybeUndoPreblend, data, canvasImgData, ctx, canvas, arr, format, buffer, base64, bytes;\n            return __generator(this, function (_b) {\n                switch (_b.label) {\n                    case 0:\n                        drawWidth = this.drawWidth;\n                        drawHeight = this.drawHeight;\n                        imgData = { width: drawWidth, height: drawHeight, interpolate: this.interpolate, kind: 0,\n                            data: null };\n                        numComps = this.numComps;\n                        originalWidth = this.width;\n                        originalHeight = this.height;\n                        bpc = this.bpc;\n                        rowBytes = (originalWidth * numComps * bpc + 7) >> 3;\n                        mustBeResized = isOffscreenCanvasSupported &&\n                            this.imageResizer._needsToBeResized(drawWidth, drawHeight);\n                        if (!(!this.smask && !this.mask && this.colorSpace.name === 'DeviceRGBA')) return [3 /*break*/, 6];\n                        imgData.kind = _image_utils__WEBPACK_IMPORTED_MODULE_4__.imageKind.rgba32BPP;\n                        _a = imgData;\n                        return [4 /*yield*/, this._getImageBytes(originalHeight * originalWidth * 4, {})];\n                    case 1:\n                        imgArray_1 = (_a.data = _b.sent());\n                        if (!isOffscreenCanvasSupported) return [3 /*break*/, 5];\n                        if (!!mustBeResized) return [3 /*break*/, 3];\n                        return [4 /*yield*/, this._createBitmap(_image_utils__WEBPACK_IMPORTED_MODULE_4__.imageKind.rgba32BPP, drawWidth, drawHeight, imgArray_1)];\n                    case 2: return [2 /*return*/, _b.sent()];\n                    case 3: return [4 /*yield*/, this.imageResizer._createImageData(imgData, false)];\n                    case 4: return [2 /*return*/, _b.sent()];\n                    case 5: return [2 /*return*/, imgData];\n                    case 6:\n                        if (!!forceRGBA) return [3 /*break*/, 10];\n                        kind = void 0;\n                        if (this.colorSpace.name === 'DeviceGray' && bpc === 1) {\n                            kind = _image_utils__WEBPACK_IMPORTED_MODULE_4__.imageKind.grayScale1Bpp;\n                        }\n                        else if (this.colorSpace.name === 'DeviceRGB' && bpc === 8 && !this.needsDecode) {\n                            kind = _image_utils__WEBPACK_IMPORTED_MODULE_4__.imageKind.rgb24BPP;\n                        }\n                        if (!(kind && !this.smask && !this.mask && drawWidth === originalWidth && drawHeight === originalHeight)) return [3 /*break*/, 10];\n                        image = this._getImage(originalWidth, originalHeight);\n                        if (image) {\n                            return [2 /*return*/, image];\n                        }\n                        return [4 /*yield*/, this._getImageBytes(originalHeight * rowBytes, {})];\n                    case 7:\n                        data_2 = _b.sent();\n                        if (!isOffscreenCanvasSupported) return [3 /*break*/, 9];\n                        if (mustBeResized) {\n                            return [2 /*return*/, this.imageResizer._createImageData({ data: data_2, kind: kind, width: drawWidth, height: drawHeight, interpolate: this.interpolate }, this.needsDecode)];\n                        }\n                        return [4 /*yield*/, this._createBitmap(kind, originalWidth, originalHeight, data_2)];\n                    case 8: return [2 /*return*/, _b.sent()];\n                    case 9:\n                        imgData.kind = kind;\n                        imgData.data = data_2;\n                        if (this.needsDecode) {\n                            buffer = imgData.data;\n                            for (i = 0, ii = buffer.length; i < ii; i++) {\n                                buffer[i] ^= 0xff;\n                            }\n                        }\n                        return [2 /*return*/, imgData];\n                    case 10: return [4 /*yield*/, this._getImageBytes(originalHeight * rowBytes, { internal: true })];\n                    case 11:\n                        imgArray = _b.sent();\n                        actualHeight = 0 | (((imgArray.length / rowBytes) * drawHeight) / originalHeight);\n                        comps = this._getComponents(imgArray);\n                        if (isOffscreenCanvasSupported && !mustBeResized) {\n                            canvas = document.createElement('canvas');\n                            canvas.width = drawWidth;\n                            canvas.height = drawHeight;\n                            ctx = canvas.getContext('2d');\n                            canvasImgData = ctx.createImageData(drawWidth, drawHeight);\n                            data = canvasImgData.data;\n                        }\n                        imgData.kind = _image_utils__WEBPACK_IMPORTED_MODULE_4__.imageKind.rgba32BPP;\n                        if (!(!forceRGBA && !this.smask && !this.mask)) return [3 /*break*/, 12];\n                        if (!isOffscreenCanvasSupported || mustBeResized) {\n                            imgData.kind = _image_utils__WEBPACK_IMPORTED_MODULE_4__.imageKind.rgb24BPP;\n                            data = new Uint8ClampedArray(drawWidth * drawHeight * 3);\n                            alpha01 = 0;\n                        }\n                        else {\n                            arr = new Uint32Array(data.buffer);\n                            arr.fill((0,_syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_5__._isLittleEndian)() ? 0xff000000 : 0x000000ff);\n                            alpha01 = 1;\n                        }\n                        maybeUndoPreblend = false;\n                        return [3 /*break*/, 14];\n                    case 12:\n                        if (!isOffscreenCanvasSupported || mustBeResized) {\n                            data = new Uint8ClampedArray(drawWidth * drawHeight * 4);\n                        }\n                        alpha01 = 1;\n                        maybeUndoPreblend = true;\n                        return [4 /*yield*/, this._fillOpacity(data, drawWidth, drawHeight, actualHeight, comps)];\n                    case 13:\n                        _b.sent();\n                        _b.label = 14;\n                    case 14:\n                        if (this.needsDecode) {\n                            this._decodeBuffer(comps);\n                        }\n                        return [4 /*yield*/, this.colorSpace._fillRgb(data, originalWidth, originalHeight, drawWidth, drawHeight, actualHeight, bpc, comps, alpha01)];\n                    case 15:\n                        _b.sent();\n                        if (maybeUndoPreblend) {\n                            this._undoPreblend(data, drawWidth, actualHeight);\n                        }\n                        if (isOffscreenCanvasSupported && !mustBeResized) {\n                            if (typeof (this._options) !== 'undefined') {\n                                this._processImageRedaction(canvasImgData);\n                            }\n                            ctx.putImageData(canvasImgData, 0, 0);\n                            format = this._findImageFormat(this._imageFormat);\n                            buffer = canvas.toDataURL(format);\n                            base64 = buffer.split(',')[1];\n                            bytes = (0,_utils__WEBPACK_IMPORTED_MODULE_8__._base64ToUint8Array)(base64);\n                            return [2 /*return*/, bytes];\n                        }\n                        imgData.data = data;\n                        if (mustBeResized) {\n                            return [2 /*return*/, this.imageResizer._createImageData(imgData)];\n                        }\n                        return [2 /*return*/, imgData];\n                }\n            });\n        });\n    };\n    /**\n     * Maps an `ImageFormat` value to a canvas MIME type string.\n     *\n     * @private\n     * @param {ImageFormat} format Target image format.\n     * @returns {string} Canvas MIME string for encoding.\n     */\n    _PdfImage.prototype._findImageFormat = function (format) {\n        if (_enum__WEBPACK_IMPORTED_MODULE_3__.ImageFormat.png === format) {\n            return 'image/png';\n        }\n        else {\n            return 'image/jpeg';\n        }\n    };\n    /**\n     * Builds a `_PdfImage` from a raw image stream, wiring SMask/Mask entries.\n     *\n     * @private\n     * @param {_PdfCrossReference} xref Cross-reference.\n     * @param {any} image Source image stream.\n     * @param {boolean} isInline Whether inline.\n     * @param {any} callBack Canvas/platform callback.\n     * @returns {Promise<_PdfImage>} The initialized `_PdfImage`.\n     */\n    _PdfImage.prototype._buildImage = function (xref, image, isInline, callBack) {\n        return __awaiter(this, void 0, void 0, function () {\n            var imageData, smaskData, maskData, smask, mask;\n            return __generator(this, function (_a) {\n                switch (_a.label) {\n                    case 0:\n                        imageData = image;\n                        smaskData = null;\n                        maskData = null;\n                        smask = image.dictionary.get('SMask');\n                        mask = image.dictionary.get('Mask');\n                        if (smask) {\n                            if (smask instanceof _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_5__._PdfBaseStream) {\n                                smaskData = smask;\n                            }\n                        }\n                        else if (mask) {\n                            if (mask instanceof _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_5__._PdfBaseStream || Array.isArray(mask)) {\n                                maskData = mask;\n                            }\n                        }\n                        return [4 /*yield*/, new _PdfImage()._initializeFromImage(xref, imageData, isInline, callBack, smaskData, maskData)];\n                    case 1: return [2 /*return*/, _a.sent()];\n                }\n            });\n        });\n    };\n    /**\n     * Creates an 8-bit per channel alpha mask for 1bpp mask streams.\n     *\n     * @private\n     * @param {any} image Source mask stream.\n     * @param {boolean} isOffscreenCanvasSupported Whether canvas export is available.\n     * @returns {any} Encoded bytes or raw buffer depending on environment.\n     * @throws {FormatError} When an unknown mask format is provided.\n     */\n    _PdfImage.prototype._createMask = function (image, isOffscreenCanvasSupported) {\n        var dict = image.dictionary;\n        var width = dict.get('W', 'Width');\n        var height = dict.get('H', 'Height');\n        var interpolate = dict.get('I', 'Interpolate'); // eslint-disable-line\n        var decode = dict.getArray('D', 'Decode');\n        var inverseDecode = decode && decode[0] > 0;\n        var computedLength = ((width + 7) >> 3) * height;\n        var imgArray = image.getBytes(computedLength); // eslint-disable-line\n        var isSingleOpaquePixel = width === 1 && height === 1 &&\n            inverseDecode === (imgArray.length === 0 || !!(imgArray[0] & 128));\n        if (isSingleOpaquePixel) {\n            return { isSingleOpaquePixel: isSingleOpaquePixel };\n        }\n        if (isOffscreenCanvasSupported) {\n            if (this.imageResizer._needsToBeResized(width, height)) {\n                var data_3 = new Uint8ClampedArray(width * height * 4);\n                (0,_image_utils__WEBPACK_IMPORTED_MODULE_4__._convertBlackAndWhiteToRGBA)(imgArray, 0, data_3, width, height, 0, inverseDecode);\n                return this.imageResizer._createImageData({ kind: _image_utils__WEBPACK_IMPORTED_MODULE_4__.imageKind.rgba32BPP, data: data_3, width: width, height: height, interpolate: interpolate });\n            }\n            var canvas = void 0; // eslint-disable-line\n            if (this._canvasRenderCallback) {\n                canvas = this._canvasRenderCallback.canvas;\n            }\n            canvas.width = width;\n            canvas.height = height;\n            var ctx = canvas.getContext('2d');\n            var imgData = ctx.createImageData(width, height); // eslint-disable-line\n            (0,_image_utils__WEBPACK_IMPORTED_MODULE_4__._convertBlackAndWhiteToRGBA)(imgArray, 0, imgData.data, width, height, 0, inverseDecode);\n            if (typeof (this._options) !== 'undefined') {\n                this._processImageRedaction(imgData);\n            }\n            ctx.putImageData(imgData, 0, 0);\n            var format = this._findImageFormat(this._imageFormat);\n            var buffer = canvas.toDataURL(format);\n            var base64 = buffer.split(',')[1];\n            var bytes = (0,_utils__WEBPACK_IMPORTED_MODULE_8__._base64ToUint8Array)(base64);\n            return bytes;\n        }\n        var actualLength = imgArray.byteLength;\n        var haveFullData = computedLength === actualLength;\n        var data;\n        if (image instanceof _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_5__._PdfDecodeStream && (!inverseDecode || haveFullData)) {\n            data = imgArray;\n        }\n        else if (!inverseDecode) {\n            data = new Uint8Array(imgArray);\n        }\n        else {\n            data = new Uint8Array(computedLength);\n            data.set(imgArray);\n            data.fill(0xff, actualLength);\n        }\n        if (inverseDecode) {\n            for (var i = 0; i < actualLength; i++) {\n                data[i] ^= 0xff;\n            }\n        }\n        return data;\n    };\n    Object.defineProperty(_PdfImage.prototype, \"drawWidth\", {\n        get: function () {\n            return Math.max(this.width, this.smask && this.smask.width || 0, this.mask && this.mask.width || 0);\n        },\n        enumerable: true,\n        configurable: true\n    });\n    Object.defineProperty(_PdfImage.prototype, \"drawHeight\", {\n        get: function () {\n            return Math.max(this.height, this.smask && this.smask.height || 0, this.mask && this.mask.height || 0);\n        },\n        enumerable: true,\n        configurable: true\n    });\n    /**\n     * Applies the decode array mapping in-place to the provided buffer.\n     *\n     * @private\n     * @param {Uint8Array} buffer Component buffer to transform.\n     * @returns {void} nothing.\n     */\n    _PdfImage.prototype._decodeBuffer = function (buffer) {\n        var bpc = this.bpc;\n        var numComps = this.numComps;\n        var decodeAddends = this.decodeAddends;\n        var decodeCoefficients = this.decodeCoefficients;\n        var max = (1 << bpc) - 1;\n        var i;\n        var ii;\n        if (bpc === 1) {\n            for (i = 0, ii = buffer.length; i < ii; i++) {\n                buffer[i] = +!buffer[i];\n            }\n            return;\n        }\n        var index = 0;\n        for (i = 0, ii = this.width * this.height; i < ii; i++) {\n            for (var j = 0; j < numComps; j++) {\n                buffer[index] = (0,_syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_5__._mathClamp)(decodeAddends[j] + buffer[index] *\n                    decodeCoefficients[j], 0, max);\n                index++;\n            }\n        }\n    };\n    /**\n     * Expands packed/bpc component data into a typed array with one value per component.\n     *\n     * @private\n     * @param {Uint8Array} buffer Packed source buffer.\n     * @returns {Uint8Array | Uint16Array | Uint32Array} Expanded component buffer.\n     */\n    _PdfImage.prototype._getComponents = function (buffer) {\n        var bpc = this.bpc;\n        if (bpc === 8) {\n            return buffer;\n        }\n        var width = this.width;\n        var height = this.height;\n        var numComps = this.numComps;\n        var length = width * height * numComps;\n        var bufferPos = 0;\n        var output; // eslint-disable-line\n        if (bpc <= 8) {\n            output = new Uint8Array(length);\n        }\n        else if (bpc <= 16) {\n            output = new Uint16Array(length);\n        }\n        else {\n            output = new Uint32Array(length);\n        }\n        var rowComps = width * numComps;\n        var max = (1 << bpc) - 1;\n        var i = 0;\n        var ii;\n        var buf; // eslint-disable-line\n        if (bpc === 1) {\n            var mask = void 0; // eslint-disable-line\n            var loop1End = void 0;\n            var loop2End = void 0;\n            for (var j = 0; j < height; j++) {\n                loop1End = i + (rowComps & ~7);\n                loop2End = i + rowComps;\n                while (i < loop1End) {\n                    buf = buffer[bufferPos++];\n                    output[i] = (buf >> 7) & 1;\n                    output[i + 1] = (buf >> 6) & 1;\n                    output[i + 2] = (buf >> 5) & 1;\n                    output[i + 3] = (buf >> 4) & 1;\n                    output[i + 4] = (buf >> 3) & 1;\n                    output[i + 5] = (buf >> 2) & 1;\n                    output[i + 6] = (buf >> 1) & 1;\n                    output[i + 7] = buf & 1;\n                    i += 8;\n                }\n                if (i < loop2End) {\n                    buf = buffer[bufferPos++];\n                    mask = 128;\n                    while (i < loop2End) {\n                        output[i++] = +!!(buf & mask);\n                        mask >>= 1;\n                    }\n                }\n            }\n        }\n        else {\n            var bits = 0;\n            buf = 0;\n            for (i = 0, ii = length; i < ii; ++i) {\n                if (i % rowComps === 0) {\n                    buf = 0;\n                    bits = 0;\n                }\n                while (bits < bpc) {\n                    buf = (buf << 8) | buffer[bufferPos++];\n                    bits += 8;\n                }\n                var remainingBits = bits - bpc;\n                var value = buf >> remainingBits;\n                if (value < 0) {\n                    value = 0;\n                }\n                else if (value > max) {\n                    value = max;\n                }\n                output[i] = value;\n                buf &= (1 << remainingBits) - 1;\n                bits = remainingBits;\n            }\n        }\n        return output;\n    };\n    /**\n     * Fills the alpha channel for RGBA output using SMask/Mask or default opaque alpha.\n     *\n     * @private\n     * @param {Uint8ClampedArray} rgbaBuf Target RGBA buffer.\n     * @param {number} width Output width.\n     * @param {number} height Output height.\n     * @param {number} actualHeight Actual populated rows.\n     * @param {Uint8ClampedArray} image Component buffer.\n     * @returns {Promise<void>} Resolves when alpha is filled.\n     * @throws {FormatError} On unknown mask formats.\n     */\n    _PdfImage.prototype._fillOpacity = function (rgbaBuf, width, height, actualHeight, image) {\n        return __awaiter(this, void 0, void 0, function () {\n            var smask, mask, alphaBuf, sw, sh, i, ii, j, numComps, opacity, imageOffset, color, maskOffset;\n            return __generator(this, function (_a) {\n                switch (_a.label) {\n                    case 0:\n                        smask = this.smask;\n                        mask = this.mask;\n                        if (!smask) return [3 /*break*/, 2];\n                        sw = smask.width;\n                        sh = smask.height;\n                        alphaBuf = new Uint8ClampedArray(sw * sh);\n                        return [4 /*yield*/, smask._fillGrayBuffer(alphaBuf)];\n                    case 1:\n                        _a.sent();\n                        if (sw !== width || sh !== height) {\n                            alphaBuf = this._resizeImageMask(alphaBuf, smask.bpc, sw, sh, width, height);\n                        }\n                        return [3 /*break*/, 3];\n                    case 2:\n                        if (mask) {\n                            if (mask instanceof _PdfImage) {\n                                sw = mask.width;\n                                sh = mask.height;\n                                alphaBuf = new Uint8ClampedArray(sw * sh);\n                                mask.numComps = 1;\n                                mask._fillGrayBuffer(alphaBuf);\n                                for (i = 0, ii = sw * sh; i < ii; ++i) {\n                                    alphaBuf[i] = 255 - alphaBuf[i];\n                                }\n                                if (sw !== width || sh !== height) {\n                                    alphaBuf = this._resizeImageMask(alphaBuf, mask.bpc, sw, sh, width, height);\n                                }\n                            }\n                            else if (Array.isArray(mask)) {\n                                alphaBuf = new Uint8ClampedArray(width * height);\n                                numComps = this.numComps;\n                                for (i = 0, ii = width * height; i < ii; ++i) {\n                                    opacity = 0;\n                                    imageOffset = i * numComps;\n                                    for (j = 0; j < numComps; ++j) {\n                                        color = image[imageOffset + j];\n                                        maskOffset = j * 2;\n                                        if (color < mask[maskOffset] || color > mask[maskOffset + 1]) {\n                                            opacity = 255;\n                                            break;\n                                        }\n                                    }\n                                    alphaBuf[i] = opacity;\n                                }\n                            }\n                            else {\n                                throw new _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_5__.FormatError('Unknown mask format.');\n                            }\n                        }\n                        _a.label = 3;\n                    case 3:\n                        if (alphaBuf) {\n                            for (i = 0, j = 3, ii = width * actualHeight; i < ii; ++i, j += 4) {\n                                rgbaBuf[j] = alphaBuf[i];\n                            }\n                        }\n                        else {\n                            for (i = 0, j = 3, ii = width * actualHeight; i < ii; ++i, j += 4) {\n                                rgbaBuf[j] = 255;\n                            }\n                        }\n                        return [2 /*return*/];\n                }\n            });\n        });\n    };\n    /**\n     * Reverses matte preblend from smask+matte to restore unblended RGB.\n     *\n     * @private\n     * @param {Uint8ClampedArray} buffer RGBA buffer in-place.\n     * @param {number} width Image width.\n     * @param {number} height Effective height to process.\n     * @returns {void} nothing.\n     */\n    _PdfImage.prototype._undoPreblend = function (buffer, width, height) {\n        var matte = this.smask && this.smask.matte; // eslint-disable-line\n        if (!matte) {\n            return;\n        }\n        var matteRgb = this.colorSpace._getRgb(matte, 0); // eslint-disable-line\n        var matteR = matteRgb[0];\n        var matteG = matteRgb[1];\n        var matteB = matteRgb[2];\n        var length = width * height * 4;\n        for (var i = 0; i < length; i += 4) {\n            var alpha = buffer[i + 3];\n            if (alpha === 0) {\n                buffer[i] = 255;\n                buffer[i + 1] = 255;\n                buffer[i + 2] = 255;\n                continue;\n            }\n            var k = 255 / alpha;\n            buffer[i] = (buffer[i] - matteR) * k + matteR;\n            buffer[i + 1] = (buffer[i + 1] - matteG) * k + matteG;\n            buffer[i + 2] = (buffer[i + 2] - matteB) * k + matteB;\n        }\n    };\n    /**\n     * Fills a gray buffer from the image data.\n     *\n     * @private\n     * @param {Uint8ClampedArray} buffer Output grayscale buffer .\n     * @returns {Promise<void>} Resolves when gray values are computed.\n     * @throws {FormatError} If called for non-grayscale images.\n     */\n    _PdfImage.prototype._fillGrayBuffer = function (buffer) {\n        return __awaiter(this, void 0, void 0, function () {\n            var numComps, width, height, bpc, rowBytes, imgArray, comps, i, length, scale;\n            return __generator(this, function (_a) {\n                switch (_a.label) {\n                    case 0:\n                        numComps = this.numComps;\n                        if (numComps !== 1) {\n                            throw new _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_5__.FormatError(\"Reading gray scale from a color image: \" + numComps);\n                        }\n                        width = this.width;\n                        height = this.height;\n                        bpc = this.bpc;\n                        rowBytes = (width * numComps * bpc + 7) >> 3;\n                        return [4 /*yield*/, this._getImageBytes(height * rowBytes, { internal: true })];\n                    case 1:\n                        imgArray = _a.sent();\n                        comps = this._getComponents(imgArray);\n                        if (bpc === 1) {\n                            length = width * height;\n                            if (this.needsDecode) {\n                                for (i = 0; i < length; ++i) {\n                                    buffer[i] = (comps[i] - 1) & 255;\n                                }\n                            }\n                            else {\n                                for (i = 0; i < length; ++i) {\n                                    buffer[i] = -comps[i] & 255;\n                                }\n                            }\n                            return [2 /*return*/];\n                        }\n                        if (this.needsDecode) {\n                            this._decodeBuffer(comps);\n                        }\n                        length = width * height;\n                        scale = 255 / ((1 << bpc) - 1);\n                        for (i = 0; i < length; ++i) {\n                            buffer[i] = scale * comps[i];\n                        }\n                        return [2 /*return*/];\n                }\n            });\n        });\n    };\n    /**\n     * Composes a bitmap from raw pixel data using canvas, and returns encoded bytes.\n     *\n     * @private\n     * @param {any} kind Image kind `imageKind.rgba32BPP` or `imageKind.rgb24BPP`.\n     * @param {number} width Image width.\n     * @param {number} height Image height.\n     * @param {Uint8ClampedArray} src Pixel data.\n     * @returns {Promise<any>} Encoded image bytes.\n     * @throws {Error} If no canvas is available from the callback.\n     */\n    _PdfImage.prototype._createBitmap = function (kind, width, height, src) {\n        return __awaiter(this, void 0, void 0, function () {\n            var imgData, canvas, ctx, format, buffer, base64, bytes;\n            return __generator(this, function (_a) {\n                if (this._canvasRenderCallback) {\n                    canvas = this._canvasRenderCallback.canvas;\n                }\n                else {\n                    throw new Error('canvas is not defined');\n                }\n                canvas.width = width;\n                canvas.height = height;\n                ctx = canvas.getContext('2d');\n                if (kind === _image_utils__WEBPACK_IMPORTED_MODULE_4__.imageKind.rgba32BPP) {\n                    imgData = new ImageData(src, width, height);\n                }\n                else {\n                    imgData = ctx.createImageData(width, height);\n                    (0,_image_utils__WEBPACK_IMPORTED_MODULE_4__._convertToRGBA)(kind, src, new Uint32Array(imgData.data.buffer), width, height, this.needsDecode);\n                }\n                if (typeof (this._options) !== 'undefined') {\n                    this._processImageRedaction(imgData);\n                }\n                ctx.putImageData(imgData, 0, 0);\n                format = this._findImageFormat(this._imageFormat);\n                buffer = canvas.toDataURL(format);\n                base64 = buffer.split(',')[1];\n                bytes = (0,_utils__WEBPACK_IMPORTED_MODULE_8__._base64ToUint8Array)(base64);\n                return [2 /*return*/, bytes];\n            });\n        });\n    };\n    /**\n     * Returns a transferable image handle if the environment provides one.\n     *\n     * @private\n     * @returns {any} Transferable bitmap or `null` if not available.\n     */\n    _PdfImage.prototype._getTransferableImage = function () {\n        return null;\n    };\n    /**\n     * Wraps a transferable bitmap into an image-like object for direct consumption.\n     *\n     * @private\n     * @param {number} width Image width.\n     * @param {number} height Image height.\n     * @returns {any} Image wrapper with `bitmap`, `width`, `height`, and `interpolate` or `null` if not available.\n     */\n    _PdfImage.prototype._getImage = function (width, height) {\n        var bitmap = this._getTransferableImage(); // eslint-disable-line \n        if (!bitmap) {\n            return null;\n        }\n        return {\n            data: null,\n            width: width,\n            height: height,\n            bitmap: bitmap,\n            interpolate: this.interpolate\n        };\n    };\n    /**\n     * Reads and returns image bytes, optionally decoding JPX via worker/processor or returning Flate/DecodeStream bytes.\n     *\n     * @private\n     * @param {number} length Number of bytes to read.\n     * @param {any} options Decode and draw options.\n     * @returns {Promise<Uint8Array>} Raw or decoded image bytes.\n     */\n    _PdfImage.prototype._getImageBytes = function (length, _a) {\n        var drawWidth = _a.drawWidth, drawHeight = _a.drawHeight, _b = _a.forceRGBA, forceRGBA = _b === void 0 ? false : _b, _c = _a.forceRGB, forceRGB = _c === void 0 ? false : _c, _d = _a.internal, internal = _d === void 0 ? false : _d;\n        return __awaiter(this, void 0, void 0, function () {\n            var data, imageBytes, platform, imagedata;\n            return __generator(this, function (_e) {\n                switch (_e.label) {\n                    case 0:\n                        this.image.reset();\n                        this.image.drawWidth = drawWidth || this.width;\n                        this.image.drawHeight = drawHeight || this.height;\n                        this.image.forceRGBA = !!forceRGBA;\n                        this.image.forceRGB = !!forceRGB;\n                        imageBytes = this._getImageData(length, this);\n                        if (!(this.image instanceof _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_5__._PdfJpxStream)) return [3 /*break*/, 2];\n                        platform = void 0;\n                        if (this._canvasRenderCallback) {\n                            platform = this._canvasRenderCallback.applicationPlatform;\n                        }\n                        imagedata = new _import_decode_image__WEBPACK_IMPORTED_MODULE_2__._PdfImageProcessor();\n                        return [4 /*yield*/, imagedata._decodeImage(imageBytes, this.image, platform)];\n                    case 1:\n                        data = _e.sent();\n                        return [2 /*return*/, data];\n                    case 2:\n                        if (internal || this.image instanceof _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_5__._PdfDecodeStream) {\n                            return [2 /*return*/, imageBytes];\n                        }\n                        return [2 /*return*/, new Uint8Array(imageBytes)];\n                }\n            });\n        });\n    };\n    /**\n     * Applies region-based redaction to the canvas image data in-place.\n     *\n     * @private\n     * @param {any} canvasImgData Canvas `ImageData` to redact.\n     * @returns {void} nothing.\n     */\n    _PdfImage.prototype._processImageRedaction = function (canvasImgData) {\n        var pt = 1.3333;\n        var bounds = this._bounds;\n        var options = this._options;\n        var page = this._page;\n        var imageBoundInPx = { x: bounds[0], y: bounds[1], width: bounds[2], height: bounds[3] };\n        imageBoundInPx.x *= pt;\n        imageBoundInPx.y *= pt;\n        imageBoundInPx.width *= pt;\n        imageBoundInPx.height *= pt;\n        var isIntersect = false;\n        var rotation = page.rotation;\n        var rect;\n        for (var i = 0; i < options.length; i++) {\n            var redactionBounds = options[i].bounds;\n            var intersectBounds = this._intersect({ x: redactionBounds.x * pt, y: redactionBounds.y * pt,\n                width: redactionBounds.width * pt, height: redactionBounds.height * pt }, imageBoundInPx);\n            if (intersectBounds !== null && imageBoundInPx.height > 0) {\n                isIntersect = true;\n                this._isIntersect = isIntersect;\n                if (rotation !== _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_5__.PdfRotationAngle.angle0 && page.cropBox === page.mediaBox) {\n                    switch (rotation) {\n                        case _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_5__.PdfRotationAngle.angle180:\n                            {\n                                intersectBounds = ({ x: (page.size.width * pt) - intersectBounds.x - intersectBounds.width,\n                                    y: (page.size.height * pt) - intersectBounds.y - intersectBounds.height,\n                                    width: intersectBounds.width, height: intersectBounds.height });\n                                var mapX = (page.size.width - (bounds[2] + bounds[0]) - bounds[0]) * pt;\n                                var mapY = (page.size.height - (bounds[3] + bounds[1]) - bounds[1]) * pt;\n                                rect = ({ x: (intersectBounds.x - (imageBoundInPx.x + mapX)) * canvasImgData.Width / imageBoundInPx.width,\n                                    y: (intersectBounds.y - (imageBoundInPx.y + mapY)) * canvasImgData.Height / imageBoundInPx.height,\n                                    width: intersectBounds.width * canvasImgData.Width / imageBoundInPx.width,\n                                    height: intersectBounds.height * canvasImgData.Height / imageBoundInPx.height });\n                            }\n                            break;\n                        case _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_5__.PdfRotationAngle.angle270:\n                            {\n                                intersectBounds = ({ x: ((page.size.width * pt) - intersectBounds.y - intersectBounds.height),\n                                    y: intersectBounds.x, width: intersectBounds.height,\n                                    height: intersectBounds.width });\n                                var mappingX = (((page.size.width - (bounds[1] + bounds[2])) - bounds[0]) * pt);\n                                rect = ({ x: (intersectBounds.x - (imageBoundInPx.x + mappingX)) * canvasImgData.width / imageBoundInPx.height,\n                                    y: ((intersectBounds.y - (imageBoundInPx.x)) * canvasImgData.Height / imageBoundInPx.width),\n                                    width: intersectBounds.width * canvasImgData.Width / imageBoundInPx.height,\n                                    height: intersectBounds.height * canvasImgData.height / imageBoundInPx.width });\n                            }\n                            break;\n                        case _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_5__.PdfRotationAngle.angle90:\n                            {\n                                intersectBounds = ({ x: intersectBounds.y, y: ((page.size.height * pt) - intersectBounds.x -\n                                        intersectBounds.width), width: intersectBounds.height, height: intersectBounds.width });\n                                var mappingY = (((page.size.height - (bounds[0] + bounds[2])) - bounds[1]) * pt);\n                                rect = ({ x: (intersectBounds.x - imageBoundInPx.y) * canvasImgData.Width / imageBoundInPx.height,\n                                    y: ((intersectBounds.y - (imageBoundInPx.y + mappingY)) * canvasImgData.Height / imageBoundInPx.width),\n                                    width: intersectBounds.width * canvasImgData.Width / imageBoundInPx.height, height: intersectBounds.\n                                        height * canvasImgData.Height / imageBoundInPx.width });\n                            }\n                            break;\n                        default:\n                            rect = ({ x: (intersectBounds.x - imageBoundInPx.x) * canvasImgData.width / imageBoundInPx.width,\n                                y: (intersectBounds.y - imageBoundInPx.y) * canvasImgData.height / imageBoundInPx.height,\n                                width: intersectBounds.width * canvasImgData.Width / imageBoundInPx.width,\n                                height: intersectBounds.height * canvasImgData.Height / imageBoundInPx.height });\n                            break;\n                    }\n                }\n                else {\n                    rect = ({ x: (intersectBounds.x - imageBoundInPx.x) * canvasImgData.width / imageBoundInPx.width,\n                        y: (intersectBounds.y - imageBoundInPx.y) * canvasImgData.height / imageBoundInPx.height,\n                        width: intersectBounds.width * canvasImgData.width / imageBoundInPx.width,\n                        height: intersectBounds.height * canvasImgData.height / imageBoundInPx.height });\n                }\n                this._applyRedaction(canvasImgData, rect.x, rect.y, rect.width, rect.height);\n            }\n        }\n    };\n    /**\n     * Computes the intersection rectangle of two rectangles.\n     *\n     * @private\n     * @param {Rectangle} a First rectangle.\n     * @param {Rectangle} b Second rectangle.\n     * @returns {any} The intersection rectangle or `null` when disjoint.\n     */\n    _PdfImage.prototype._intersect = function (a, b) {\n        var x1 = Math.max(a.x, b.x);\n        var x2 = Math.min(a.x + a.width, b.x + b.width);\n        var y1 = Math.max(a.y, b.y);\n        var y2 = Math.min(a.y + a.height, b.y + b.height);\n        if (x2 >= x1 && y2 >= y1) {\n            return { x: x1, y: y1, width: x2 - x1, height: y2 - y1 };\n        }\n        return null;\n    };\n    /**\n     * Applies a white fill over the specified rectangle on the image data.\n     *\n     * @private\n     * @param {ImageData} imageData Canvas image data to modify.\n     * @param {number} x Left position.\n     * @param {number} y Top position.\n     * @param {number} w Width of the rectangle.\n     * @param {number} h Height of the rectangle.\n     * @returns {void} nothing.\n     */\n    _PdfImage.prototype._applyRedaction = function (imageData, x, y, w, h) {\n        var width = imageData.width, height = imageData.height, data = imageData.data;\n        var x0 = Math.max(0, Math.floor(x));\n        var y0 = Math.max(0, Math.floor(y));\n        var x1 = Math.min(width, Math.ceil(x + w));\n        var y1 = Math.min(height, Math.ceil(y + h));\n        for (var row = y0; row < y1; row++) {\n            var i = (row * width + x0) * 4;\n            for (var col = x0; col < x1; col++) {\n                data[i] = 255;\n                data[i + 1] = 255;\n                data[i + 2] = 255;\n                data[i + 3] = 255;\n                i += 4;\n            }\n        }\n    };\n    return _PdfImage;\n}());\n\n/**\n * Represents the application platform type.\n *\n * ```typescript\n * // Load an existing PDF document\n * let document: PdfDocument = new PdfDocument(data);\n * // Define a canvas render callback that returns a canvas element and the application platform.\n * const canvasRenderCallback = (): {canvas: any, applicationPlatform: ApplicationPlatform} => {\n *     const canvas = document.createElement('canvas');\n *     return { canvas: canvas, applicationPlatform: ApplicationPlatform.vue };\n * };\n * // Initialize a new instance of the `PdfDataExtractor` class\n * let extractor: PdfDataExtractor = new PdfDataExtractor(document, callBack: canvasRenderCallback);\n * // Extract collection of `PdfEmbeddedImage` from the PDF document.\n * let imageInfoCollection: PdfEmbeddedImage[] = extractor.extractImages({ startPageIndex: 0, endPageIndex: document.pageCount - 1});\n * let imageInfo: PdfEmbeddedImage = imageInfoCollection[0];\n * let data: Uint8Array = imageInfo.data;\n * // Destroy the documents\n * document.destroy();\n */\nvar ApplicationPlatform;\n(function (ApplicationPlatform) {\n    /**\n     * Specifies the `typeScript` platform.\n     */\n    ApplicationPlatform[\"typescript\"] = \"typescript\";\n    /**\n     * Specifies the `javascript` platform.\n     */\n    ApplicationPlatform[\"javascript\"] = \"javascript\";\n    /**\n     * Specifies the `angular` platform.\n     */\n    ApplicationPlatform[\"angular\"] = \"angular\";\n    /**\n     * Specifies the `react` platform.\n     */\n    ApplicationPlatform[\"react\"] = \"react\";\n    /**\n     * Specifies the `vue` platform.\n     */\n    ApplicationPlatform[\"vue\"] = \"vue\";\n    /**\n     * Specifies the `aspnetcore` platform.\n     */\n    ApplicationPlatform[\"aspnetcore\"] = \"aspnetcore\";\n    /**\n     * Specifies the `aspnetmvc` platform.\n     */\n    ApplicationPlatform[\"aspnetmvc\"] = \"aspnetmvc\";\n})(ApplicationPlatform || (ApplicationPlatform = {}));\n\n\n//# sourceURL=webpack://@syncfusion/ej2-documents-vue-samples/./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/image-extraction/image.js?");

/***/ })

}]);