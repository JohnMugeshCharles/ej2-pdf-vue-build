"use strict";
/*
 * ATTENTION: The "eval" devtool has been used (maybe by default in mode: "development").
 * This devtool is neither made for production nor for readable output files.
 * It uses "eval()" calls to create a separate source file in the browser devtools.
 * If you are trying to read the output file, select a different devtool (https://webpack.js.org/configuration/devtool/)
 * or disable the default devtool with "devtool: false".
 * If you are looking for production-ready output files, see mode: "production" (https://webpack.js.org/configuration/mode/).
 */
(self["webpackChunk_syncfusion_ej2_documents_vue_samples"] = self["webpackChunk_syncfusion_ej2_documents_vue_samples"] || []).push([["chunk-vendors-node_modules_syncfusion_ej2-pdf-data-extract_src_pdf-data-extract_core_utils_js-3feea4-72cfebef"],{

/***/ "./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/utils.js":
/*!******************************************************************************************!*\
  !*** ./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/utils.js ***!
  \******************************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   _addFontResources: () => (/* binding */ _addFontResources),\n/* harmony export */   _base64ToUint8Array: () => (/* binding */ _base64ToUint8Array),\n/* harmony export */   _decodeEncodedText: () => (/* binding */ _decodeEncodedText),\n/* harmony export */   _getLiteralString: () => (/* binding */ _getLiteralString),\n/* harmony export */   _getXObject: () => (/* binding */ _getXObject),\n/* harmony export */   _getXObjectResources: () => (/* binding */ _getXObjectResources),\n/* harmony export */   _hexToChar: () => (/* binding */ _hexToChar),\n/* harmony export */   _ignoreEscapeSequence: () => (/* binding */ _ignoreEscapeSequence),\n/* harmony export */   _isArrayEqual: () => (/* binding */ _isArrayEqual),\n/* harmony export */   _parseEncodedText: () => (/* binding */ _parseEncodedText),\n/* harmony export */   _parseEscapedText: () => (/* binding */ _parseEscapedText),\n/* harmony export */   _skipEscapeSequence: () => (/* binding */ _skipEscapeSequence)\n/* harmony export */ });\n/* harmony import */ var _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @syncfusion/ej2-pdf */ \"./node_modules/@syncfusion/ej2-pdf/index.js\");\n/* harmony import */ var _text_extraction_font_structure__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./text-extraction/font-structure */ \"./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/text-extraction/font-structure.js\");\n/* harmony import */ var _graphic_state__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./graphic-state */ \"./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/graphic-state.js\");\n/* harmony import */ var _enum__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./enum */ \"./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/enum.js\");\n/* harmony import */ var _content_parser_helper__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./content-parser-helper */ \"./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/content-parser-helper.js\");\n/* harmony import */ var _image_extraction_image_structure__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./image-extraction/image-structure */ \"./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/image-extraction/image-structure.js\");\n\n\n\n\n\n\n/**\n * Removes escape sequences from a text string and returns the cleaned text.\n *\n * @private\n * @param {string} text - The string to process.\n * @returns {string} The processed string without escape sequences.\n */\nfunction _ignoreEscapeSequence(text) {\n    var index = -1;\n    do {\n        index = text.indexOf('\\\\', index + 1);\n        if (index >= 0) {\n            if (text.length > index + 1) {\n                var nextLiteral = text[index + 1];\n                if (nextLiteral === '\\\\' || nextLiteral === '(' || nextLiteral === ')') {\n                    text = text.slice(0, index) + text.slice(index + 1);\n                }\n            }\n            else {\n                text = text.slice(0, index) + text.slice(index + 1);\n                index = -1;\n            }\n        }\n    } while (index >= 0);\n    return text;\n}\n/**\n * Adds font resources from a PDF dictionary into a collection.\n *\n * @private\n * @param {_PdfDictionary} dictionary - PDF Dictionary containing font resources.\n * @param {_PdfCrossReference} crossReference - The cross-reference of the PDF document.\n * @returns {Map<string, _FontStructure>} A map of font structure objects.\n */\nfunction _addFontResources(dictionary, crossReference) {\n    var font = dictionary.get('Font');\n    var fontCollection = new Map();\n    if (typeof (font) !== 'undefined' && font !== null) {\n        font.forEach(function (key, value) {\n            var fontDictionary = crossReference._fetch(value);\n            var fontstruct = new _text_extraction_font_structure__WEBPACK_IMPORTED_MODULE_1__._FontStructure(fontDictionary, crossReference);\n            fontCollection.set(key.toString(), fontstruct);\n        });\n    }\n    return fontCollection;\n}\n/**\n * Retrieves XObject resources from a PDF dictionary and their associated cross-references.\n *\n * @private\n * @param {_PdfDictionary} resources - The resources dictionary from a PDF page.\n * @param {_PdfCrossReference} crossReference - The cross-reference of the PDF document.\n * @param { _TextProcessingMode } [mode] - Optional processing mode that controls how XObjects are interpreted\n * @param { PdfPage } page - The current PDF page, used when constructing image structures.\n * @returns {Map<string, any>} A map of XObject resources.\n */\nfunction _getXObjectResources(resources, crossReference, mode, page) {\n    var xObjectCollection = new Map(); // eslint-disable-line\n    if (resources && resources.has('XObject')) {\n        var xObjects = resources.get('XObject');\n        xObjects.forEach(function (key, value) {\n            if (value instanceof _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__._PdfReference) {\n                var xobject = crossReference._fetch(value); // eslint-disable-line\n                if (xobject !== null && typeof (xobject) !== 'undefined') {\n                    var subtype = xobject.dictionary.get('Subtype').name;\n                    switch (mode) {\n                        case _enum__WEBPACK_IMPORTED_MODULE_3__._TextProcessingMode.imageExtraction:\n                            if (subtype === 'Image') {\n                                var imageStruct = new _image_extraction_image_structure__WEBPACK_IMPORTED_MODULE_5__._ImageStructure(xobject, crossReference, page);\n                                xObjectCollection.set(key, imageStruct);\n                            }\n                            break;\n                        case _enum__WEBPACK_IMPORTED_MODULE_3__._TextProcessingMode.imageRedaction:\n                            if (subtype === 'Image') {\n                                var imageStruct = new _image_extraction_image_structure__WEBPACK_IMPORTED_MODULE_5__._ImageStructure(xobject, crossReference, page);\n                                imageStruct._imageReference = value;\n                                xObjectCollection.set(key, imageStruct);\n                            }\n                            else if (subtype === 'Form') {\n                                xObjectCollection.set(key, xobject);\n                            }\n                            break;\n                        default:\n                            if (subtype === 'Form') {\n                                xObjectCollection.set(key, xobject);\n                            }\n                            break;\n                    }\n                }\n            }\n        });\n    }\n    return xObjectCollection;\n}\n/**\n * Converts a hexadecimal string to its equivalent character representation.\n *\n * @private\n * @param {string} hex - The hexadecimal string to convert.\n * @returns {string} The resulting string of characters.\n */\nfunction _hexToChar(hex) {\n    if (hex.startsWith('0x')) {\n        hex = hex.slice(2);\n    }\n    hex = hex.replace(/\\s+/g, '');\n    var result = '';\n    for (var i = 0; i < hex.length; i += 2) {\n        var byte = hex.slice(i, i + 2);\n        var charCode = parseInt(byte, 16);\n        result += String.fromCharCode(charCode);\n    }\n    return result;\n}\n/**\n * Skips recognized escape sequences in a text string.\n *\n * @private\n * @param {string} text - The string containing escape sequences.\n * @returns {string} A new string with escape sequences removed or handled appropriately.\n */\nfunction _skipEscapeSequence(text) {\n    var isDefault = false;\n    var replaceText = text;\n    var isAlphabetic = false;\n    while (replaceText.indexOf('\\\\') !== -1) {\n        isAlphabetic = false;\n        var i = replaceText.indexOf('\\\\');\n        if (i + 1 !== replaceText.length) {\n            var escapeSequence = replaceText.substring(i + 1, i + 2);\n            switch (escapeSequence) {\n                case 'a':\n                    text = text.replace(/\\\\a/g, '\\x07');\n                    break;\n                case '(':\n                    text = text.replace(/\\\\\\(/g, '(');\n                    break;\n                case ')':\n                    text = text.replace(/\\\\\\)/g, ')');\n                    break;\n                case 'b':\n                    text = text.replace(/\\\\b/g, '\\b');\n                    break;\n                case 'e':\n                    text = text.replace(/\\\\e/g, '\\\\e');\n                    isAlphabetic = true;\n                    break;\n                case 'f':\n                    text = text.replace(/\\\\f/g, '\\f');\n                    break;\n                case 'n':\n                    text = text.replace(/\\\\n/g, '\\n');\n                    break;\n                case 'r':\n                    text = text.replace(/\\\\r/g, '\\r');\n                    break;\n                case 't':\n                    text = text.replace(/\\\\t/g, '\\t');\n                    break;\n                case 'v':\n                    text = text.replace(/\\\\v/g, '\\v');\n                    break;\n                case \"'\": //eslint-disable-line\n                    text = text.replace(/\\\\'/g, \"'\"); //eslint-disable-line\n                    break;\n                default: {\n                    var charCode = escapeSequence.charCodeAt(0);\n                    if (charCode === 3) {\n                        text = text.replace(/\\\\/g, '\\\\');\n                        isDefault = true;\n                    }\n                    else if (charCode >= 127) {\n                        text = text.replace(/\\\\/g, '');\n                        isDefault = true;\n                    }\n                    else if ((charCode >= 65 && charCode <= 90) || (charCode >= 97 && charCode <= 122)) {\n                        replaceText = replaceText.slice(0, i) + replaceText.slice(i + 1);\n                        isAlphabetic = true;\n                    }\n                    else {\n                        var isUnrecognizedEscapeSequence = false;\n                        if (text.includes('\\\\')) {\n                            for (var c = 0; c < text.length - 1; c++) {\n                                var nextChar = text[c + 1];\n                                if (text[Number.parseInt(c.toString(), 10)] === '\\\\' && text[c + 1] === '\\\\') {\n                                    c++;\n                                }\n                                else if (text[Number.parseInt(c.toString(), 10)] === '\\\\' && /[A-Zdghijklmopqswyz]/.test(nextChar)) {\n                                    isUnrecognizedEscapeSequence = true;\n                                    break;\n                                }\n                            }\n                        }\n                        if (isUnrecognizedEscapeSequence) {\n                            text = text.replace(/\\\\/g, '');\n                        }\n                        else {\n                            text = _parseEscapedText(text);\n                        }\n                        isDefault = true;\n                    }\n                    break;\n                }\n            }\n            if (isDefault) {\n                break;\n            }\n            else if (!isAlphabetic) {\n                replaceText = text;\n            }\n        }\n        else {\n            break;\n        }\n    }\n    return text;\n}\n/**\n * Converts escape sequences in a string to their corresponding literal characters.\n *\n * @private\n * @param {string} text The input string containing escape sequences.\n *\n * @returns {string} The parsed string with escape sequences replaced by literal characters.\n */\nfunction _parseEscapedText(text) {\n    return text\n        .replace(/\\\\n/g, '\\n')\n        .replace(/\\\\r/g, '\\r')\n        .replace(/\\\\t/g, '\\t')\n        .replace(/\\\\\"/g, '\\\"') // eslint-disable-line\n        .replace(/\\\\'/g, \"'\") // eslint-disable-line\n        .replace(/\\\\</g, '<')\n        .replace(/\\\\>/g, '>')\n        .replace(/\\\\\\(/g, '(')\n        .replace(/\\\\\\)/g, ')')\n        .replace(/\\\\\\{/g, '{')\n        .replace(/\\\\\\}/g, '}')\n        .replace(/\\\\\\[/g, '[')\n        .replace(/\\\\\\]/g, ']')\n        .replace(/\\\\\\|/g, '|')\n        .replace(/\\\\\\*/g, '*')\n        .replace(/\\\\\\?/g, '?')\n        .replace(/\\\\\\-/g, \"-\") // eslint-disable-line\n        .replace(/\\\\\\+/g, '+')\n        .replace(/\\\\\\./g, '.')\n        .replace(/\\\\\\//g, '/')\n        .replace(/\\\\,/g, ',')\n        .replace(/\\\\:/g, ':')\n        .replace(/\\\\;/g, ';')\n        .replace(/\\\\=/g, '=')\n        .replace(/\\\\&/g, '&')\n        .replace(/\\\\%/g, '%')\n        .replace(/\\\\#/g, '#')\n        .replace(/\\\\!/g, '!')\n        .replace(/\\\\u([0-9A-Fa-f]{4})/g, function (_, p1) { return String.fromCharCode(parseInt(p1, 16)); }) // eslint-disable-line\n        .replace(/\\\\\\\\/g, '\\\\');\n}\n/**\n * Retrieves a literal string, decoding escape sequences and null characters.\n *\n * @private\n * @param {string} encodedText - The encoded string to decode.\n * @param {string} [encoding] - The encoding used in the text.\n * @returns {string} The decoded literal string.\n */\nfunction _getLiteralString(encodedText, encoding) {\n    var decodedText = encodedText;\n    var octalIndex = -1;\n    var limit = 3;\n    var temp = '';\n    while ((decodedText.indexOf('\\\\') !== -1 && (decodedText.indexOf('\\\\\\\\') === -1)) ||\n        decodedText.indexOf('\\0') ||\n        ((decodedText.indexOf('\\\\\\\\') !== -1) && encoding === 'Encoding')) {\n        var octalText = '';\n        if (decodedText.indexOf('\\\\', octalIndex + 1) >= 0) {\n            var nullPosition = decodedText.indexOf('\\0', octalIndex + 1);\n            octalIndex = decodedText.indexOf('\\\\', octalIndex + 1);\n            if (nullPosition > -1 && octalIndex > nullPosition) {\n                octalIndex = nullPosition;\n                limit = 2;\n            }\n        }\n        else {\n            octalIndex = decodedText.indexOf('\\0', octalIndex + 1);\n            if (octalIndex < 0) {\n                break;\n            }\n            limit = 2;\n        }\n        for (var i = octalIndex + 1; i <= octalIndex + limit; i++) {\n            if (i < decodedText.length) {\n                var val = parseInt(decodedText[Number.parseInt(i.toString(), 10)], 10);\n                if (!isNaN(val) && val <= 8) {\n                    octalText += decodedText[Number.parseInt(i.toString(), 10)];\n                }\n                else {\n                    octalText = '';\n                    break;\n                }\n            }\n            else {\n                octalText = '';\n            }\n        }\n        if (octalText !== '') {\n            var decimalValue = parseInt(octalText, 8);\n            temp = String.fromCharCode(decimalValue);\n            decodedText = decodedText.substring(0, octalIndex) + decodedText.substring(octalIndex + limit + 1);\n            decodedText = decodedText.substring(0, octalIndex) + temp + decodedText.substring(octalIndex);\n        }\n    }\n    if (decodedText.indexOf('\\\\') !== -1 && encoding === 'Encoding') {\n        if (decodedText.indexOf('\\\\\\\\') === -1) {\n            var initialLength = 0;\n            while ((decodedText.indexOf('\\\\\u0003') === -1 && decodedText.indexOf('\\\\') !== -1) && (decodedText.length !== initialLength)) {\n                initialLength = decodedText.length;\n                decodedText = _skipEscapeSequence(decodedText);\n            }\n        }\n    }\n    return decodedText;\n}\n/**\n * Decodes encoded text using the specified font structure, adjusting for encoding differences.\n *\n * @private\n * @param {string} encodedText - The encoded string to decode.\n * @param {_FontStructure} font - The font structure for decoding glyphs.\n * @param {string[]} inputText - An array of strings representing parts of the text.\n * @returns {string} The decoded text.\n */\nfunction _decodeEncodedText(encodedText, font, inputText) {\n    var key = '';\n    var isHex = false;\n    var decodedText; // eslint-disable-line\n    var tempString = '';\n    var decodedList = [];\n    var encoding = font._encoding;\n    var splittedText = inputText;\n    switch (encodedText[0]) {\n        case '(':\n            encodedText = encodedText.substring(1, encodedText.length - 1);\n            if (encodedText.indexOf('\\\\\\n') !== -1 || encodedText.indexOf('\\\\(') !== -1 || encodedText.indexOf('\\\\)') !== -1) {\n                if (encodedText.indexOf('\\\\\\n') !== -1) {\n                    encodedText = encodedText.replace(/\\\\\\n/g, '');\n                }\n                if (encodedText.indexOf('\\\\(') !== -1) {\n                    encodedText = encodedText.replace(/\\\\\\(/g, '(');\n                }\n                if (encodedText.indexOf('\\\\)') !== -1) {\n                    encodedText = encodedText.replace(/\\\\\\)/g, ')');\n                }\n            }\n            if (encodedText.indexOf('\\\\n') !== -1) {\n                encodedText = encodedText.replace(/\\\\n/g, '\\n');\n            }\n            if (encodedText.indexOf('\\\\r') !== -1) {\n                encodedText = encodedText.replace(/\\\\r/g, '\\r');\n            }\n            encodedText = _getLiteralString(encodedText, encoding);\n            encodedText = _skipEscapeSequence(encodedText);\n            decodedText = font._charsToGlyphs(encodedText);\n            for (var i = 0; i < decodedText.length; i++) {\n                key += decodedText[Number.parseInt(i.toString(), 10)]._unicode;\n            }\n            decodedText += key;\n            break;\n        case '[':\n            splittedText = inputText;\n            for (var i = 0; i < splittedText.length; i++) {\n                var input = splittedText[Number.parseInt(i.toString(), 10)];\n                if (input.indexOf('\\\\\\n') !== -1) {\n                    input = input.replace(/\\\\\\n/g, '');\n                }\n                if (input[0] === '<') {\n                    isHex = true;\n                    input = input.slice(1, -1);\n                }\n                else if (input[0] === '(') {\n                    input = input.slice(1, -1);\n                }\n                else if (input.length > 0) {\n                    input = input.replace('\\n', '');\n                    decodedList.push(input);\n                    continue;\n                }\n                if (isHex) {\n                    tempString = _hexToChar(input);\n                }\n                else {\n                    tempString = _getLiteralString(input, encoding);\n                    if (tempString.indexOf('\\\\') !== -1) {\n                        tempString = _skipEscapeSequence(tempString);\n                    }\n                }\n                decodedText = font._charsToGlyphs(tempString);\n                for (var i_1 = 0; i_1 < decodedText.length; i_1++) {\n                    key += decodedText[Number.parseInt(i_1.toString(), 10)]._unicode;\n                }\n            }\n            break;\n        case '<':\n            encodedText = encodedText.substring(1, encodedText.length - 1);\n            tempString = _hexToChar(encodedText);\n            decodedText = font._charsToGlyphs(tempString);\n            for (var i = 0; i < decodedText.length; i++) {\n                key += decodedText[Number.parseInt(i.toString(), 10)]._unicode;\n            }\n            decodedText += key;\n            break;\n    }\n    return key;\n}\n/**\n * Retrieves a content stream object for a specified XObject, processing it according to the provided mode.\n *\n * @private\n * @param {string[]} xObjectElement - The XObject elements to process.\n * @param {PdfPage} page - The PDF page to which the content stream belongs.\n * @param {Map<string, any>} xObjectCollection - A collection of XObject elements.\n * @param {_PdfContentParserHelper | PdfDataExtractor} data - The data extractor or content parser helper.\n * @param {_TextProcessingMode} [mode] - The mode of text processing.\n * @param {_GraphicState} [graphicState] - The current graphic state.\n * @returns {_PdfContentStream | void} The processed PDF content stream or void.\n */\nfunction _getXObject(xObjectElement, page, xObjectCollection, data, mode, graphicState) {\n    var xobject = xObjectElement[0].replace('/', '');\n    var array;\n    var contentParser;\n    var extractor;\n    if (data instanceof _content_parser_helper__WEBPACK_IMPORTED_MODULE_4__._PdfContentParserHelper) {\n        contentParser = data;\n    }\n    else {\n        extractor = data;\n    }\n    if (xObjectCollection.has(xobject)) {\n        var base = xObjectCollection.get(xobject); //eslint-disable-line\n        if (base) {\n            if (base instanceof _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__._PdfContentStream) {\n                array = new Uint8Array(base._bytes);\n            }\n            else if (base instanceof _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__._PdfBaseStream) {\n                array = base.getBytes();\n            }\n            if (array) {\n                var parser = new _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__._ContentParser(array);\n                var recordCollection = parser._readContent();\n                var childFontCollection = new Map();\n                var xObjectCollection_1 = new Map(); //eslint-disable-line\n                if (base.dictionary.has('Resources')) {\n                    var childResources = base.dictionary.get('Resources');\n                    childFontCollection = _addFontResources(childResources, childResources._crossReference);\n                    xObjectCollection_1 = _getXObjectResources(childResources, childResources._crossReference);\n                }\n                var state = void 0;\n                if (typeof (mode) !== 'undefined') {\n                    if (base.dictionary.has('Matrix')) {\n                        var currentState = graphicState._state._clone();\n                        state = new _graphic_state__WEBPACK_IMPORTED_MODULE_2__._GraphicState(currentState);\n                        var matrix = base.dictionary.get('Matrix');\n                        if (matrix) {\n                            state._transform(matrix);\n                        }\n                    }\n                    else {\n                        state = graphicState;\n                    }\n                }\n                if (mode === _enum__WEBPACK_IMPORTED_MODULE_3__._TextProcessingMode.textLineExtraction || mode === _enum__WEBPACK_IMPORTED_MODULE_3__._TextProcessingMode.textExtraction) {\n                    contentParser._processRecordCollection(recordCollection, page, childFontCollection, xObjectCollection_1, state);\n                }\n                else if (mode === _enum__WEBPACK_IMPORTED_MODULE_3__._TextProcessingMode.redaction) {\n                    var pdfStream = contentParser._processRecordCollection(recordCollection, page, // eslint-disable-line\n                    childFontCollection, xObjectCollection_1, state);\n                    return pdfStream;\n                }\n                else {\n                    extractor._renderTextAsLayOut(recordCollection, page, childFontCollection, xObjectCollection_1);\n                }\n            }\n        }\n    }\n}\n/**\n * Parses encoded text and returns both the decoded string list and width table.\n *\n * @private\n * @param {string} encodedText - The encoded text string to be parsed.\n * @param {_FontStructure} font - The font structure used to map encoded characters to glyphs.\n * @returns {object} An object containing `decodedStrings`, a list of decoded strings, and `charWidths`, a list of character widths.\n */\nfunction _parseEncodedText(encodedText, font) {\n    var decodedList = [];\n    var key = '';\n    var decodedText; // eslint-disable-line\n    var widths = [];\n    var widthtable = [];\n    var tempString = '';\n    var isRotatePage = false;\n    switch (encodedText[0]) {\n        case '(':\n            encodedText = encodedText.substring(1, encodedText.length - 1);\n            if (encodedText.indexOf('\\\\\\n') !== -1 || encodedText.indexOf('\\\\(') !== -1 || encodedText.indexOf('\\\\)') !== -1) {\n                if (encodedText.indexOf('\\\\\\n') !== -1) {\n                    encodedText = encodedText.replace(/\\\\\\n/g, '');\n                }\n                if (encodedText.indexOf('\\\\(') !== -1) {\n                    encodedText = encodedText.replace(/\\\\\\(/g, '(');\n                }\n                if (encodedText.indexOf('\\\\)') !== -1) {\n                    encodedText = encodedText.replace(/\\\\\\)/g, ')');\n                }\n            }\n            if (encodedText.indexOf('\\\\n') !== -1) {\n                encodedText = encodedText.replace(/\\\\n/g, '\\n');\n            }\n            if (encodedText.indexOf('\\\\r') !== -1) {\n                encodedText = encodedText.replace(/\\\\r/g, '\\r');\n            }\n            encodedText = _getLiteralString(encodedText);\n            encodedText = _skipEscapeSequence(encodedText);\n            if (!isRotatePage) {\n                decodedText = font._charsToGlyphs(encodedText);\n                for (var i = 0; i < decodedText.length; i++) {\n                    if (decodedText[Number.parseInt(i.toString(), 10)]._unicode.length > 1) {\n                        key += decodedText[Number.parseInt(i.toString(), 10)]._unicode;\n                        for (var j = 0; j < decodedText[Number.parseInt(i.toString(), 10)]._unicode.length; j++) {\n                            widths.push(decodedText[Number.parseInt(i.toString(), 10)]._width);\n                        }\n                    }\n                    else {\n                        key += decodedText[Number.parseInt(i.toString(), 10)]._unicode;\n                        widths.push(decodedText[Number.parseInt(i.toString(), 10)]._width);\n                    }\n                }\n            }\n            else {\n                key = encodedText;\n            }\n            key += 's';\n            decodedList.push(key);\n            widthtable.push(widths);\n            break;\n        case '[':\n            encodedText = encodedText.replace(/\\\\\\n/g, '');\n            if (encodedText.length > 1) {\n                encodedText = encodedText.substring(1, encodedText.length - 1);\n            }\n            while (encodedText.length > 0) {\n                var isHex = false;\n                var listElement = '';\n                var textStart = encodedText.indexOf('(');\n                var textEnd = encodedText.indexOf(')');\n                for (var j = textEnd + 1; j < encodedText.length; j++) {\n                    if (encodedText[Number.parseInt(j.toString(), 10)] === '(') {\n                        break;\n                    }\n                    else if (encodedText[Number.parseInt(j.toString(), 10)] === ')') {\n                        textEnd = j;\n                        break;\n                    }\n                }\n                var textHexStart = encodedText.indexOf('<');\n                var textHexEnd = encodedText.indexOf('>');\n                if (textHexStart < textStart && textHexStart > -1) {\n                    textStart = textHexStart;\n                    textEnd = textHexEnd;\n                    isHex = true;\n                }\n                if (textStart < 0) {\n                    textStart = encodedText.indexOf('<');\n                    textEnd = encodedText.indexOf('>');\n                    if (textStart >= 0) {\n                        isHex = true;\n                    }\n                    else {\n                        decodedList.push(encodedText);\n                        break;\n                    }\n                }\n                if (textEnd < 0 && encodedText.length > 0) {\n                    decodedList.push(encodedText);\n                    break;\n                }\n                else if (textEnd > 0) {\n                    while (encodedText[textEnd - 1] === '\\\\') {\n                        if (textEnd - 1 > 0 && encodedText[textEnd - 2] === '\\\\') {\n                            break;\n                        }\n                        var nextEnd = encodedText.indexOf(')', textEnd + 1);\n                        if (nextEnd >= 0) {\n                            textEnd = nextEnd;\n                        }\n                        else {\n                            break;\n                        }\n                    }\n                }\n                if (textStart !== 0) {\n                    listElement = encodedText.substring(0, textStart);\n                    decodedList.push(listElement);\n                }\n                var tempString_1 = encodedText.substring(textStart + 1, textEnd);\n                if (isHex) {\n                    tempString_1 = _hexToChar(tempString_1);\n                }\n                else {\n                    tempString_1 = _getLiteralString(tempString_1);\n                    if (tempString_1.indexOf('\\\\') !== -1) {\n                        tempString_1 = _skipEscapeSequence(tempString_1);\n                    }\n                }\n                if (!isRotatePage) {\n                    decodedText = font._charsToGlyphs(tempString_1);\n                    for (var i = 0; i < decodedText.length; i++) {\n                        if (decodedText[Number.parseInt(i.toString(), 10)]._unicode.length > 1) {\n                            key += decodedText[Number.parseInt(i.toString(), 10)]._unicode;\n                            for (var j = 0; j < decodedText[Number.parseInt(i.toString(), 10)]._unicode.length; j++) {\n                                widths.push(decodedText[Number.parseInt(i.toString(), 10)]._width);\n                            }\n                        }\n                        else {\n                            key += decodedText[Number.parseInt(i.toString(), 10)]._unicode;\n                            widths.push(decodedText[Number.parseInt(i.toString(), 10)]._width);\n                        }\n                    }\n                }\n                else {\n                    key = tempString_1;\n                }\n                key += 's';\n                decodedList.push(key);\n                widthtable.push(widths);\n                key = '';\n                widths = [];\n                encodedText = encodedText.substring(textEnd + 1);\n            }\n            break;\n        case '<':\n            encodedText = encodedText.substring(1, encodedText.length - 1);\n            tempString = _hexToChar(encodedText);\n            if (!isRotatePage) {\n                decodedText = font._charsToGlyphs(tempString);\n                for (var i = 0; i < decodedText.length; i++) {\n                    key += decodedText[Number.parseInt(i.toString(), 10)]._unicode;\n                    widths.push(decodedText[Number.parseInt(i.toString(), 10)]._width);\n                }\n            }\n            else {\n                key = tempString;\n            }\n            key += 's';\n            decodedList.push(key);\n            widthtable.push(widths);\n            break;\n    }\n    return [decodedList, widthtable];\n}\n/**\n * Compare two arrays of numbers to determine if they are equal.\n *\n * This function checks if two arrays have the same length and\n * identical elements in the same order.\n *\n * @private\n * @param {number[]} arr1 - The first array to compare.\n * @param {number[]} arr2 - The second array to compare.\n * @returns {boolean} 'true' if the arrays are equal, otherwise 'false'.\n */\nfunction _isArrayEqual(arr1, arr2) {\n    if (arr1.length !== arr2.length) {\n        return false;\n    }\n    for (var i = 0, ii = arr1.length; i < ii; i++) {\n        if (arr1[Number.parseInt(i.toString(), 10)] !== arr2[Number.parseInt(i.toString(), 10)]) {\n            return false;\n        }\n    }\n    return true;\n}\n/**\n * Convert a Base64-encoded string into a Uint8Array of bytes.\n *\n * @private\n * @param {string} base64String - The Base64-encoded string to decode.\n * @returns {Uint8Array} A Uint8Array containing the decoded bytes.\n *\n */\nfunction _base64ToUint8Array(base64String) {\n    var binaryString = atob(base64String);\n    var len = binaryString.length;\n    var bytes = new Uint8Array(len);\n    for (var i = 0; i < len; i++) {\n        bytes[i] = binaryString.charCodeAt(i);\n    }\n    return bytes;\n}\n\n\n//# sourceURL=webpack://@syncfusion/ej2-documents-vue-samples/./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/utils.js?");

/***/ })

}]);