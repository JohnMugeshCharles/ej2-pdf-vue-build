"use strict";
/*
 * ATTENTION: The "eval" devtool has been used (maybe by default in mode: "development").
 * This devtool is neither made for production nor for readable output files.
 * It uses "eval()" calls to create a separate source file in the browser devtools.
 * If you are trying to read the output file, select a different devtool (https://webpack.js.org/configuration/devtool/)
 * or disable the default devtool with "devtool: false".
 * If you are looking for production-ready output files, see mode: "production" (https://webpack.js.org/configuration/mode/).
 */
(self["webpackChunk_syncfusion_ej2_documents_vue_samples"] = self["webpackChunk_syncfusion_ej2_documents_vue_samples"] || []).push([["chunk-vendors-node_modules_syncfusion_ej2-pdf_src_pdf_core_security_digital-signature_asn1_s"],{

/***/ "./node_modules/@syncfusion/ej2-pdf/src/pdf/core/security/digital-signature/asn1/syntax-verifier.js":
/*!**********************************************************************************************************!*\
  !*** ./node_modules/@syncfusion/ej2-pdf/src/pdf/core/security/digital-signature/asn1/syntax-verifier.js ***!
  \**********************************************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   _isGeneralCharacter: () => (/* binding */ _isGeneralCharacter),\n/* harmony export */   _isGraphicCharacter: () => (/* binding */ _isGraphicCharacter),\n/* harmony export */   _isPrintableCharacter: () => (/* binding */ _isPrintableCharacter),\n/* harmony export */   _validateDate: () => (/* binding */ _validateDate),\n/* harmony export */   _validateDateTime: () => (/* binding */ _validateDateTime),\n/* harmony export */   _validateDateTimeComponent: () => (/* binding */ _validateDateTimeComponent),\n/* harmony export */   _validateTime: () => (/* binding */ _validateTime),\n/* harmony export */   isNumericString: () => (/* binding */ isNumericString)\n/* harmony export */ });\n/**\n * Creates a function to validate dateTime components.\n *\n * @private\n * @param {string} unitName - The name of the dateTime unit (e.g., 'day', 'month').\n * @param {number} min - The minimum allowable value for this unit.\n * @param {number} max - The maximum allowable value for this unit.\n * @returns {(function(string, number): void)} A function that validates a dateTime component.\n * @throws {Error} Throws an error if the value is not an integer or is out of bounds.\n */\nfunction _validateDateTimeComponent(unitName, min, max) {\n    return function (dataType, value) {\n        if (!Number.isInteger(value)) {\n            throw new Error(\"Non-integral \" + unitName + \" supplied to \" + dataType + \".\");\n        }\n        if (value > max) {\n            throw new Error(\"Encountered \" + unitName + \" greater than \" + max + \" in \" + dataType + \".\");\n        }\n        if (value < min) {\n            throw new Error(\"Encountered \" + unitName + \" less than \" + min + \" in \" + dataType + \".\");\n        }\n    };\n}\n/**\n * Determines if a given character code represents a general character in the ASCII range.\n *\n * @private\n * @param {number} characterCode The character code to evaluate.\n * @returns {boolean} boolean - Returns true if the character code is in the ASCII range (0 - 127);\n * false otherwise.\n */\nfunction _isGeneralCharacter(characterCode) {\n    return (characterCode <= 0x7F);\n}\n/**\n * Evaluates whether the provided character code corresponds to a graphic character\n * within the standard ASCII range.\n *\n * @private\n * @param {number} characterCode The character code to evaluate.\n * @returns {boolean} - Returns true if the character code represents a graphic character;\n * false if it does not.\n */\nfunction _isGraphicCharacter(characterCode) {\n    return (characterCode >= 0x20 && characterCode <= 0x7E);\n}\n/**\n * Checks if the provided character code corresponds to a printable character.\n *\n * @private\n * @param {number} characterCode The character code to evaluate.\n * @returns {boolean} - Returns true if the character code is considered printable\n * according to the defined ranges and conditions; false otherwise.\n */\nfunction _isPrintableCharacter(characterCode) {\n    return ((characterCode >= 0x27 && characterCode <= 0x39 && characterCode !== 0x2A) // '()+,-./ AND 0 - 9 BUT NOT *\n        || (characterCode >= 0x41 && characterCode <= 0x5A) // A - Z\n        || (characterCode >= 0x61 && characterCode <= 0x7A) // a - z\n        || (characterCode === 0x20) // SPACE\n        || (characterCode === 0x3A) // :\n        || (characterCode === 0x3D) // =\n        || (characterCode === 0x3F) // ?\n    );\n}\n/**\n * Validates the given date components to ensure they form a legitimate date.\n *\n * @private\n * @param {string} dataType string - A descriptive label for the type of data being validated.\n * @param {number} year - The year part of the date, which must be a safe integer.\n * @param {number} month - The month part of the date, ranging from 0 (January) to 11 (December).\n * @param {number} date - The day part of the date, required to be a valid day in the given month.\n * @returns {void} This function does not return a value.\n * @throws Will throw an error if any components do not together make a valid date.\n */\nfunction _validateDate(dataType, year, month, date) {\n    if (!Number.isSafeInteger(year)) {\n        throw new Error(\"Invalid year in \" + dataType);\n    }\n    if (!Number.isSafeInteger(month)) {\n        throw new Error(\"Invalid month in \" + dataType);\n    }\n    if (!Number.isSafeInteger(date) || (date < 1)) {\n        throw new Error(\"Invalid day in \" + dataType);\n    }\n    switch (month) {\n        case 0: // January\n        case 2: // March\n        case 4: // May\n        case 6: // July\n        case 7: // August\n        case 9: // October\n        case 11: { // December\n            if (date > 31) {\n                throw new Error(\"Day > 31 encountered in \" + dataType + \" with 31-day month.\");\n            }\n            break;\n        }\n        // 30-day months\n        case 3: // April\n        case 5: // June\n        case 8: // September\n        case 10: { // November\n            if (date > 30) {\n                throw new Error(\"Day > 31 encountered in \" + dataType + \" with 30-day month.\");\n            }\n            break;\n        }\n        case 1: {\n            var isLeapYear = ((year % 4 === 0) && (year % 100 !== 0)) || (year % 400 === 0);\n            if (isLeapYear) {\n                if (date > 29) {\n                    throw new Error(\"Day > 29 encountered in \" + dataType + \" with month of February in leap year.\");\n                }\n            }\n            else if (date > 28) {\n                throw new Error(\"Day > 28 encountered in \" + dataType + \" with month of February and non leap year.\");\n            }\n            break;\n        }\n        default:\n            throw new Error(\"Invalid month in \" + dataType);\n    }\n}\n/**\n * Validates the given time components to ensure they form a legitimate time.\n *\n * @private\n * @param {string} dataType - A descriptive label for the type of data being validated.\n * @param {number} hours - The hour component of the time, which must be a safe integer from 0 to 23.\n * @param {number} minutes - The minute component of the time, which must be a safe integer from 0 to 59.\n * @param {number} seconds - The second component of the time, which must be a safe integer from 0 to 59.\n * @returns {void} This function does not return a value.\n * @throws Will throw an error if the time components do not form a valid time representation.\n */\nfunction _validateTime(dataType, hours, minutes, seconds) {\n    if (!Number.isSafeInteger(hours)) {\n        throw new Error(\"Invalid hours in \" + dataType);\n    }\n    if (!Number.isSafeInteger(minutes)) {\n        throw new Error(\"Invalid minutes in \" + dataType);\n    }\n    if (!Number.isSafeInteger(seconds) || (seconds < 0)) {\n        throw new Error(\"Invalid seconds in \" + dataType);\n    }\n    if (hours > 23) {\n        throw new Error(\"Hours > 23 encountered in \" + dataType + \".\");\n    }\n    if (minutes > 59) {\n        throw new Error(\"Minutes > 60 encountered in \" + dataType + \".\");\n    }\n    if (seconds > 59) {\n        throw new Error(\"Seconds > 60 encountered in \" + dataType + \".\");\n    }\n}\n/**\n * Validates a full date and time specification to ensure it is legitimate.\n *\n * @private\n * @param {string} dataType string - A descriptive label for the type of data being validated.\n * @param {number} year number - The year component of the date.\n * @param {number} month number - The month component of the date, 0-based (0 for January, 11 for December).\n * @param {number} date number - The day of the month.\n * @param {number} hours number - The hour component of the time.\n * @param {number} minutes number - The minute component of the time.\n * @param {number} seconds number - The second component of the time.\n * @returns {void} This function does not return a value.\n * @throws Will throw an error if any of the date or time components are invalid.\n */\nfunction _validateDateTime(dataType, year, month, date, hours, minutes, seconds) {\n    _validateDate(dataType, year, month, date);\n    _validateTime(dataType, hours, minutes, seconds);\n}\n/**\n * Determines if the given character code corresponds to a numeric character\n * or a space character in the ASCII range.\n *\n * @private\n * @param {number} characterCode number - The character code to evaluate.\n * @returns {boolean} - Returns true if the character code is a numeric character (0-9)\n * or a space; false otherwise.\n */\nfunction isNumericString(characterCode) {\n    return ((characterCode >= 0x30 && characterCode <= 0x39) || characterCode === 0x20);\n}\n\n\n//# sourceURL=webpack://@syncfusion/ej2-documents-vue-samples/./node_modules/@syncfusion/ej2-pdf/src/pdf/core/security/digital-signature/asn1/syntax-verifier.js?");

/***/ }),

/***/ "./node_modules/@syncfusion/ej2-pdf/src/pdf/core/security/digital-signature/asn1/utils.js":
/*!************************************************************************************************!*\
  !*** ./node_modules/@syncfusion/ej2-pdf/src/pdf/core/security/digital-signature/asn1/utils.js ***!
  \************************************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   _bufferToInteger: () => (/* binding */ _bufferToInteger),\n/* harmony export */   _convertBytesToText: () => (/* binding */ _convertBytesToText),\n/* harmony export */   _decodeSignedBigEndianInteger: () => (/* binding */ _decodeSignedBigEndianInteger),\n/* harmony export */   _decodeUnsignedBigEndianInteger: () => (/* binding */ _decodeUnsignedBigEndianInteger),\n/* harmony export */   _dissectFloat: () => (/* binding */ _dissectFloat),\n/* harmony export */   _encodeBigEndianSignedInteger: () => (/* binding */ _encodeBigEndianSignedInteger),\n/* harmony export */   _encodeUnsignedBigEndianInteger: () => (/* binding */ _encodeUnsignedBigEndianInteger),\n/* harmony export */   _encodeX690BinaryRealNumber: () => (/* binding */ _encodeX690BinaryRealNumber),\n/* harmony export */   _getBit: () => (/* binding */ _getBit),\n/* harmony export */   _integerToBuffer: () => (/* binding */ _integerToBuffer),\n/* harmony export */   _isBasicEncodingElement: () => (/* binding */ _isBasicEncodingElement),\n/* harmony export */   _packBits: () => (/* binding */ _packBits),\n/* harmony export */   _setBit: () => (/* binding */ _setBit),\n/* harmony export */   _setBitInBase256: () => (/* binding */ _setBitInBase256)\n/* harmony export */ });\n/* harmony import */ var _enumerator__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./enumerator */ \"./node_modules/@syncfusion/ej2-pdf/src/pdf/core/security/digital-signature/asn1/enumerator.js\");\n\n/**\n * Convert a buffer to an integer.\n *\n * @private\n * @param {Uint8Array} input - The input buffer as a Uint8Array.\n * @returns {number} The integer value.\n */\nfunction _bufferToInteger(input) {\n    var length = input.length;\n    if (length === 0) {\n        return 0;\n    }\n    if (length > 4) {\n        throw new Error('Input too large to convert to number without BigInt.');\n    }\n    var value = 0;\n    for (var i = 0; i < length; i++) {\n        value = (value << 8) | input[i];\n    }\n    return value;\n}\n/**\n * Converts an integer into a buffer (Uint8Array) representation.\n *\n * @private\n * @param {number} int - The integer to convert to a Uint8Array.\n * @returns {Uint8Array} A Uint8Array representing the integer in its encoded form.\n * @throws {Error} Throws an error if the integer is out of range for 32-bit encoding without BigInt.\n */\nfunction _integerToBuffer(int) {\n    if (int >= -128 && int <= 127) {\n        var array = new Uint8Array(1);\n        var view = new DataView(array.buffer);\n        view.setInt8(0, int);\n        return array;\n    }\n    if (int >= -32768 && int <= 32767) {\n        var array = new Uint8Array(2);\n        var view = new DataView(array.buffer);\n        view.setInt16(0, int, false);\n        return array;\n    }\n    if (int >= -8388608 && int <= 8388607) {\n        var array = new Uint8Array(4);\n        var view = new DataView(array.buffer);\n        view.setInt32(0, int, false);\n        return array.subarray(1);\n    }\n    if (int >= -2147483648 && int <= 2147483647) {\n        var array = new Uint8Array(4);\n        var view = new DataView(array.buffer);\n        view.setInt32(0, int, false);\n        return array;\n    }\n    throw new Error('Integer out of range for 32-bit encoding without BigInt.');\n}\n/**\n * Decodes a signed big-endian integer from a Uint8Array.\n *\n * @private\n * @param {Uint8Array} value - The byte array representing the signed integer in big-endian format.\n * @returns {number} The decoded signed integer.\n * @throws {Error} Throws an error if the byte array is longer than 4 bytes\n */\nfunction _decodeSignedBigEndianInteger(value) {\n    var length = value.length;\n    if (length === 0) {\n        return 0;\n    }\n    if (length > 4) {\n        throw new Error('Number too long to decode.');\n    }\n    var padded = new Uint8Array(4);\n    var isNegative = (value[0] & 0x80) !== 0;\n    padded.fill(isNegative ? 0xFF : 0x00, 0, 4 - length);\n    padded.set(value, 4 - length);\n    var view = new DataView(padded.buffer);\n    return view.getInt32(0, false);\n}\n/**\n * Decodes an unsigned big-endian integer from a Uint8Array.\n *\n * @private\n * @param {Uint8Array} value - The byte array representing the unsigned integer in big-endian format.\n * @returns {number} The decoded unsigned integer.\n * @throws {Error} Throws an error if the byte array is longer than 4 bytes, which cannot be decoded.\n */\nfunction _decodeUnsignedBigEndianInteger(value) {\n    var length = value.length;\n    if (length === 0) {\n        return 0;\n    }\n    if (length > 4) {\n        throw new Error(\"Number on \" + length + \" bytes is too long to decode.\");\n    }\n    var padded = new Uint8Array(4);\n    padded.set(value, 4 - length);\n    var view = new DataView(padded.buffer);\n    return view.getUint32(0, false);\n}\n/**\n * Dissects a floating-point number into its sign, exponent, and mantissa components.\n *\n * @private\n * @param {number} value - The floating-point number to dissect.\n * @returns {{ negative: boolean, exponent: number, mantissa: number }} An object.\n */\nfunction _dissectFloat(value) {\n    var float = new Float64Array([value]);\n    var exponentBitMask = 2146435072;\n    var uints = new Uint32Array(float.buffer);\n    var exponent = (((uints[1] & exponentBitMask) >>> 20) - 1023 - 31);\n    var mantissa = 2147483648 + ((((uints[1] & 1048575) << 11)\n        | ((uints[0] & 4292870144) >>> 21)));\n    return {\n        negative: (value < 0),\n        exponent: exponent,\n        mantissa: mantissa\n    };\n}\n/**\n * Encodes a signed 32-bit integer to a big-endian Uint8Array.\n *\n * @private\n * @param {number} value - The signed integer to encode. Must be within the range -2147483648 to 2147483647.\n * @returns {Uint8Array} A Uint8Array representing the number in big-endian format.\n * @throws {Error} Throws an error if the number is outside the valid 32-bit signed integer range.\n */\nfunction _encodeBigEndianSignedInteger(value) {\n    var minSint32 = -0x80000000;\n    var maxSint32 = 0x7FFFFFFF;\n    if (value < minSint32) {\n        throw Error(\"Number \" + value + \" too small to be encoded as a big-endian signed integer.\");\n    }\n    if (value > maxSint32) {\n        throw Error(\"Number \" + value + \" too big to be encoded as a big-endian signed integer.\");\n    }\n    if (value <= 127 && value >= -128) {\n        return new Uint8Array([\n            (value & 255)\n        ]);\n    }\n    else if (value <= 32767 && value >= -32768) {\n        return new Uint8Array([\n            ((value >> 8) & 255),\n            (value & 255)\n        ]);\n    }\n    else if (value <= 8388607 && value >= -8388608) {\n        return new Uint8Array([\n            ((value >> 16) & 255),\n            ((value >> 8) & 255),\n            (value & 255)\n        ]);\n    }\n    else {\n        return new Uint8Array([\n            ((value >> 24) & 255),\n            ((value >> 16) & 255),\n            ((value >> 8) & 255),\n            (value & 255)\n        ]);\n    }\n}\n/**\n * Encodes an unsigned 32-bit integer into a big-endian Uint8Array.\n *\n * @private\n * @param {number} value - The unsigned integer to encode. Must be within the range 0 to 4294967295.\n * @returns {Uint8Array} A Uint8Array representing the number in big-endian format.\n * @throws {Error} Throws an error if the number is outside the valid 32-bit unsigned integer range.\n */\nfunction _encodeUnsignedBigEndianInteger(value) {\n    var maxUnit32 = 0x00FFFFFFFF;\n    var minUnit32 = 0x0000000000;\n    if (value < minUnit32) {\n        throw new Error(\"Number \" + value + \" too small to be encoded as a big-endian unsigned integer.\");\n    }\n    if (value > maxUnit32) {\n        throw new Error(\"Number \" + value + \" too big to be encoded as a big-endian unsigned integer.\");\n    }\n    var fullBytes = new Uint8Array(4);\n    var view = new DataView(fullBytes.buffer);\n    view.setUint32(0, value, false);\n    var startOfNonPadding = 0;\n    for (var i = 0; i < fullBytes.length - 1; i++) {\n        if (fullBytes[i] === 0x00) {\n            startOfNonPadding++;\n        }\n        else {\n            break;\n        }\n    }\n    return fullBytes.subarray(startOfNonPadding);\n}\n/**\n * Encodes a floating-point number into an X690 binary real number format (as a Uint8Array).\n *\n * @private\n * @param {number} value - The floating-point number to encode.\n * @returns {Uint8Array} A Uint8Array representing the encoded X690 binary real number.\n * @throws {Error} Throws an error if the number is too precise to encode.\n */\nfunction _encodeX690BinaryRealNumber(value) {\n    if (value === 0.0) {\n        return new Uint8Array(0);\n    }\n    else if (Number.isNaN(value)) {\n        return new Uint8Array([_enumerator__WEBPACK_IMPORTED_MODULE_0__._RealValueType.notANumber]);\n    }\n    else if (value === Infinity) {\n        return new Uint8Array([_enumerator__WEBPACK_IMPORTED_MODULE_0__._RealValueType.plusInfinity]);\n    }\n    else if (value === -Infinity) {\n        return new Uint8Array([_enumerator__WEBPACK_IMPORTED_MODULE_0__._RealValueType.minusInfinity]);\n    }\n    var floatComponents = _dissectFloat(value);\n    while (floatComponents.mantissa !== 0 && (floatComponents.mantissa % 2) === 0) {\n        floatComponents.mantissa = floatComponents.mantissa >>> 1;\n        floatComponents.exponent++;\n    }\n    if (floatComponents.exponent <= -1020) {\n        throw new Error(\"Real number \" + value + \" (having exponent \" + floatComponents.exponent + \") \"\n            + 'is too precise to encode.');\n    }\n    var singleByteExponent = ((floatComponents.exponent <= 127)\n        && (floatComponents.exponent >= -128));\n    var firstByte = (128\n        | (value >= 0 ? 0 : 64)\n        | (singleByteExponent ? 0 : 1));\n    var exponentBytes = _encodeBigEndianSignedInteger(floatComponents.exponent);\n    var mantissaBytes = _encodeUnsignedBigEndianInteger(floatComponents.mantissa);\n    var ret = new Uint8Array(1 + exponentBytes.length + mantissaBytes.length);\n    ret[0] = firstByte;\n    ret.set(exponentBytes, 1);\n    ret.set(mantissaBytes, (1 + exponentBytes.length));\n    return ret;\n}\n/**\n * Retrieves the value of a specific bit from a Uint8Array.\n *\n * @private\n * @param {Uint8Array} from - The Uint8Array from which the bit value is extracted.\n * @param {number} bitIndex - The index of the bit to retrieve (0-indexed).\n * @returns {boolean} `true` if the bit is set (1), otherwise `false`.\n */\nfunction _getBit(from, bitIndex) {\n    return ((from[from.length - (Math.floor(bitIndex / 8) + 1)] & (0x01 << (bitIndex % 8))) > 0);\n}\n/**\n * Packs a Uint8ClampedArray of bits into a Uint8Array of bytes.\n *\n * @private\n * @param {Uint8ClampedArray} bits - The array of bits to be packed. Each bit should be 0 or 1.\n * @returns {Uint8Array} A Uint8Array representing the packed bits into bytes.\n */\nfunction _packBits(bits) {\n    var bytesNeeded = Math.ceil(bits.length / 8);\n    var ret = new Uint8Array(bytesNeeded);\n    var byte = -1;\n    for (var bit = 0; bit < bits.length; bit++) {\n        var bitMod8 = bit % 8;\n        if (bitMod8 === 0) {\n            byte++;\n        }\n        if (bits[bit] !== 0) {\n            ret[byte] |= (0x01 << (7 - bitMod8));\n        }\n    }\n    return ret;\n}\n/**\n * Sets or clears a bit at a specific index in a Uint8Array.\n *\n * @private\n * @param {Uint8Array} to - The Uint8Array into which the bit will be set or cleared.\n * @param {number} bitIndex - The index of the bit to set or clear, 0-indexed.\n * @param {boolean} value - A boolean indicating whether to set (true) or clear (false) the bit.\n * @returns {void} This function does not return a value.\n */\nfunction _setBit(to, bitIndex, value) {\n    var byteIndex = to.length - (Math.floor(bitIndex / 7) + 1);\n    if (value) {\n        to[byteIndex] |= (0x01 << (bitIndex % 7));\n    }\n    else {\n        to[byteIndex] &= ~(0x01 << (bitIndex % 7));\n    }\n}\n/**\n * Sets or clears a bit at a specific index in a Uint8Array for Base-256 encoding.\n *\n * @private\n * @param {Uint8Array} to - The Uint8Array into which the bit will be set or cleared.\n * @param {number} bitIndex - The index of the bit to set or clear, 0-indexed.\n * @param {boolean} value - A boolean indicating whether to set (true) or clear (false) the bit.\n * @returns {void} This function does not return a value.\n */\nfunction _setBitInBase256(to, bitIndex, value) {\n    var byteIndex = (to.length - (Math.floor(bitIndex / 8) + 1));\n    if (value) {\n        to[byteIndex] |= (0x01 << (bitIndex % 8));\n    }\n    else {\n        to[byteIndex] &= ~(0x01 << (bitIndex % 8));\n    }\n}\n/**\n * Decodes a Uint8Array to a string using the specified text encoding.\n *\n * @private\n * @param {Uint8Array} bytes - The byte array to decode into a string.\n * @param {string} [encoding ='utf-8'] - The text encoding to use for decoding. Defaults to 'utf-8'.\n * @returns {string} The decoded string.\n * @throws {Error} Throws an error if `TextDecoder` is not available.\n */\nfunction _convertBytesToText(bytes, encoding) {\n    if (encoding === void 0) { encoding = 'utf-8'; }\n    if (encoding !== 'utf-8') {\n        throw new Error('Only utf-8 encoding is supported in fallback.');\n    }\n    var result = '';\n    var i = 0;\n    while (i < bytes.length) {\n        var byte1 = bytes[i++];\n        if (byte1 < 0x80) {\n            result += String.fromCharCode(byte1);\n        }\n        else if (byte1 >= 0xC0 && byte1 < 0xE0) {\n            var byte2 = bytes[i++];\n            result += String.fromCharCode(((byte1 & 0x1F) << 6) | (byte2 & 0x3F));\n        }\n        else if (byte1 >= 0xE0 && byte1 < 0xF0) {\n            var byte2 = bytes[i++];\n            var byte3 = bytes[i++];\n            result += String.fromCharCode(((byte1 & 0x0F) << 12) |\n                ((byte2 & 0x3F) << 6) |\n                (byte3 & 0x3F));\n        }\n        else if (byte1 >= 0xF0) {\n            var byte2 = bytes[i++];\n            var byte3 = bytes[i++];\n            var byte4 = bytes[i++];\n            var codePoint = ((byte1 & 0x07) << 18) |\n                ((byte2 & 0x3F) << 12) |\n                ((byte3 & 0x3F) << 6) |\n                (byte4 & 0x3F);\n            var highSurrogate = ((codePoint - 0x10000) >> 10) + 0xD800;\n            var lowSurrogate = ((codePoint - 0x10000) & 0x3FF) + 0xDC00;\n            result += String.fromCharCode(highSurrogate, lowSurrogate);\n        }\n    }\n    return result;\n}\n/**\n * Checks whether a given Uint8Array is BER-encoded.\n *\n * BER (Basic Encoding Rules) encoding for a sequence starts with the bytes 0x30 0x80\n *\n * @private\n * @param {Uint8Array} input - The input byte array to check.\n * @returns {boolean} True if the input starts with BER indefinite-length encoding; otherwise, false.\n */\nfunction _isBasicEncodingElement(input) {\n    for (var i = 0; i < input.length - 1; i++) {\n        if (input[i] === 0x30 && input[i + 1] === 0x80) {\n            return true;\n        }\n    }\n    return false;\n}\n\n\n//# sourceURL=webpack://@syncfusion/ej2-documents-vue-samples/./node_modules/@syncfusion/ej2-pdf/src/pdf/core/security/digital-signature/asn1/utils.js?");

/***/ })

}]);