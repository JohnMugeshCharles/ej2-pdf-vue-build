"use strict";
/*
 * ATTENTION: The "eval" devtool has been used (maybe by default in mode: "development").
 * This devtool is neither made for production nor for readable output files.
 * It uses "eval()" calls to create a separate source file in the browser devtools.
 * If you are trying to read the output file, select a different devtool (https://webpack.js.org/configuration/devtool/)
 * or disable the default devtool with "devtool: false".
 * If you are looking for production-ready output files, see mode: "production" (https://webpack.js.org/configuration/mode/).
 */
(self["webpackChunk_syncfusion_ej2_documents_vue_samples"] = self["webpackChunk_syncfusion_ej2_documents_vue_samples"] || []).push([["chunk-vendors-node_modules_syncfusion_ej2-pdf-data-extract_src_pdf-data-extract_core_redaction_pdf-p"],{

/***/ "./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/redaction/pdf-path-segment.js":
/*!***************************************************************************************************************!*\
  !*** ./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/redaction/pdf-path-segment.js ***!
  \***************************************************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   _PdfBezierSegment: () => (/* binding */ _PdfBezierSegment),\n/* harmony export */   _PdfLineSegment: () => (/* binding */ _PdfLineSegment),\n/* harmony export */   _PdfPathFigure: () => (/* binding */ _PdfPathFigure),\n/* harmony export */   _PdfPathSegment: () => (/* binding */ _PdfPathSegment)\n/* harmony export */ });\nvar __extends = (undefined && undefined.__extends) || (function () {\n    var extendStatics = function (d, b) {\n        extendStatics = Object.setPrototypeOf ||\n            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||\n            function (d, b) { for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p]; };\n        return extendStatics(d, b);\n    };\n    return function (d, b) {\n        extendStatics(d, b);\n        function __() { this.constructor = d; }\n        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());\n    };\n})();\nvar __assign = (undefined && undefined.__assign) || function () {\n    __assign = Object.assign || function(t) {\n        for (var s, i = 1, n = arguments.length; i < n; i++) {\n            s = arguments[i];\n            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))\n                t[p] = s[p];\n        }\n        return t;\n    };\n    return __assign.apply(this, arguments);\n};\n/**\n * Represents a path figure consisting of line/curve segments.\n *\n * @private\n */\nvar _PdfPathFigure = /** @class */ (function () {\n    function _PdfPathFigure() {\n        this._segments = [];\n        this._isClosed = false;\n        this._isFilled = false;\n        this._startPoint = { x: 0, y: 0 };\n    }\n    /**\n     * Creates a deep clone of the path figure and its segments.\n     *\n     * @private\n     * @returns {_PdfPathFigure} Cloned path figure.\n     */\n    _PdfPathFigure.prototype._clone = function () {\n        var pathFigure = new _PdfPathFigure();\n        pathFigure._isClosed = this._isClosed;\n        pathFigure._isFilled = this._isFilled;\n        pathFigure._startPoint = __assign({}, this._startPoint);\n        for (var _i = 0, _a = this._segments; _i < _a.length; _i++) {\n            var segment = _a[_i];\n            pathFigure._segments.push(segment._clone());\n        }\n        return pathFigure;\n    };\n    return _PdfPathFigure;\n}());\n\n/**\n * Base class for path segments used in PDF drawing.\n *\n * @private\n */\nvar _PdfPathSegment = /** @class */ (function () {\n    function _PdfPathSegment() {\n    }\n    return _PdfPathSegment;\n}());\n\n/**\n * Represents a straight line segment in a PDF path.\n *\n * @private\n */\nvar _PdfLineSegment = /** @class */ (function (_super) {\n    __extends(_PdfLineSegment, _super);\n    function _PdfLineSegment(point) {\n        if (point === void 0) { point = { x: 0, y: 0 }; }\n        var _this = _super.call(this) || this;\n        _this._point = point;\n        return _this;\n    }\n    /**\n     * Clones this line segment.\n     *\n     * @private\n     * @returns {_PdfPathSegment} Cloned line segment.\n     */\n    _PdfLineSegment.prototype._clone = function () {\n        return new _PdfLineSegment(__assign({}, this._point));\n    };\n    return _PdfLineSegment;\n}(_PdfPathSegment));\n\n/**\n * Represents a cubic Bézier curve segment in a PDF path.\n *\n * @private\n */\nvar _PdfBezierSegment = /** @class */ (function (_super) {\n    __extends(_PdfBezierSegment, _super);\n    function _PdfBezierSegment(point1, point2, point3) {\n        if (point1 === void 0) { point1 = { x: 0, y: 0 }; }\n        if (point2 === void 0) { point2 = { x: 0, y: 0 }; }\n        if (point3 === void 0) { point3 = { x: 0, y: 0 }; }\n        var _this = _super.call(this) || this;\n        _this._point1 = point1;\n        _this._point2 = point2;\n        _this._point3 = point3;\n        return _this;\n    }\n    /**\n     * Clones the Bézier segment.\n     *\n     * @private\n     * @returns {_PdfPathSegment} Cloned Bézier segment.\n     */\n    _PdfBezierSegment.prototype._clone = function () {\n        return new _PdfBezierSegment(__assign({}, this._point1), __assign({}, this._point2), __assign({}, this._point3));\n    };\n    return _PdfBezierSegment;\n}(_PdfPathSegment));\n\n\n\n//# sourceURL=webpack://@syncfusion/ej2-documents-vue-samples/./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/redaction/pdf-path-segment.js?");

/***/ }),

/***/ "./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/redaction/pdf-redaction-region.js":
/*!*******************************************************************************************************************!*\
  !*** ./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/redaction/pdf-redaction-region.js ***!
  \*******************************************************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   PdfRedactionRegion: () => (/* binding */ PdfRedactionRegion)\n/* harmony export */ });\n/* harmony import */ var _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @syncfusion/ej2-pdf */ \"./node_modules/@syncfusion/ej2-pdf/index.js\");\n\n/**\n * Represents the redaction process in a PDF document\n *\n * ```typescript\n * // Load an existing PDF document\n * let document: PdfDocument = new PdfDocument(data, password);\n * // Add redactions to the collection\n * let redactions: PdfRedactionRegion[] = [];\n * // Initialize a new instance of the `PdfRedactor` class\n * let redactor: PdfRedactor = new PdfRedactor(document);\n * // Initialize a new instance of the `PdfRedactionRegion` class.\n * let redaction: PdfRedactionRegion = new PdfRedactionRegion(0, {x: 40, y: 41.809, width: 80, height: 90});\n * // Sets the fill color used to fill the redacted area.\n * redaction.fillColor = {r: 255, g: 0, b: 0};\n * redactions.push(redaction);\n * // Add redactions with specified options.\n * redactor.add(redactions);\n * // Apply redactions on the PDF document\n * redactor.redactSync();\n * // Save the document\n * document.save('output.pdf');\n * // Destroy the document\n * document.destroy();\n * ```\n */\nvar PdfRedactionRegion = /** @class */ (function () {\n    /*\n     * Initialize a new instance of the `PdfRedactionRegion` class.\n     *\n     * @param {number} pageIndex The index of the page in the PDF document where redaction is to be applied.\n     * @param {Rectangle} bounds The rectangular bounds that define areas to be redacted\n     * @param {PdfColor} fillColor An array of numbers representing the RGB color used to fill the redacted area.\n     * @param {Boolean} isTextOnly A boolean indicating whether only the text within the redaction bounds should be redacted.\n     *\n     * ```typescript\n     * // Load an existing PDF document\n     * let document: PdfDocument = new PdfDocument(data, password);\n     * // Add redactions to the collection\n     * let redactions: PdfRedactionRegion[] = [];\n     * let redactor: PdfRedactor = new PdfRedactor(document);\n     * let redaction: PdfRedactionRegion = new PdfRedactionRegion(0, {x: 40, y: 41.809, width: 80, height: 90}, true, {r: 255, g: 0, b: 0});\n     * redactions.push(redaction);\n     * redactor.add(redactions);\n     * // Apply redactions on the PDF document\n     * redactor.redactSync();\n     * // Save the document\n     * document.save('output.pdf');\n     * // Destroy the document\n     * document.destroy();\n     * ```\n     */\n    function PdfRedactionRegion(pageIndex, bounds, isTextOnly, fillColor) {\n        /**\n         * Internal flag to indicate text-only redaction.\n         *\n         * @private\n         */\n        this._isTextOnly = false;\n        /**\n         * Internal flag indicating whether appearance is created/used.\n         *\n         * @private\n         */\n        this._appearanceEnabled = false;\n        this._pageIndex = pageIndex;\n        this._bounds = bounds;\n        if (typeof (isTextOnly) !== 'undefined') {\n            this._isTextOnly = isTextOnly;\n        }\n        if (typeof (fillColor) !== 'undefined') {\n            this._fillColor = fillColor;\n        }\n    }\n    Object.defineProperty(PdfRedactionRegion.prototype, \"pageIndex\", {\n        /**\n         * Gets the page index of the PDF document where redaction is to be applied.\n         *\n         *  @returns {number} page index\n         *\n         * ```typescript\n         * // Load an existing PDF document\n         * let document: PdfDocument = new PdfDocument(data, password);\n         * // Add redactions to the collection\n         * let redactions: PdfRedactionRegion[] = [];\n         * let redactor: PdfRedactor = new PdfRedactor(document);\n         * let redaction: PdfRedactionRegion = new PdfRedactionRegion(0, {x: 40, y: 41.809, width: 80, height: 90});\n         * // Gets the page index of the PDF document where redaction is to be applied.\n         * let index: number = redaction.pageIndex;\n         * redactions.push(redaction);\n         * redactor.add(redactions);\n         * // Apply redactions on the PDF document\n         * redactor.redactSync();\n         * // Save the document\n         * document.save('output.pdf');\n         * // Destroy the document\n         * document.destroy();\n         * ```\n         */\n        get: function () {\n            return this._pageIndex;\n        },\n        /**\n         * Sets the page index of the PDF document where redaction is to be applied.\n         *\n         * @param {number} value The page index of the PDF document where redaction is to be applied.\n         *\n         * ```typescript\n         * // Load an existing PDF document\n         * let document: PdfDocument = new PdfDocument(data, password);\n         * // Add redactions to the collection\n         * let redactions: PdfRedactionRegion[] = [];\n         * // Initialize a new instance of the `PdfRedactor` class\n         * let redactor: PdfRedactor = new PdfRedactor(document);\n         * // Initialize a new instance of the `PdfRedactionRegion` class.\n         * let redaction: PdfRedactionRegion = new PdfRedactionRegion(0, {x: 40, y: 41.809, width: 80, height: 90});\n         * // Sets the page index of the PDF document where redaction is to be applied.\n         * redaction.pageIndex = 1;\n         * redactions.push(redaction);\n         * // Add redactions with specified options.\n         * redactor.add(redactions);\n         * // Apply redactions on the PDF document\n         * redactor.redactSync();\n         * // Save the document\n         * document.save('output.pdf');\n         * // Destroy the document\n         * document.destroy();\n         * ```\n         */\n        set: function (value) {\n            this._pageIndex = value;\n        },\n        enumerable: true,\n        configurable: true\n    });\n    Object.defineProperty(PdfRedactionRegion.prototype, \"bounds\", {\n        /**\n         * Gets the bounds of the redaction area.\n         *\n         * @returns {Rectangle} A Rectangle object representing the boundaries of the redaction.\n         *\n         * // Load an existing PDF document\n         * let document: PdfDocument = new PdfDocument(data, password);\n         * // Add redactions to the collection\n         * let redactions: PdfRedactionRegion[] = [];\n         * // Initialize a new instance of the `PdfRedactor` class\n         * let redactor: PdfRedactor = new PdfRedactor(document);\n         * // Initialize a new instance of the `PdfRedactionRegion` class.\n         * let redaction: PdfRedactionRegion = new PdfRedactionRegion(0, {x: 40, y: 41.809, width: 80, height: 90});\n         * // Gets the bounds of the redaction area.\n         * let bounds: Rectangle = redaction.bounds;\n         * redactions.push(redaction);\n         * // Add redactions with specified options.\n         * redactor.add(redactions);\n         * // Apply redactions on the PDF document\n         * redactor.redactSync();\n         * // Save the document\n         * document.save('output.pdf');\n         * // Destroy the document\n         * document.destroy();\n         * ```\n         */\n        get: function () {\n            return this._bounds;\n        },\n        /**\n         * Sets the rectangular bounds that define areas to be redacted\n         *\n         * @param {number} value The rectangular bounds that define the areas to be redacted.\n         *\n         * ```typescript\n         * // Load an existing PDF document\n         * let document: PdfDocument = new PdfDocument(data, password);\n         * // Add redactions to the collection\n         * let redactions: PdfRedactionRegion[] = [];\n         * // Initialize a new instance of the `PdfRedactor` class\n         * let redactor: PdfRedactor = new PdfRedactor(document);\n         * // Initialize a new instance of the `PdfRedactionRegion` class.\n         * let redaction: PdfRedactionRegion = new PdfRedactionRegion(0, {x: 40, y: 41.809, width: 80, height: 90});\n         * // Sets the rectangular bounds that define areas to be redacted\n         * redaction.bounds = {x: 40, y: 41.809, width: 80, height: 90};\n         * redactions.push(redaction);\n         * // Add redactions with specified options.\n         * redactor.add(redactions);\n         * // Apply redactions on the PDF document\n         * redactor.redactSync();\n         * // Save the document\n         * document.save('output.pdf');\n         * // Destroy the document\n         * document.destroy();\n         * ```\n         */\n        set: function (value) {\n            this._bounds = value;\n        },\n        enumerable: true,\n        configurable: true\n    });\n    Object.defineProperty(PdfRedactionRegion.prototype, \"isTextOnly\", {\n        /**\n         * Gets a value indicating whether only the text should be redacted.\n         *\n         * @returns {boolean} value indicating if only text should be redacted.\n         *\n         * // Load an existing PDF document\n         * let document: PdfDocument = new PdfDocument(data, password);\n         * // Add redactions to the collection\n         * let redactions: PdfRedactionRegion[] = [];\n         * // Initialize a new instance of the `PdfRedactor` class\n         * let redactor: PdfRedactor = new PdfRedactor(document);\n         * // Initialize a new instance of the `PdfRedactionRegion` class.\n         * let redaction: PdfRedactionRegion = new PdfRedactionRegion(0, {x: 40, y: 41.809, width: 80, height: 90});\n         * // Gets a value indicating whether only the text should be redacted.\n         * let textOnly: boolean = redaction.isTextOnly;\n         * redactions.push(redaction);\n         * // Add redactions with specified options.\n         * redactor.add(redactions);\n         * // Apply redactions on the PDF document\n         * redactor.redactSync();\n         * // Save the document\n         * document.save('output.pdf');\n         * // Destroy the document\n         * document.destroy();\n         * ```\n         */\n        get: function () {\n            return this._isTextOnly;\n        },\n        /**\n         * Sets a value indicating whether only the text within the bounds should be redacted.\n         * The default value is `false`.\n         *\n         * @param {boolean} value  A boolean flag indicating text only redaction.\n         *\n         * ```typescript\n         * // Load an existing PDF document\n         * let document: PdfDocument = new PdfDocument(data, password);\n         * // Add redactions to the collection\n         * let redactions: PdfRedactionRegion[] = [];\n         * // Initialize a new instance of the `PdfRedactor` class\n         * let redactor: PdfRedactor = new PdfRedactor(document);\n         * // Initialize a new instance of the `PdfRedactionRegion` class.\n         * let redaction: PdfRedactionRegion = new PdfRedactionRegion(0, {x: 40, y: 41.809, width: 80, height: 90});\n         * // Sets the fill color used to fill the redacted area.\n         * redaction.isTextOnly = true;\n         * redactions.push(redaction);\n         * // Add redactions with specified options.\n         * redactor.add(redactions);\n         * // Apply redactions on the PDF document\n         * redactor.redactSync();\n         * // Save the document\n         * document.save('output.pdf');\n         * // Destroy the document\n         * document.destroy();\n         * ```\n         */\n        set: function (value) {\n            this._isTextOnly = value;\n        },\n        enumerable: true,\n        configurable: true\n    });\n    Object.defineProperty(PdfRedactionRegion.prototype, \"fillColor\", {\n        /**\n         * Gets the fill color used to fill the redacted area.\n         *\n         * @returns {PdfColor} value indicating array of numbers representing the RGB color values [R, G, B].\n         *\n         * // Load an existing PDF document\n         * let document: PdfDocument = new PdfDocument(data, password);\n         * // Add redactions to the collection\n         * let redactions: PdfRedactionRegion[] = [];\n         * // Initialize a new instance of the `PdfRedactor` class\n         * let redactor: PdfRedactor = new PdfRedactor(document);\n         * // Initialize a new instance of the `PdfRedactionRegion` class.\n         * let redaction: PdfRedactionRegion = new PdfRedactionRegion(0, {x: 40, y: 41.809, width: 80, height: 90});\n         * // Gets a value indicating whether only the text should be redacted.\n         * let fillColor: PdfColor = redaction.fillColor;\n         * redactions.push(redaction);\n         * // Add redactions with specified options.\n         * redactor.add(redactions);\n         * // Apply redactions on the PDF document\n         * redactor.redactSync();\n         * // Save the document\n         * document.save('output.pdf');\n         * // Destroy the document\n         * document.destroy();\n         * ```\n         */\n        get: function () {\n            return this._fillColor;\n        },\n        /**\n         * Sets the fill color used to fill the redacted area.\n         *\n         * @param {PdfColor} value An array of numbers representing the RGB color used to fill the redacted area.\n         *\n         * ```typescript\n         * // Load an existing PDF document\n         * let document: PdfDocument = new PdfDocument(data, password);\n         * // Add redactions to the collection\n         * let redactions: PdfRedactionRegion[] = [];\n         * // Initialize a new instance of the `PdfRedactor` class\n         * let redactor: PdfRedactor = new PdfRedactor(document);\n         * // Initialize a new instance of the `PdfRedactionRegion` class.\n         * let redaction: PdfRedactionRegion = new PdfRedactionRegion(0, {x: 40, y: 41.809, width: 80, height: 90});\n         * // Sets the fill color used to fill the redacted area.\n         * redaction.fillColor = {r: 255, g: 0, b: 0};\n         * redactions.push(redaction);\n         * // Add redactions with specified options.\n         * redactor.add(redactions);\n         * // Apply redactions on the PDF document\n         * redactor.redactSync();\n         * // Save the document\n         * document.save('output.pdf');\n         * // Destroy the document\n         * document.destroy();\n         * ```\n         */\n        set: function (value) {\n            this._fillColor = value;\n        },\n        enumerable: true,\n        configurable: true\n    });\n    Object.defineProperty(PdfRedactionRegion.prototype, \"appearance\", {\n        /**\n         * Get the appearance of the pdf redaction\n         *\n         * @returns {PdfAppearance} Returns the appearance of the redaction.\n         *\n         * // Load an existing PDF document\n         * let document: PdfDocument =  new PdfDocument(data, password);\n         * // Add redactions to the collection\n         * let redactions: PdfRedactionRegion[] =  [];\n         * let region = { x: 40, y: 43.620000000000005, width: 80, height: 20};\n         * // Initialize a new instance of the `PdfRedactionRegion` class.\n         * let redaction = new PdfRedactionRegion(0, {x: 0, y: 0, width: 80, height: 20}, true);\n         * let font: PdfStandardFont = new PdfStandardFont(PdfFontFamily.helvetica, 10);\n         * redaction.appearance.normal.graphics.drawString('Redacted Text', font, {x: 0, y: 0, width: 80, height: 20}, new PdfBrush({r: 255, g: 0, b: 0}));\n         * redactions.push(redaction);\n         * redaction = new PdfRedactionRegion(0, {x: 0, y: 0, width: 80, height: 20}, true);\n         * region = { x: 40, y: 43.620000000000005, width: 80, height: 20};\n         * redactions.push(redaction);\n         * // Initialize a new instance of the `PdfRedactor` class\n         * let redactor: PdfRedactor = new PdfRedactor(document);\n         * // Add redactions with specified options.\n         * redactor.add(redactions);\n         * // Apply redactions on the PDF document\n         * redactor.redactSync();\n         * // Save the document\n         * document.save('output.pdf');\n         * // Destroy the document\n         * document.destroy();\n         * ```\n         */\n        get: function () {\n            if (typeof (this._appearance) === 'undefined') {\n                this._appearanceEnabled = true;\n                this._appearance = new _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__.PdfAppearance(this.bounds);\n                this._appearance.normal = new _syncfusion_ej2_pdf__WEBPACK_IMPORTED_MODULE_0__.PdfTemplate({ x: 0, y: 0, width: this._bounds.width, height: this._bounds.height });\n            }\n            return this._appearance;\n        },\n        enumerable: true,\n        configurable: true\n    });\n    return PdfRedactionRegion;\n}());\n\n\n\n//# sourceURL=webpack://@syncfusion/ej2-documents-vue-samples/./node_modules/@syncfusion/ej2-pdf-data-extract/src/pdf-data-extract/core/redaction/pdf-redaction-region.js?");

/***/ })

}]);